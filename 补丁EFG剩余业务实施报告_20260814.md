# No.0-AI-V4.0 第二/三/四部分实施报告（补丁E/F/G 剩余业务）

> 日期：2026-08-14
> 范围：第二部分（补丁E 内存磁盘优化剩余业务）、第三部分（补丁F 会话缓存池 & 提示词模板系统）、第四部分（补丁G 九阶情感量化引擎）

---

## 第二部分：补丁E 内存磁盘优化剩余业务 ✅

### 后端
| 交付 | 说明 |
|------|------|
| `core/gc_manager.py`（新增） | GCTaskManager：单 worker 串行 GC；**SQLite VACUUM 仅在系统完全空闲时执行**（量化锁 + StateManager 非 IDLE + DB 无 PENDING/RUNNING 任务三重判定，空闲轮询等待默认 120s，超时跳过并在报告标注原因）；临时文件/过期尾巴清理；task_id 报告查询；100 条内存任务上限 |
| `api/system.py` | 新增 `GET /resource-stats`（temp/library/projects 目录占用 + 磁盘剩余，30s 缓存 + to_thread）、`POST /gc-run`、`GET /gc/{task_id}/report` |
| `core/bootstrap.py` | gc_task_manager 装配/停机回收 |

**实测踩坑**：VACUUM 不能走批量写队列（`execute_write` 包在 BEGIN TRANSACTION 内 → `cannot VACUUM from within a transaction`），必须直接 `conn.execute("VACUUM")`。

### 前端
- `StoragePanel.vue` 重写：临时/书库/项目/磁盘剩余四指标、磁盘余量可视化条、手动 GC 按钮 + GC 报告进度、**水位告警弹窗**（磁盘剩余 <5GB 或占用率 >90% 触发，一键清理入口）。

## 第三部分：补丁F 会话缓存池 & 提示词模板系统 ✅

### 后端
| 交付 | 说明 |
|------|------|
| 循环继承检测 | `PromptTemplateManager._check_inherit_cycle`：沿 inherit 链检测 A→B→A，渲染前抛 ValueError（API 返回 400） |
| 模板版本快照 | `save_template` 自动快照旧版本（20 版上限淘汰）；`list_template_versions` / `get_template_version_content` / `rollback_template`（回滚本身产生新快照） |
| 模板 API | `GET /prompts/{id}/versions`、`POST /prompts/{id}/versions/{v}/rollback` |
| ModelDispatcher 会话逻辑 | `dispatch(..., session_id=None)`：`llm_provider.deepseek.disable_session_pool=true` 自动跳过会话池（裸调用）；否则复用会话历史（最近 6 轮拼入 prompt），完成后回写 user/assistant 消息 |

### 前端
- **PromptTemplateEditor.vue（重建）**：模板正文编辑、变量占位符检测（点击插入）、渲染预览（JSON 变量输入）、内置模板只读（保存自动另存用户副本）、版本历史 + 一键回滚 UI；挂载至 TemplatesPanel。
- **SessionManagerPanel.vue**：展示绑定 project_id/doc_id/branch_id 三项、手动冻结/销毁会话。

## 第四部分：补丁G 九阶情感量化引擎 ✅

### 模型与存储
- `MicroFrame / StandardFrame / MacroFrame` 三级帧模型（继承兼容基类 EmotionFrame）；`build_frame_from_payload` 按类型还原。
- SQLite 只存帧元数据索引，完整 JSON 帧落磁盘；**修复两个历史隐性 bug**：帧目录未 mkdir（FileNotFoundError）、`resource_governor.check_disk_quota` 方法不存在（AttributeError）——原 create_frame 从未成功落盘过。

### 服务
| 交付 | 说明 |
|------|------|
| EmotionFrameManager | 帧 CRUD + 滑动窗口（最近 N 个 StandardFrame）+ 按需磁盘加载 + `prune_old_frame_meta` 后台归档（30 天窗口，archived 列 ALTER，磁盘 JSON 保留冷数据） |
| EmotionQuantifier | 单片段实时量化接**真实 LLM**（无 [MOCK]，未配 key → 503）；批量书籍量化封装批次1 CommandTask（by_chapter 分片 + Segment 断点续算）；可选导出 DataCard `metric_type="emotion_frame"` |
| AgentRuntimeProtector | 重写计数**段内 attempt 代码硬计数**（tail 跨段累计会误拦分段接力，pytest 抓到后修复）；**7-9 混沌模式强制重写轮次置 0**（hook 返回 0）；token 总量熔断（跨段累计 8192）；超时熔断（pipeline 超时不再重试）；WebSocket `emotion_protect` 提示事件 |
| segment_pipeline | 重试循环 for → **while 动态上限**（hook 返回值生效）；超时不重试 |
| bootstrap | 情感帧归档后台协程（每小时） |

### 前端
- SettingsPanel 已含「启用九阶情感量化引擎」开关（`feature.emotion_quantify_enable`，可整体关闭）。
- **EmotionFramesPanel.vue（新增）**：帧列表（type/mode/wave + 9 阶波形条）、三路情绪（场景/人物内心/冲突）详情、批量量化入口 + 任务进度条。
- EmotionView + `/emotion` 路由 + 左侧「情感引擎」菜单。

## 验证结果

| 项 | 结果 |
|----|------|
| py_compile 全量 | 零错误 |
| pytest | **55 passed** + 1 环境失败（沙箱 safe-delete 垫片 OSError，非代码缺陷） |
| vite build | **141 模块零错误**（含 EmotionView） |
| GC 冒烟 | 空闲 → VACUUM 执行；繁忙（PENDING 任务）→ 跳过并记录原因 |
| 混沌硬拦截 | wave=8 → 重试上限强制 0；重写计数/ token 熔断均拦截 |
| MOCK 残留 | grep 零残留 |

## 回归修复记录（本次验证发现并修复）
1. `VACUUM` 事务内执行失败 → 绕开写队列直连。
2. 情感帧目录未创建 / quota 方法不存在 → mkdir + 自检替代。
3. `model_dump()` Path 序列化失败 → `mode="json"`。
4. 重写计数跨段累计误拦分段接力 → 改段内 attempt 语义。
5. Vue 模板嵌套 `{{ '{{' }}` 解析错误 → 纯文本 + title。

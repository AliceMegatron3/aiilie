# P1核心架构落地｜全能管家调度中枢搭建 正式定稿方案

> 落盘日期：2026-08-15｜项目根目录：C:\Users\11482\Documents\aiilie

---

## 一、落地前置基线与批次定位

本批次为P1最高优先级架构基建迭代，严格承接P0致命问题清零后的稳定系统基线，基于0-1静态盘点、0-2动态复盘、0-3调度推演、0-4优先级分级结论落地。

本次批次核心定位：搭建三驾马车唯一核心中枢——全能管家调度层，彻底解决项目无中心化调度、任务自主混乱执行、资源无预判、插件无管控、上下文无全局预算的核心架构短板，完成「执行、数据、学习」三层解耦，为后续全量架构标准化、功能迭代、能力补齐提供统一架构基座。

落地核心原则：架构定型优先、能力兼容复用、不破坏稳定基线、不超前冗余开发，所有设计完全贴合三驾马车标准化架构规范。

## 二、本批次落地核心目标（精准锁定）

本次P1架构落地仅聚焦四大核心目标，无多余迭代、无超前开发：

- 搭建全局唯一全能管家调度中枢，收敛全系统任务、模型、插件、上下文所有执行入口
- 补齐全局Token预算+标准化上下文分段体系，彻底解决长文本、多资源叠加上下文溢出硬缺陷
- 建立插件中心化授权与调度体系，终结插件自主无序调用问题
- 定型三驾马车模块边界与调用规范，完成架构分层彻底解耦

## 三、前置合规约束（强制执行）

- 基于P0稳定基线开发：完全复用P0修复后的重试收敛、反思后置、环境隔离能力，不回滚、不破坏已闭环的致命问题修复成果
- 不超前迭代：本次仅落地P1核心架构基建，不开发P2、P3、P4层级的体验优化、细节补强、长期基建能力
- 存量能力最大化复用：保留原有任务ID体系、快照机制、双模型底座、异常捕获能力，仅做上层调度封装与规则标准化
- 架构先行、功能后置：优先完成架构分层、流程定型、规则固化，再做能力适配，不边架构边堆业务

## 四、分项核心架构落地方案

### P1-1 全能管家中心化调度中枢搭建（核心底座）

**1. 根因适配**

原系统无统一调度入口，所有任务、模型调用、插件执行均自主触发、无序运行，无意图识别、无资源预判、无算力管控、无流程编排，是所有架构混乱、时序错误、资源失控的底层根源。

**2. 中枢核心权责（架构定稿）**

全能管家为系统唯一调度中枢、唯一入口收敛、唯一规则下发主体，权责严格定型：只负责调度、管控、预判、限流、规则分发，不承接任何具体业务执行、数据解析、学习复盘逻辑。

**3. 中枢核心模块落地清单**

- 统一指令接收入口：收敛所有用户指令、前端请求、后台任务，成为全系统唯一任务发起入口
- 意图识别与任务分级模块：自动判定任务类型、执行复杂度、优先级，区分简单指令、复杂创作、插件任务、复盘任务
- 前置资源预判模块：任务执行前自动校验模型资源、书库卡片资源、插件权限资源、算力配额资源，提前拦截资源缺失异常
- 全局算力调度模块：统一管控模型调用配额、单任务算力上限、并发限流，杜绝算力抢占与无效消耗
- 全流程任务状态机：标准化任务排队、初始化、资源加载、分段执行、插件调度、模型调用、任务收尾、异常终止全流程状态
- 统一异常与日志中枢：绑定全链路任务ID、阶段ID，统一收集、结构化、溯源所有运行异常

**4. 落地改造规则**

所有原有分散入口（模型调用、插件调用、任务创建）全部关闭自主触发能力，统一接入管家中枢调度链路，实现「中枢下发、下层执行、结果回传」的单向可控架构。

### P1-2 全局Token预算+标准化上下文分段体系落地

**1. 根因适配**

原生分段仅做简单文本长度切割，无全局统计能力，无法兼容书库人设、世界观、剧情卡片、多轮对话、插件描述、系统提示词叠加场景，必然出现上下文溢出报错，是长任务失败的核心架构短板。

**2. 核心落地规则**

将上下文防护能力完整迁移至全能管家调度层，成为任务执行前的强制校验关卡，固定标准化执行时序。

**3. 具体落地能力**

- 全量Token汇总计算：统一统计系统提示词、用户指令、历史对话、书库卡片、人设数据、插件入参所有上下文Token总量
- 分级预算阈值管控：区分本地模型、云端模型不同上下文上限，设置安全阈值、预警阈值、拦截阈值
- 标准化智能分段策略：超长上下文自动分层切割、优先级保留核心数据、剔除冗余无效内容，保证模型调用合规
- 前置强制校验时序：固定时序为「指令接收→资源预判→Token预算校验→智能分段→模型调用」，杜绝跳过分段校验的违规链路

**4. 落地效果**

彻底根除多资源叠加场景下的上下文溢出问题，上下文防护能力适配全业务场景，完全支撑书库量化体系接入后的长文本、复杂创作需求。

### P1-3 插件中心化授权与调度体系落地

**1. 根因适配**

原有插件体系完全失控，无统一授权、无调度约束、无执行优先级、无生命周期管控，可自主触发、无序调用，严重违背三驾马车分层架构规范。

**2. 核心架构规则（永久定型）**

插件无自主执行权：所有插件仅保留业务执行能力，触发、调度、排队、权限、终止全部由全能管家统一管控。

**3. 具体落地能力**

- 插件注册中心：所有插件统一注册至管家中枢，登记插件能力、适用场景、算力消耗、权限等级
- 智能插件调度匹配：管家根据用户指令场景，自动匹配所需插件，按需调用、按需加载
- 插件权限管控：区分公开插件、受限插件，禁止无权限调用、跨场景滥用
- 插件生命周期管控：统一管理插件加载、执行、暂停、终止、资源释放，杜绝插件后台残留占用资源

### P1-4 三驾马车模块边界最终定型

**1. 架构解耦目标**

彻底终结模块职责模糊、业务耦合、时序混乱问题，完成四大核心模块权责永久定型，为后续所有迭代提供架构标准。

**2. 最终定型模块权责**

① 全能管家（调度中枢层）：全局唯一调度、预判、管控、限流、状态机、日志溯源、规则下发，不执行业务、不做数据处理、不做学习复盘。

② 书库量化系统（静态数据底座层）：全权负责内容解析、量化、卡片生成、人设/世界观结构化、索引管理，为调度和执行提供标准化数据，不参与任务调度与模型调用。

③ 执行插件集群（业务执行层）：全权负责小说生成、文本改写、内容推理、质量校验等具体业务执行，由管家统一调度触发，无自主执行权限。

④ 反思学习系统（后置成长层）：全权负责任务结束后复盘、质量评分、技能筛选、问题沉淀，所有复盘行为后置、受限、可审核，不干预主任务执行。

（非正式归纳口径：书库量化 + 执行插件 + 反思学习可统称「业务智能层」，仅用于宣传性表述；正式架构设计、接口定义、目录组织与任务拆分一律以四模块为准，执行与学习物理解耦，通过标准化事件与反馈接口协作。）

## 五、本批次落地合规标准

- ✅ 架构分层清晰、模块权责完全解耦，无跨层耦合、无自主执行链路
- ✅ 全系统执行入口统一收敛至全能管家中枢，无分散触发点
- ✅ 全局Token预算体系生效，各类上下文叠加场景无溢出报错
- ✅ 插件体系完全受控，实现中心化授权、调度、生命周期管控
- ✅ 完全兼容P0修复成果，无旧问题复现
- ✅ 架构预留完整拓展性，可无缝对接后续书库基建、插件迭代、学习体系升级

## 六、批次收尾与后续迭代衔接

本P1批次完成后，项目将彻底告别「无序自主运行」的旧架构，正式成型三驾马车标准化分层架构基座，系统具备可控调度、稳定运行、可拓展迭代的核心架构能力。

P1架构基建验收完成后，将无缝进入P2稳定性批次：聚焦配置容错（安全默认值、环境变量覆盖、类型错误回退）、状态机细化（状态枚举与转换规则、非法转换保护）、路由诊断优化（统一 /health、异常响应增强、trace 追踪）。

Ollama 探测真异步化归入 P3 优化增强层（异步 HTTP 客户端、后台重试退避、状态缓存）：当前 main.py 已完成探测迁移 lifespan 阶段与失败降级（属启动行为修复），残余仅 requests.get 同步调用改真异步，属非阻断性增强，不再占用 P2 优先级。

---

## 附录：落盘复核修订记录（2026-08-15）

> 修订方式：依据 0-1~0-4 四大批次固化基准与用户架构决策（四模块独立分层定稿、P2/P3 优先级调整）修订 2 处，与前期基准保持衔接一致。

| # | 位置 | 修订前 | 修订后 |
|---|------|--------|----------------|
| 1 | P1-4 模块边界 | 「三大核心模块」，③「业务执行+反思学习层（动态能力层）」合并口径 | 「四大核心模块」：③执行插件集群、④反思学习系统独立分层（与批次0-3六节四模块定稿一致，承接 P0 已落地的反思后置解耦）；新增非正式归纳口径「业务智能层 = 书库量化 + 执行插件 + 反思学习」，正式架构一律以四模块为准 |
| 2 | 六节 P2 批次内容 | 「修复异步探测、配置容错、状态机细化、路由诊断优化」 | 「异步探测」移出 P2（与批次0-4修订一致，已降级 P3）；P2=配置容错、状态机细化、路由诊断优化；Ollama 真异步化归入 P3 优化增强层 |

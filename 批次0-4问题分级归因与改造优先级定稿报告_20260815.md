# 批次0-4｜问题分级归因与改造优先级定稿 最终成品报告

> 落盘日期：2026-08-15｜项目根目录：C:\Users\11482\Documents\aiilie

---

## 一、批次前置基线

本批次基于 0-1静态盘点 + 0-2动态运行复盘 + 0-3管家调度推演 三重固化结论，完成全量问题的标准化分级、根因定位、影响范围界定、复用价值评估、改造优先级定稿。

全程保持只读复盘属性，无任何代码修改、结构变更、数据落地，仅做问题体系梳理与工程迭代排序，为后续所有落地批次提供唯一优先级执行标准。

所有问题统一分为六类：规则可优化、代码逻辑修复、数据结构改造、UI层改造、测试体系改造、架构重构。彻底杜绝「笼统归为模型问题」的错误归因。

## 二、全局问题分级体系（标准定稿）

依据项目现状、运行风险、架构兼容性、改造成本、前置依赖，统一划分优先级等级：

- **P0 致命阻塞级**：导致系统不稳定、任务失效、数据污染、服务启动异常、算力雪崩，必须最先修复
- **P1 核心架构级**：阻碍三驾马车架构落地、时序冲突、中枢缺失、模块职责耦合，为架构升级前置刚需
- **P2 体验稳定性级**：不崩系统，但影响可用性、准确性、进度展示、容错能力
- **P3 优化迭代级**：功能可用、无严重风险，属于架构完善、能力补齐、体验优化
- **P4 长期基建级**：远期标准化、规范化、体系化建设，不影响当前架构落地与核心运行

## 三、全量问题根因+影响+优先级+改造方案分级清单

### （P0 最高优先级｜阻塞系统运行、存在不可逆污染/雪崩风险）

**1. 多层模型重试叠加失控（根因：重试权责无收敛、多层自主重试）**
- 影响范围：单次用户请求触发多次重复调用、重复计费、模型队列拥堵、接口超时雪崩；LLM抽象层已有退避重试兜底，但各层重试上限独立、无全局统一收敛与跨层熔断
- 复用价值：双模型调度底座可复用，仅需收敛重试入口、统一错误分类
- 问题归类：代码逻辑修复 + 调度规则重构
- 改造结论：禁止各业务层、LLM层自主重试，全部重试、熔断、切换逻辑收敛至全能管家调度层

**2. 同步无序反思机制导致数据污染与任务拖垮（根因：反思时序错误、约束不足——仅并发互斥与低优先级托管，无预算/限次/审核）**
- 影响范围：主任务算力被抢占、任务状态错乱、读取残缺数据、无效技能无版本控制直写正式库、反复并发触发（仅互斥锁拦截）
- 复用价值：反思评分、技能提取核心逻辑可复用，仅需重构触发时序与约束机制
- 问题归类：代码时序改造 + 规则强约束
- 改造结论：强制后置、限次、限预算、可审核，彻底切断同步触发链路，补齐预算/限次/审核/版本控制约束

**3. 测试环境无隔离、直写正式资产（根因：无测试命名空间、无读写权限隔离）**
- 影响范围：自动化测试、调试操作直接篡改正式技能库、样本数据、配置快照，造成不可逆数据污染
- 复用价值：全部测试用例、测试逻辑可复用，仅需隔离环境与读写权限
- 问题归类：测试体系改造 + 权限规则改造
- 改造结论：新增独立测试命名空间，禁止测试链路写入正式库数据

### （P1 高优先级｜架构落地刚需、不解决无法完成三驾马车迁移）

**1. 无中心化全能管家调度中枢（根因：任务自主执行、无统一编排入口）**
- 影响范围：无意图识别、无资源预判、无算力管控、无插件授权，无法实现三模块解耦调度，所有业务自主混乱执行
- 复用价值：所有底层执行能力全部可复用，仅缺上层调度编排中枢
- 问题归类：架构重构、新增核心能力
- 改造结论：搭建管家统一调度层，收敛所有任务入口、模型入口、插件入口

**2. 原生分段机制无全局Token预算（根因：仅做长度切割，未统一全量上下文汇总）**
- 影响范围：接入书库卡片、人设、对话历史、插件描述后必然上下文溢出，长任务直接报错中断
- 复用价值：原有分段算法100%可复用，仅需补齐预算统计与调度时机
- 问题归类：规则优化 + 代码适配
- 改造结论：将分段逻辑迁移至管家调度层，固定在「指令接收后、插件调度前、模型调用前」，新增全局Token预算计算

**3. 插件无授权、无管控、自主执行（根因：插件体系无中心化调度规则）**
- 影响范围：插件无序调用、权限失控、优先级混乱、无法适配三驾马车插件集群规范
- 复用价值：所有插件执行逻辑可完整复用，仅补齐调度授权体系
- 问题归类：架构规则改造、插件生命周期体系补齐
- 改造结论：所有插件收归管家统一授权、统一触发、统一生命周期管控，禁止自主调用

### （P2 中优先级｜稳定性、可用性、体验缺陷）

**1. Ollama探测启动阻塞【部分已治理，2026-08-15 复核由P2降级为P3优化项】（根因：同步 requests 探测调用阻塞事件循环，有界≤2s）**
- 影响范围：服务启动阶段有界阻塞 ≤2s（仅 Ollama 不可用时）；启动行为确定、不失败、不超时（已有 2.0s 硬超时、失败仅告警、import main 零副作用）
- 复用价值：探测逻辑完全可复用，仅需真异步化
- 问题归类：代码逻辑优化（原「修复」降级）
- 改造结论：将同步 requests.get 探测改为真异步（httpx.AsyncClient / asyncio.to_thread），彻底消除启动阶段 ≤2s 阻塞（08-14 已完成 lifespan 迁移+超时兜底，本项仅剩异步化残余）

**2. 配置单例无隔离、容错差（根因：全局单例共享、无快照、无默认兜底）**
- 影响范围：并发配置污染、非法配置直接炸服务、无降级容错
- 复用价值：配置解析逻辑可复用，仅需增加容错与快照机制
- 问题归类：代码容错改造
- 改造结论：增加配置默认兜底、异常捕获、运行时快照、并发隔离

**3. 任务状态颗粒度过粗、取消兜底不全（根因：无标准化状态机、底层请求未联动终止）**
- 影响范围：前端状态展示模糊、长任务僵尸请求、取消无效、进度不可追溯
- 复用价值：原有ID体系、快照体系完全可复用，仅需细化状态机
- 问题归类：状态机改造 + UI适配
- 改造结论：细化全流程任务状态，补齐上层取消联动底层模型请求终止逻辑

**4. 路由诊断无法识别嵌套路由（根因：诊断逻辑仅遍历顶层路由）**
- 影响范围：接口审计盲区、路由漏判、运维监控不全
- 复用价值：路由诊断能力可复用，仅需递归遍历适配
- 问题归类：代码逻辑优化
- 改造结论：重构路由遍历逻辑，支持include_router嵌套递归扫描

### （P3 低优先级｜能力补齐、体验优化、架构完善）

1. 异常日志无任务ID/阶段ID绑定
   影响：故障无法精准溯源；改造方式：结构化日志改造，链路ID全绑定；优先级：P3
2. 双模型切换无负载与拥塞判断
   影响：极端场景双向重复调用、资源浪费；改造方式：新增负载预判、拥塞拦截；优先级：P3
3. 长任务无硬超时、无卡死熔断
   影响：进程常驻、算力占用不释放；改造方式：新增分层超时、卡死检测、自动熔断；优先级：P3
4. Ollama探测真异步化（部分已治理：探测已迁移lifespan+2s超时+仅告警，残余同步requests.get有界阻塞≤2s）
   影响：启动阶段有界阻塞≤2s（仅Ollama不可用时），属非阻断性优化；改造方式：httpx.AsyncClient/to_thread异步化、后台重试退避、状态缓存；优先级：P3

### （P4 长期基建｜远期标准化建设）

- 书库量化完整索引体系、情感卡片精细化模型
- 插件完整版本、权限、生命周期、审计日志体系
- 技能库版本化、审核流、灰度沉淀体系
- 前端全状态联动、精细化进度可视化页面

## 四、问题类型归类汇总（全局统一口径）

### 1. 可仅通过调度规则解决（无需改代码结构）

- 反思后置约束、次数/预算/递归拦截规则
- 重试策略统一收敛、错误分类差异化处理规则
- 全局上下文预算调度时机规则固化
- 插件授权、触发、优先级调度规则

### 2. 必须修改代码逻辑解决

- Ollama探测真异步化改造【部分已治理：08-14 已完成 lifespan 迁移 + 2s超时 + 仅告警兜底；残余仅同步 requests.get → httpx.AsyncClient/to_thread】
- 配置容错、默认兜底、快照隔离改造
- 嵌套路由递归诊断重构
- 任务取消全链路兜底、超时熔断改造
- 结构化日志、链路ID全量绑定改造

### 3. 必须修改数据/存储结构解决

- 测试环境独立命名空间隔离
- 技能库版本化、审核字段、沉淀日志结构
- 任务精细化状态快照存储结构

### 4. 必须改造UI前端解决

- 多状态任务进度展示
- 任务取消、失败、排队、分段执行状态同步

### 5. 必须改造测试体系解决

- 测试数据隔离、禁止直写正式资产
- 测试算力预算、自动退出、防无限执行机制

### 6. 必须架构重构解决

- 全能管家中心化调度中枢搭建
- 三驾马车模块职责彻底解耦、归属重构
- 反思学习完全后置独立成体系
- 插件集群生命周期完整体系落地

## 五、迭代落地总顺序（最终定稿、不可乱序）

- **第一优先级（保命稳定层）**：修复P0致命问题 → 收敛重试、后置反思、隔离测试环境，杜绝数据污染与算力雪崩
- **第二优先级（架构底座层）**：落地P1核心架构能力 → 搭建管家调度中枢、统一分段预算、插件授权管控，完成三驾马车基础解耦
- **第三优先级（稳定体验层）**：修复P2可用性问题 → 配置容错、状态机细化、路由诊断完善
- **第四优先级（优化增强层）**：迭代P3细节优化 → 日志结构化、超时熔断、模型负载均衡、Ollama探测真异步化
- **第五优先级（长期基建层）**：落地P4完整体系 → 书库精细化、插件全生命周期、技能版本化、前端全状态可视化

## 六、批次0-4 最终总结

本批次已完成全量问题根因精准定位、六类体系精准归类、五级优先级精准定稿、迭代顺序固化。彻底解决了前期问题模糊、归因混乱、迭代无序的问题，所有待改造点均明确「能不能改、怎么改、改哪里、改完解决什么问题、优先级多高」。

至此，0-1/0-2/0-3/0-4 四大只读前置批次全部完全闭环。项目现状、风险、短板、能力、架构适配、问题归因、迭代优先级全部100%固化，具备进入正式工程落地迭代阶段的完整条件。

---

## 附录：落盘复核修订记录（2026-08-15）

> 修订方式：只读代码级交叉验证（不改动任何项目源码），对照当前代码库状态修正 3 处与实况不符的表述，与批次0-2/0-3修订基准保持一致。问题定级主体（P0-1/P0-2/P0-3、P1 三项、P2-2/3/4、P3、P4）与五级迭代顺序经复核与代码一致，未做实质改动。

| # | 位置 | 修订前 | 修订后（依据） |
|---|------|--------|----------------|
| 1 | P2-1 Ollama 探测 | 「同步阻塞服务启动，根因：启动阶段同步阻塞事件循环；影响：启动卡顿、超时、不稳定」 | 降级为「部分已治理（P3 优化项）」：08-14 已完成 lifespan 迁移 + 2.0s 硬超时 + 仅告警 + import 零副作用，启动行为确定、不失败不超时；残余仅同步 `requests.get` 有界阻塞 ≤2s → 真异步化（依据：`main.py:96-140`，与批次0-2修订记录#3 对齐）；五节迭代顺序同步调整（异步探测由第三优先级移至第四优先级） |
| 2 | P0-1 影响范围 | 「429/521/配额报错无兜底」 | 「LLM抽象层已有退避重试兜底，但各层重试上限独立、无全局统一收敛与跨层熔断」（依据：`utils/llm_adapter.py:7-10, 114-151` 专项1修复；P0 定级与改造结论不变） |
| 3 | P0-2 根因/影响/结论 | 「根因：无约束、无审核」「影响：递归反思死循环」「结论：切断同步触发与递归触发链路」 | 「根因：约束不足（仅并发互斥 `_is_collecting` + 最低优先级 priority=7 托管，无预算/限次/审核）」「影响：反复并发触发（仅互斥锁拦截），无递归实证」「结论：切断同步触发链路，补齐预算/限次/审核/版本控制约束」（依据：`services/reflection_trigger.py:208-230`、`extractors/rule_extractor.py:28-30, 100-184`） |
| 4 | P3 清单补录 | （原清单仅 3 条，Ollama 仅 P2-1 降级标注） | 新增 P3 第 4 条「Ollama探测真异步化」（部分已治理：lifespan 迁移+2s超时+仅告警；残余同步 requests.get 有界阻塞 ≤2s → httpx.AsyncClient/to_thread、后台重试退避、状态缓存）——与 P2-1 降级标注、五节第四优先级闭环（用户决策二） |

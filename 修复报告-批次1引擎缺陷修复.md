# 修复报告 — No.0 AI V4.0 批次1底层分段命令执行引擎缺陷修复

- 修复日期：2026-08-13
- 依据：批次1审查报告（总体可用度 65/100，P0 阻断业务风险 3 项、P1 重要缺陷 1 项、P2 架构/运维/接口 2 项）
- 约束：**增量修补**，不删除现有业务逻辑；`CommandTask` / `Segment` 对外数据模型不变；API 契约向后兼容；兼容 Windows PyInstaller 打包（全部路径 pathlib，复用 core.path_resolver）
- 不改动范围：批次2/3/4业务代码

---

## 一、修复总览

| 编号 | 级别 | 缺陷 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | P0 | ResultMerger 全量加载文本合并 OOM | ConcatenateStrategy 流式分块重写（64KB/块），产物落盘返回路径 | ✅ |
| 2 | P0 | SegmentPipeline 大 result_content 写入 SQLite 致 DB 膨胀 | 阈值 `task.max_db_payload_bytes` 分级：短内容入库、超阈值置空只存 output_path | ✅ |
| 3 | P0 | GlobalRouter 未调用批次1引擎（集成断层） | 长任务构造标准 CommandTask 提交 `submit_task()`，完整走拆分→分段→尾巴→合并链路 | ✅ |
| 4 | P1 | TailContext 无独立管理类、无落盘双模式 | 新建 `services/tail_context_manager.py` | ✅ |
| 5 | P2 | 任务查询接口散落 projects.py、手写 RAW SQL | 新建 `api/task_api.py`，旧路由 307 代理保留 | ✅ |
| 6 | P2 | TempFileManager 缺配额/LRU/启动清理 | 补丁E：磁盘配额 + LRU 淘汰 + 启动孤儿扫描 | ✅ |
| 7 | P2 | 异常中断恢复粒度不足、尾巴无单独落盘 | 失败现场快照落盘 + 重启恢复注入 | ✅ |

## 二、逐项修复说明

### P0-1：SegmentPipeline 数据库存储改造（防 SQLite 膨胀）

- `services/segment_pipeline.py` 新增 `_should_store_in_db()`：result_content 字节数 ≤ `task.max_db_payload_bytes`（默认 10KB）时入库；超阈值时 `result_content` 置空，仅持久化 `output_path`。
- 读取语义统一为「磁盘优先」：`result_merger.resolve_segment_content()` / `iter_segment_chunks()` 优先判断 output_path 文件存在则从磁盘读，否则回退 result_content。
- Pydantic 模型字段零改动，上层业务无感知。
- 回退开关：`task.allow_large_payload_in_db: true` 恢复旧逻辑（全部入库）。

### P0-2：ResultMerger 流式合并（百万字防 OOM）

- `ConcatenateStrategy` 重写：不再全量读入内存 list 后 join，改由 `iter_segment_chunks()`（64KB 分块）逐块写入最终输出文件，任意时刻内存只驻留一个分块。
- `StructuredStrategy` 同步改为流式 JSON 转义写入。
- `merge()` 接口签名不变，返回最终结果文件路径（落盘于 `<appdata>/No0_AI_V4/data/results/`）。
- 新增 `merge_and_load()` 兼容模式：合并产物 ≤ `result_merger.memory_load_threshold_bytes`（默认 1MB）时返回内存字符串，超阈值返回 None（文件已落地，上层改按路径读取）。

### P0-3：GlobalRouter 与批次1引擎集成（最重要）

- `services/global_router.py`：
  - 新增 `batch1_task_manager` 注入参数（main.py 装配）；
  - 长任务识别 `_is_long_task()`：创作/书籍量化/深度思考模式（think/deep/reasoning/deepseek-r1）/ 强制分段规则 / 超过 `task.long_command_min_chars`（2000字）；
  - 长任务不再生成 `_lazy_creation` 闭包，改为 `_submit_to_batch1()` 构造标准 CommandTask 提交 `task_manager.submit_task()`，真实走 CommandSplitter → SegmentPipeline → tail 接力 → ResultMerger 全链路；
  - 短任务保留原闭包队列逻辑做兼容；QUANTIZATION 旧闭包内部亦做适配层转发；
  - 优先级映射：PriorityLevel LV6→2、LV4→4、LV1→7、LV7→1（批次1为 1 最高）；
  - 兼容开关 `global_router.use_batch1_engine`，关闭后切回旧闭包队列。
- `main.py`：
  - lifespan 装配批次1引擎全链路（CommandSplitter/SegmentPipeline/TailContextManager/ResultMerger/TempFileManager）；
  - 新增 `batch1_engine_worker` 后台协程循环消费 `process_next()`；
  - 启动时扫描孤儿临时目录，保护 DB 中非终态任务的分段目录；
  - `GlobalRouter` 注入 `batch1_task_manager`；`app.state.batch1_task_manager` 供 task_api 使用。
- `api/system.py`：`/queue` 大屏接口新增 `batch1_engine` 实时负载信息（挂载状态/队列深度/跟踪任务数）。

### P1-4：TailContextManager 独立模块（新建）

`services/tail_context_manager.py` 提供：

- `store()`：双模式存储——尾巴 JSON ≤ `task.tail_memory_threshold_bytes`（默认 64KB）驻留内存（原样 dict）；超阈值自动序列化到 `temp/tails/{task_id}/`，内存仅保存 `__tail_offload__` 极小标记 dict（防 SQLite 与内存双重膨胀）；
- `materialize()`：统一读取入口，标记 dict 自动从磁盘还原，业务无感知；
- TTL 过期标记（`task.tail_ttl_seconds` 默认 86400s）+ `cleanup_expired()`；
- `save_failure_snapshot()/load_failure_snapshot()/discard_failure_snapshot()`：分段异常时失败现场尾巴强制落盘（无视阈值），任务重启恢复时注入现场继续执行；
- 回退开关：`task.enable_tail_disk_offload: false` 全部尾巴驻留内存（旧行为）。

`SegmentPipeline` 已全部改为调用该模块，删除内部硬编码尾巴传递逻辑（含原 `tail_context.json` 调试写入）。

### P2-5：api/task_api.py 独立任务接口（新建）

| 接口 | 说明 |
|---|---|
| `GET /api/v1/tasks/{task_id}` | 任务信息（旧 /status 接口字段超集：含 retry_count/progress） |
| `GET /api/v1/tasks/{task_id}/segments` | 分段列表（大内容以 output_path/result_available 标记，不整体搬运） |
| `POST /api/v1/tasks/{task_id}/cancel` | 取消任务（内存标记 + DB 直取消双路径） |
| `GET /api/v1/tasks/{task_id}/audit-report` | 审计报告 |

- 全部接口内部调用 `services/task_manager.TaskManager` 新增封装方法 `get_task_detail()` / `get_task_segments()` / 增强版 `cancel_task()`，**零 RAW SQL**。
- `api/projects.py` 原 `/tasks/{task_id}/status` 保留，改为 307 重定向代理至新接口，上层不报错。

### P2-6：TempFileManager 增强（补丁E）

- 磁盘配额：`task.temp_max_size_gb`（默认 2GB），超限按 LRU（最久未访问）淘汰已完成任务的临时目录；
- 运行中任务保护：`register_active_task()/unregister_active_task()`，受保护目录免于淘汰与启动扫描；
- 启动扫描：`startup_cleanup()` 清理 orphaned 遗留目录，跳过 `tails` 保留目录；开关 `task.cleanup_orphan_on_start`；
- `get_disk_usage()` 新增配额占用统计。

### 其他同步修改

- `core/database.py`：新增 `get_active_tasks()`（PENDING+RUNNING 非终态查询，供启动恢复与目录保护）。
- `services/task_manager.py`：`initialize()` 恢复非终态任务**及分段记录**（RUNNING 回退 PENDING 重新入队）；新增 `_clamp_priority()` 兼容修复（历史 DB 中由旧调度链路写入的 PriorityLevel 枚举值 70/60/40/10 超出 CommandTask 的 1-7 校验范围，恢复时映射为 1/2/4/7，否则 lifespan 启动即崩）；`process_next()` 登记/解除运行中任务保护；`_segment_from_row()` DB 行恢复 Segment 模型。
- `config/config.yaml`：新增 `task`、`global_router`、`result_merger` 全部配置段。

## 三、配置清单（config/config.yaml 新增）

```yaml
task:
  max_db_payload_bytes: 10240          # P0-1 DB 载荷阈值（10KB）
  allow_large_payload_in_db: false     # P0-1 旧逻辑回退开关
  tail_memory_threshold_bytes: 65536   # P1-4 尾巴落盘阈值（64KB）
  enable_tail_disk_offload: true       # P1-4 尾巴落盘总开关
  tail_ttl_seconds: 86400              # P1-4 尾巴 TTL
  temp_max_size_gb: 2                  # P2-6 磁盘配额
  cleanup_orphan_on_start: true        # P2-6 启动孤儿清理
  long_command_min_chars: 2000         # P0-3 长任务判定阈值
global_router:
  use_batch1_engine: true              # P0-3 引擎集成开关（关闭回退旧闭包）
result_merger:
  memory_load_threshold_bytes: 1048576 # P0-2 内存加载阈值（1MB）
```

## 四、验证结果

`tests/test_batch1_engine.py` 冒烟验证脚本（独立可运行）7 项全部通过：

```
[P0-1] 短结果入库 + 流式合并文件 ✔
[P0-2] 大结果不入库 / 流式合并 / merge_and_load 阈值 ✔
[P1-4] 大尾巴双模式落盘 + materialize ✔
[P1-4] 失败现场快照 + 重启恢复链路 ✔
[P2-6] 启动孤儿清理(保护) + LRU 磁盘配额 ✔
[P2-5] task_api 四接口（详情/分段/取消/审计） ✔
[P0-3] GlobalRouter→批次1引擎集成 + 开关回退 ✔
```

覆盖要点：400段规模下大结果 DB 字段置空且 output_path 文件存在；流式合并体积正确；大尾巴落盘标记可物化还原；分段异常快照落盘后重启恢复（快照注入使 processed_count 由 1 续为 2、成功后快照清除）；孤儿目录清理与运行中保护；LRU 淘汰最久未访问目录；路由任务经 `route_command` 真实入引擎队列并消费完成；`use_batch1_engine=false` 时回退旧闭包（task_id 前缀 `create_`）。

另：全部改动文件通过 `py_compile` 语法检查；既有 `tests/test_integration.py` 回归运行 8/9 通过（唯一失败项 `test_patch_f_session_pool` 为沙箱 safe-delete 垫片拦截 session 文件删除所致——`SAFE_DELETE_FAIL_CLOSED: recycle-bin-unavailable`，属测试环境限制，与本次改动无关，生产环境不受影响）。

## 五、兼容性保证

1. `CommandTask` / `Segment` Pydantic 模型零字段改动；`tail_context` 仍为 `dict[str, Any]`（落盘标记是极小 dict）；
2. 所有新增开关默认值与推荐配置一致，**全部关闭后行为回退原版**；
3. 旧路由 `/api/v1/tasks/{task_id}/status` 不删除（307 代理），新接口返回字段为其超集；
4. `MergeStrategy.merge()` / `TaskManager.submit_task()` 等既有调用签名不变；
5. 批次2/3/4 代码零改动；批次2 后续可通过 `pipeline.set_execution_hook()` 注入真实业务钩子。

## 六、遗留说明

1. 任务完成后 `process_next` 收尾会清理分段临时目录（合并产物已持久在 results 目录），分段 output_path 成为历史引用，属预期行为；
2. 单段执行含 2 次 DB commit，400 段实测约 80s（历史实现即如此），若后续需要可做批量提交优化（不在本次范围内）；
3. 大结果入库回退开关 `allow_large_payload_in_db` 开启时 DB 膨胀风险自行承担，仅供调试。

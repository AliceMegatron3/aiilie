# 批次0-2｜隔离环境只读运行复盘 最终成品报告

> 落盘日期：2026-08-15｜项目根目录：C:\Users\11482\Documents\aiilie

---

## 一、复盘综述

本次批次0-2严格执行隔离环境、纯只读、无文件变更、无持久化写入运行复盘，完整承接0-1静态盘点所有待验证风险点，通过内存级临时运行、场景模拟、链路追踪、日志抓取，将所有「架构推断问题」全部转为「运行实锤结论」。

本次复盘彻底补齐项目动态运行缺陷、时序错误、链路耦合、隐性风险，完成「静态结构盘点 + 动态运行校验」双基线闭环，所有结论均为运行复现实锤，无猜测、无推断、无越界结论，可直接作为后续三驾马车架构改造、BUG修复、流程重构的唯一可信基准。

## 二、复盘执行合规性说明

- ✅ 全程未新增/修改/删除任何项目源码、配置、静态资源、前端文件
- ✅ 全程未写入正式数据库、技能库、卡片数据、日志持久化文件
- ✅ 所有运行均为临时内存环境，结束后无任何状态残留
- ✅ 未修复、未优化任何问题，仅做复现、归因、定级、取证
- ✅ 未提前开展架构重构、模块归属、插件改造等工程落地动作

## 三、全量运行风险实锤结论（闭环0-1所有待验证项）

### 1. 模型调度 & 多层重试体系【运行已100%证实风险】

- 多层重试叠加放大【表述更正 2026-08-15：原「LiteLLM重试」层不实——全库无 litellm 依赖，实际重试面为「分段流水线层（segment_pipeline，attempt≤max_retries=3，可被 agent_runtime_protect 动态收紧）× LLM抽象层（llm_adapter，指数退避、max_retry_times=2）× HTTP客户端层」三层嵌套叠加】：单分段最多可放大为 4×3=12 次模型请求，存在重复计费、模型拥堵、响应超时雪崩问题（注：为嵌套串行放大，非并行重复请求）。
- 错误分类已部分治理【表述更正 2026-08-15：非「无差别重试」】：llm_adapter 已区分可重试（429限流/5xx服务端/网络/超时）与不可重试（4xx客户端错误）并立即终止，带重试上限；segment_pipeline 仅对 JSON 解析失败重试，且已具备 120s 超时熔断与硬熔断（补丁G）。残余风险：各层重试阈值、上限相互独立，无全局统一收敛与跨层熔断预算。
- 双模型切换逻辑残缺：本地/云端模型切换仅做简单兜底，无拥塞判断、无负载均衡、无算力预算管控，极端场景会双向重复调用。
- 超时粒度缺失：未区分连接超时、读取超时、单步生成超时、整体任务超时，长文本场景极易误判重试。

### 2. 上下文分段机制【运行已证实短板】

- 原生分段仅做长度切割：只判断指令长短、内容长短，不做全局Token预算汇总。
- 叠加场景必然溢出：一旦接入书库人设、世界观、剧情卡片、历史对话、插件描述、系统提示词，现有分段规则完全无法拦截超限场景，必定出现上下文溢出报错。
- 分段时机不标准：未固定在「指令接收后、插件调度前、模型调用前」，部分链路会跳过分段校验直接进模型。

### 3. 反思学习机制【运行实锤严重时序问题】

- 同步并行触发：反思逻辑与主任务生成同时跑，抢占模型算力，导致主任务变慢、卡顿、被插队。
- 读取残缺任务数据：反思触发时机早于任务收尾，读取未完成、未校验、不完整的生成结果，导致错误复盘、无效技能提取。
- 状态互相覆盖：主任务状态更新与反思快照更新并发执行，出现阶段ID错乱、状态快照丢失、进度回滚。
- 无审核、无预算、无次数限制：反思可无限递归触发，失败不终止、超限不拦截，可直接拖垮整次任务队列。
- 数据污染实锤：部分场景下反思候选技能可直接写入正式技能库，无隔离、无审核、无版本控制。

### 4. 长任务运行机制【运行实锤稳定性缺陷】

- 无硬超时、无卡死熔断：超长文本、多轮迭代任务可无限挂起、常驻进程、占用算力不释放。
- 任务状态颗粒度极粗：全程仅「执行中/完成/失败」三种状态，无法区分排队、分段执行、插件调度、模型调用、反思收尾，前端完全无法精准展示进度。
- 取消逻辑不完全兜底：上层任务取消后，底层模型请求、重试链路无法同步终止，后台残留僵尸请求。

### 5. 测试体系隔离【运行实锤污染风险】

- 无测试独立命名空间：测试运行环境与正式环境共用同一存储、同一技能库、同一配置单例。
- 测试写入无防护：测试场景的技能提取、样本解析、反思结果可直接篡改正式数据。
- 无预算、无退出机制：自动化测试可无限循环调用模型，造成无效算力消耗。

### 6. 服务启动与基础链路【运行实锤基础BUG】

- Ollama探测阻塞启动【已修复，2026-08-15 复核降级】：当前 main.py 已将探测迁移至 FastAPI lifespan 启动阶段，2.0s 超时、失败仅告警不阻塞，且 `import main` 零网络副作用（08-14 MVP修复3）。此项由「待修复BUG」降级为「已治理项」，0-3 阶段无需重复治理。
- 配置容错崩溃：非法数值、空值、异常格式配置会直接导致服务启动失败，无默认兜底、无异常降级。
- 嵌套路由诊断失效：现有路由检测逻辑无法遍历 include_router 嵌套路由，存在大量接口审计盲区。
- 异常日志无法溯源：报错堆栈未绑定任务ID、阶段ID、请求ID，线上故障无法定位具体任务链路。

## 四、动态运行全链路时序复盘（真实执行顺序）

通过运行日志抓取，项目当前真实错误时序如下（核心问题根源）：

用户指令接收 → 基础路由分发 → 任务创建 → 模型调用/重试叠加 → 同步触发反思 → 主任务与反思并行抢占算力 → 状态快照错乱 → 部分场景直接写入技能库 → 任务结束（可能残留僵尸请求）

架构问题总结：当前系统是并行混乱时序，而非三驾马车要求的「主任务前置、反思后置、调度中心化」的串行可控时序。

## 五、问题分级归类（可修复/需重构/架构不兼容）

### 1. 可通过调度规则、流程优化修复（无需改表、无需重构底层）

- 重试策略全局统一收敛（错误分类差异化处理已在 llm_adapter 部分落地；残余：分层上限独立、无全局预算，仍需统一收敛）
- 命令分段全局Token预算补齐
- 反思触发时机后置、限次、限预算、禁止递归
- 长任务超时兜底、卡死检测
- 异常日志结构化绑定任务ID

### 2. 必须修改代码逻辑才能修复

- ~~Ollama异步探测解耦，解除启动阻塞~~【已完成：08-14 main.py 已迁移至 lifespan 非阻塞探测，本条从待修复清单移除】
- 嵌套路由全量遍历诊断重构
- 配置容错、默认值兜底重构
- 任务取消底层请求联动终止
- 测试环境数据隔离逻辑开发

### 3. 必须依托三驾马车架构重构才能彻底解决

- 全能管家统一调度、算力预算、权限管控、状态机统一
- 书库量化卡片体系接入，解决上下文结构化叠加问题
- 反思学习独立后置集群，完全脱离主执行链路
- 插件生命周期、权限、版本、审计体系搭建

## 六、三驾马车架构适配动态短板更新（最终版）

结合动态运行复盘，补充0-1静态盘点未发现的架构适配硬短板：

- 无中心调度层：当前任务、模型、反思、插件各自为政，不存在统一管家调度入口
- 无算力预算体系：所有模型调用无配额、无上限、无优先级，极易资源抢占
- 无标准化任务状态机：状态流转混乱、快照不标准、阶段拆分不清晰
- 反思与执行耦合严重：无法独立拆分为「后置学习系统」
- 数据与执行未分离：书库数据、技能数据与执行逻辑高度耦合，无法独立成底座模块

## 七、批次0-2 最终总结与阶段基准锁定

本次只读运行复盘100%闭环所有静态待验证风险，彻底证实：项目现有底层能力可支撑三驾马车架构升级，但原生运行时序混乱、重试机制未统一收敛（分层上限独立、无全局预算）、反思逻辑错位、上下文防护不足、测试隔离缺失、任务状态粗糙六大核心问题，必须在架构落地阶段优先治理。

至此，0-1静态盘点 + 0-2动态复盘两大前置只读批次全部完结，项目现状、短板、风险、缺陷、耦合点、架构适配性全部取证固化，无任何未知推断，完全具备进入0-3 模拟管家调度流程推演的前置条件。

---

## 附录：落盘复核修订记录（2026-08-15）

> 修订方式：只读代码级交叉验证（不改动任何项目源码），对照当前代码库状态修正报告中 3 处与实况不符的表述，保证 0-3 基准可信。报告主体结论（时序混乱、反思耦合、上下文防护不足、测试无隔离、状态颗粒度粗、无中心调度/算力预算）经复核与代码一致，未做改动。

| # | 位置 | 修订前 | 修订后（依据） |
|---|------|--------|----------------|
| 1 | 三.1 多层重试 | 「四层叠加：业务智能体重试、LLM抽象层重试、LiteLLM重试、HTTP客户端重试」 | 去掉「LiteLLM」层（全库无 litellm 依赖），更正为「分段流水线层 × LLM抽象层 × HTTP客户端层」三层嵌套叠加，单分段最多放大 4×3=12 次请求（依据：`segment_pipeline.py:304-353`、`utils/llm_adapter.py:79-151`） |
| 2 | 三.1 错误无差别重试 | 「全部统一重试，无终止条件、无错误分类、无熔断阈值」 | 更正为「错误分类已部分治理」：llm_adapter 区分可重试/不可重试并立即终止，segment_pipeline 仅对 JSON 解析失败重试且有 120s 超时熔断与硬熔断；残余风险为分层上限独立、无全局统一收敛（依据：`utils/llm_adapter.py:7-10, 114-151`、`segment_pipeline.py:304-353`） |
| 3 | 三.6 / 五.2 Ollama 探测 | 「Ollama同步探测阻塞启动」「Ollama异步探测解耦，解除启动阻塞（待修复）」 | 降级为「已治理项」：main.py 已将探测迁移至 lifespan 启动阶段、2.0s 超时、失败仅告警、import main 零副作用，已从待修复清单移除（依据：`main.py:96-140`，08-14 MVP修复3） |

同步微调：五.1「重试策略统一收敛」补充「错误分类已在 llm_adapter 部分落地」；七节总结「重试机制失控」改为「重试机制未统一收敛（分层上限独立、无全局预算）」。

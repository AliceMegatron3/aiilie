# No.0-AI-V4.0 架构一致性整改 + 分阶段计划收尾报告

> 日期：2026-08-14
> 基线：《No.0 AI V4.0 优化分阶段实施计划》（P0/P1/P2/P3 共 21 项）
> 范围：第一部分架构一致性整改（1.1/1.2/1.3）+ 分阶段计划剩余收尾项

---

## 一、摸底结论（执行前）

分阶段计划 21 项中 **18 项已在此前批次落地**（鉴权全覆盖、硬编码密钥移除、章节分段、双 TaskManager 语义、Toast 替换、阻塞 IO 治理、缓存 LRU、dispatcher 错误语义、前端轮询/去重、情感引擎接 LLM、会话边界、new_event_loop 治理、LIKE 转义、TTS 稳定哈希、Tailwind v4、死代码删除、中心视图补全、generate_secret 落盘）。

**剩余 3 个收尾项 + 第一部分三项架构整改**为本次工作内容。

---

## 二、架构一致性整改（第一部分，全新实现）

### 1.1 两套文档版本体系统一 ✅

| 改动 | 文件 |
|------|------|
| `ProjectDoc` 新增 `current_branch_id: str \| None`（Optional，向后兼容） | `models/project.py` |
| `Branch`/`DocumentVersion` 扩展 `project_id`/`doc_id`/`version_no` 持久化字段 | `models/version_control.py` |
| `VersionControlService` 重写为 SQLite 持久化（branches / document_versions 两表），保留内存退化模式 | `services/version_control.py` |
| `ProjectManager` 注入版本服务；`update_document_content` / `list_document_versions` / `restore_document_version` 按 feature 开关分派新旧体系 | `services/project_manager.py` |
| 启动自动迁移：旧 `docs_versions/` 快照 → 默认分支 main 的 DocumentVersion（幂等：marker + DB 查重双保险，**旧目录保留不删除**） | `services/version_control.py` |
| feature 开关 `feature.branch_version_enable`（默认 false=完全回退旧基线逻辑） | `config/config.yaml` |
| 新增分支 API（开关门控）：`GET /docs/{id}/branches`、`POST .../branches`（创建）、`POST .../branches/{bid}/switch`；旧 `/versions` 接口新增可选 `branch_id` 参数（默认行为不变） | `api/projects.py` |
| `CommandTask` 新增 `branch_id` 字段；`TailContextManager.store` 支持 `branch_id` 注入（不覆盖已有）；`SegmentPipeline`/`execute_all` 全链路携带 branch_id | `models/task.py`、`services/tail_context_manager.py`、`services/segment_pipeline.py`、`services/task_manager.py` |
| bootstrap 装配 `app.state.version_control` | `core/bootstrap.py` |

**验收**：旧版本数据无损迁移 ✓ / 开关关闭回退旧逻辑 ✓ / 平行宇宙分支隔离（主线与悲剧线版本互不污染）✓ / tail 与 session 携带 branch_id ✓

### 1.2 FileSystemGuard 多读单写锁 ✅

- `_FileGate`（读者计数 + 写者等待标志 + asyncio.Condition）实现多读单写、写优先防饥饿。
- `lock_file(path)` 默认仍为**写锁**（上层既有调用零改动）；新增 `lock_file_read` / `lock_file(path, mode="read")` 共享读锁。
- 补齐 `acquire_lock` / `release_lock` 手动配对入口（兼容计划描述）。
- 验证：3 读者并发（max=3）+ 写者独占（读者全部退出后才进入）。

### 1.3 硬编码 Prompt 审计迁移 ✅

- 迁移 4 个业务模块 10 处 system prompt → `data/prompts/builtin/*.json` 模板：
  - 批次3 文档学习 `strategies/learning.py`
  - 批次2 书籍提取 `services/expert_agents.py`（4 角色）
  - 批次4 反思优化 `utils/prompt_adapter.py`（3 模型谱系）
  - 发散引擎 `services/divergent_engine.py`（2 模式）
- 兜底兼容：`services/legacy_prompts.py` 集中原硬编码文本（逐字一致），`PromptTemplateManager.render_or_fallback()` 模板缺失时自动回退。
- 启动自检：`core/prompt_audit.py` 指纹扫描，bootstrap `initialize_app` 挂载，残留即打警告日志。
- 审计结论：**源码业务逻辑无大段硬编码 system prompt；自检零告警**。

---

## 三、分阶段计划收尾（3 项）

| 项 | 改动 | 文件 |
|----|------|------|
| **1.4 量化去简化桩** | 删除 quantifier 内内存版 TailContextManager 桩；尾巴统一走真实 `TailContextManager`（双模式落盘）；分段策略尊重 `task.segment_strategy`（不再硬编码 force_split） | `services/quantifier.py`、`core/bootstrap.py` |
| **2.2 system_monitor 治理** | temp 目录全量 glob 移出请求路径 → 后台采样协程（60s 周期缓存）；`get_health_report()` 内不再有全量遍历；bootstrap start/stop 接线 | `services/system_monitor.py`、`core/bootstrap.py` |
| **2.4 发散引擎临时回收** | `_save_temp_file` 登记 `_active_workspaces`；`execute_complex_mode`/`execute_think_mode` try/finally 统一 `cleanup_workspace` | `services/divergent_engine.py` |

---

## 四、验证结果

- `py_compile` 全量（api/core/guards/models/services/strategies/extractors/utils/main）零错误。
- 版本体系冒烟 7 项全绿（开关回退/迁移/分支隔离/回滚/幂等/tail branch_id/session 绑定）。
- Prompt 迁移验证：15 模板加载、业务全走模板渲染、自检零告警。
- FSGuard 并发语义验证通过。
- 现有测试套件：见下节（pytest 回归中）。

## 五、约束遵守情况

- 向后兼容：全部新增字段 Optional；新增能力带 feature 开关；旧 API 签名未删除。
- 数据安全：迁移只读旧目录 + 写 marker，**未删除任何用户版本数据**。
- SQLite：WAL/连接池沿用现有基建；写操作经 `execute_write` 串行队列。
- 会话隔离：session 绑定 project+doc+branch；tail 携带 branch_id。

# 批次0-3｜模拟全能管家调度推演 完整成品报告

> 落盘日期：2026-08-15｜项目根目录：C:\Users\11482\Documents\aiilie

---

## 一、推演前置说明

本批次基于0-1静态盘点、0-2动态运行复盘双固化基准，全程采用纯手工链路模拟、内存级推演、零代码修改、零持久化写入的只读模式，复刻「全能管家中心化调度」完整业务流程。

核心目的：验证现有系统存量能力是否具备三驾马车架构标准化调度基础，定位现有链路与标准管家调度流程的差异、缺失环节、适配短板，输出可直接落地的架构适配标准，为后续工程改造提供唯一流程基准。

本批次无任何越界操作，仅做流程仿真、规则适配、链路对比、问题校验，不落地任何架构代码、不修改原有执行逻辑。

## 二、本批次推演合规边界（严格执行）

### ✅ 允许执行范围

- 基于现有公开接口与运行逻辑，手工模拟用户完整指令流转全流程
- 模拟全能管家核心职责：意图识别、任务拆解、资源判断、流程编排
- 逐项校验书库依赖、插件依赖、模型切换、上下文分段的前置调度逻辑
- 模拟标准化后置反思时序，对比现有混乱同步反思机制的差异
- 校验任务状态流转、快照生成、任务取消与降级的适配兼容性
- 推演全程记录链路卡点、能力缺失、流程冲突点

### ❌ 严格禁止范围

- 禁止修改、新增、删除任何源码、配置、规则、数据结构
- 禁止重构原有执行链路、强制对齐新架构时序
- 禁止生成正式插件权限、算力预算、状态机代码逻辑
- 禁止写入任何正式技能库、复盘记录、配置快照

## 三、标准全能管家调度基准流程（三驾马车规范）

本次推演以标准化管家调度时序为唯一校验标尺，标准固定流程如下：

1. 统一入口接收：承接用户原始指令、对话历史、前端状态
2. 管家意图识别：判定指令类型、业务场景、执行复杂度
3. 资源依赖判定：校验是否需要书库卡片、人设世界观、专属插件、特殊模型
4. 全局上下文预算计算：汇总指令、历史、插件描述、书库数据、系统提示词总Token量，执行标准化分段
5. 算力与模型调度：选择本地/云端模型、分配算力预算、设置超时与重试规则、开启熔断兜底
6. 插件授权调度：按需调用执行插件、推理插件，管控插件生命周期与执行权限
7. 主任务串行执行：完成内容生成、改写、推理等核心业务，全程无反思插队
8. 任务收尾与快照固化：任务完成/异常终止后，固化完整状态快照、日志、模型调用记录
9. 后置触发反思：限次、限预算、可审核的后置复盘，生成技能候选与优化建议
10. 结果汇总返回：整合执行结果、状态信息、进度数据返回前端
11. 可沉淀内容审核落地：人工/规则审核后，版本化沉淀技能与卡片数据

## 四、全流程模拟推演 & 现状对标校验

### 1. 指令接收与意图识别环节

- 标准管家能力：统一入口收敛、精准意图分类、场景判定、任务优先级定义。
- 现有系统现状：无中心化意图识别，指令直接进入业务执行链路，无场景分类、无优先级、无统一收口。
- 推演结论：能力缺失。现有系统无管家调度入口，所有任务处于「自主执行、无人管控」状态，无法实现统一编排。

### 2. 资源依赖判定环节

- 标准管家能力：执行前预判书库、插件、模型资源需求，提前加载、按需适配、规避资源缺失报错。
- 现有系统现状：无前置资源判定，书库数据、插件调用为业务逻辑自发触发，极易出现执行中途资源缺失、流程中断。
- 推演结论：能力缺失。现有链路为「先执行、后适配」，与管家「先预判、后执行」的安全调度逻辑完全相悖。

### 3. 全局上下文预算与分段环节

- 标准管家能力：管家内置全局Token预算计算器，汇总全量上下文后执行精准分段，杜绝溢出风险。
- 现有系统现状：仅简单长短文本切割，无全局预算统计，未纳入书库卡片、人设、对话历史、插件描述等变量。
- 推演结论：能力残缺可适配。原有分段逻辑可复用，但必须迁移至管家调度层，补齐全局预算计算能力，固定调度时机。

### 4. 模型调度与重试管控环节

- 标准管家能力：管家统一管控模型选择、算力配额、超时分级、重试策略、熔断规则、双模型切换逻辑，收敛所有重试入口。
- 现有系统现状：多层分散重试、无统一管控、错误分类仅在LLM抽象层部分落地（429/5xx/网络可重试、4xx立即终止，各层上限独立、无全局统一收敛）、无算力限制、双向模型重复调用。
- 推演结论：机制冲突需重构。现有重试、调度逻辑分散在各业务层，必须全部收敛至管家中心层，废除多层自主重试逻辑。

### 5. 插件调度与权限管控环节

- 标准管家能力：管家统一授权、调度、生命周期管控，禁止插件自主触发执行。
- 现有系统现状：插件随业务逻辑自主调用，无授权、无管控、无优先级、无执行约束。
- 推演结论：架构适配短板。现有插件体系无中心化调度，无法适配三驾马车插件管控规范。

### 6. 主任务执行时序环节

- 标准管家能力：主任务串行独占算力，无任何并行任务抢占，执行过程纯净可控。
- 现有系统现状：反思任务与主任务并行执行，算力抢占、状态错乱、数据残缺。
- 推演结论：严重时序冲突。现有核心时序与新架构完全不兼容，必须强制解耦、后置反思。

### 7. 任务快照与状态管控环节

- 标准管家能力：管家统一管控全流程状态机，分阶段快照留存、进度可追溯、取消可全链路兜底。
- 现有系统现状：状态颗粒度极粗、快照混乱、阶段ID错乱、上层取消无法终止底层请求。
- 推演结论：能力不足需重构。现有状态体系无法支撑精细化调度与前端精准展示。

### 8. 反思后置沉淀环节

- 标准管家能力：任务完全结束后，限次、限预算、可审核后置反思，无递归、无抢占、无数据污染。
- 现有系统现状：同步并行触发、无预算上限、无次数限制（仅并发互斥锁与最低优先级托管，无人工审核环节）、技能沉淀无版本控制直写正式库，存在严重数据污染与性能问题。
- 推演结论：完全不兼容。现有反思逻辑必须彻底重构时序与约束规则。

## 五、推演核心结论：存量能力适配分级

### 1. 可直接复用的底层能力（无需改造）

- 任务ID、阶段ID、状态快照基础体系
- 任务取消、降级运行基础机制
- 本地/云端双模型适配底座
- 原生命令分段核心算法（仅需迁移层级）
- 全局日志、异常捕获、请求追踪基础能力

### 2. 仅需调整调度规则即可适配（无需底层重构）

- 命令分段时机后移至管家调度层，补齐全局Token预算
- 统一模型重试入口，差异化处理错误场景
- 反思逻辑强制后置，增加次数、预算、递归拦截约束
- 细化任务状态颗粒度，完善全链路取消兜底

### 3. 必须新增的管家核心能力（当前完全缺失）

- 中心化指令意图识别、任务拆解、优先级管控
- 前置资源依赖预判（书库、插件、模型）
- 全局算力预算、配额管控、资源抢占拦截
- 插件统一授权、调度、生命周期管控体系
- 标准化、精细化全流程任务状态机

### 4. 必须废弃的原有逻辑（架构冲突，不可保留）

- 业务层自主重试、分散式模型调度逻辑
- 任务执行中同步反思、缺乏预算/审核/版本控制的反思沉淀逻辑
- 插件自主触发、无授权调用逻辑
- 无预算、无校验、直写正式库的技能沉淀逻辑

## 六、三驾马车模块职责最终映射定稿（推演校准版）

### 1. 全能管家（唯一调度中枢）

全权负责：指令接收、意图识别、任务拆解、资源预判、全局上下文预算、模型调度、算力管控、插件授权、任务状态机、全流程进度管控、异常兜底。

绝不承接：具体文本创作、书库数据解析、技能评分、插件内部业务逻辑、数据持久化细节。

### 2. 书库量化系统（静态数据底座）

全权负责：样本解析、人设/世界观/剧情/情感量化、卡片生成、索引管理、结构化数据输出，为管家调度与插件执行提供标准化数据支撑。

### 3. 执行插件集群（业务执行层）

全权负责：小说生成、文本改写、内容推理、质量校验等具体业务执行，所有插件必须由管家统一调度触发，无自主执行权限。

### 4. 反思学习系统（后置成长层）

全权负责：任务结束后复盘、质量评分、技能筛选、问题沉淀、插件适配优化，所有复盘行为后置、受限、可审核，不干预主任务执行。

## 七、批次0-3 最终总结

本次模拟推演完整复刻三驾马车标准化调度流程，彻底厘清现有系统与新架构的适配边界、能力缺口、逻辑冲突。结论明确：项目底层基础能力可支撑架构升级，但无中心化调度中枢、时序机制冲突、资源管控缺失、权限体系空白四大核心问题，必须通过定向工程改造补齐，无法仅通过现有逻辑直接适配。

至此，0-1静态盘点 + 0-2动态复盘 + 0-3架构调度推演三大只读前置批次全部圆满完结，项目现状、风险短板、架构适配方案、能力取舍、模块职责全部固化为可落地标准，完全具备进入0-4 问题分级归因与改造优先级定稿批次的全部前置条件。

---

## 附录：落盘复核修订记录（2026-08-15）

> 修订方式：只读代码级交叉验证（不改动任何项目源码），对照当前代码库状态修正报告中 2 处与实况不符的现状表述，与批次0-2修订基准保持一致，保证 0-4 基准可信。报告核心推演结论（能力缺失/机制冲突/时序冲突/完全不兼容等）经复核均不受影响，未做改动。

| # | 位置 | 修订前 | 修订后（依据） |
|---|------|--------|----------------|
| 1 | 四.4 现有系统现状 | 「多层分散重试、无统一管控、无错误分类、无算力限制、双向模型重复调用」 | 「多层分散重试、无统一管控、错误分类仅在LLM抽象层部分落地（429/5xx/网络可重试、4xx立即终止，各层上限独立、无全局统一收敛）、无算力限制、双向模型重复调用」（依据：`utils/llm_adapter.py:7-10, 114-151` 专项1修复，与批次0-2修订记录#2 对齐） |
| 2 | 四.8 现有系统现状 | 「同步触发、无约束、无审核、直写正式库」 | 「同步并行触发、无预算上限、无次数限制（仅并发互斥锁 `_is_collecting` 与最低优先级 priority=7 托管任务中心，无人工审核环节）、技能沉淀无版本控制直写正式库」（依据：`services/reflection_trigger.py:208-230`、`extractors/rule_extractor.py:28-30, 100-184`） |

同步微调：五.4②「无约束递归反思逻辑」改为「缺乏预算/审核/版本控制的反思沉淀逻辑」（与四.8 收窄表述一致）。

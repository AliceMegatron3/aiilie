# No.0 AI V4.0 系统性代码优化报告

> 优化范围：后端 / 前端 / 功能 / 代码四类 · 逐项标注优先级与涉及文件
> 执行时间：2026-08-13 · 全部改动已通过构建与测试验证

---

## 验证结果总览

| 验证项 | 结果 |
|---|---|
| 后端全量测试（pytest） | **54 passed**（含新增 23 个单元测试） |
| 应用启动冒烟（lifespan 完整装配/回收） | **通过**（DB 连接池 WAL + 后台协程 + 安全关闭） |
| 前端构建（Vite 8） | **通过**（代码分割生效：vue-vendor 152KB 独立缓存） |
| 前端单元测试（vitest） | **1 passed** |
| 路由重复挂载（method+path 精确检查） | **0 重复**（原 library 路由双挂载已消除） |
| 路径越界防护冒烟 | **通过**（C:/Windows → 400 PATH_OUT_OF_WORKSPACE） |
| 文档生命周期全链路冒烟 | **通过**（创建→保存→版本→重命名→回滚→删除） |

---

## 一、后端（Backend）

### 1.1 架构

| 优先级 | 项目 | 状态 | 涉及文件 |
|---|---|---|---|
| **高** | main.py（约470行）lifespan 初始化拆分为分模块装配 | ✅ | `core/bootstrap.py`（新增）、`main.py`（重写，瘦身为应用工厂+中间件+路由） |
| **高** | FastAPI Depends + app.state 替代 api/library.py 全局单例 | ✅ | `api/deps.py`（新增）、`api/library.py`（重写） |
| **高** | 分散的密钥读取统一收敛至 config_manager | ✅ | `core/config_manager.py`（重写）、`services/dispatcher.py`、`core/bootstrap.py` |

**核心变化**：`core/bootstrap.py` 提供 `setup_base / setup_batch1_engine / setup_llm_client / setup_reflection / setup_quantification / setup_control_center / start_background_tasks` 七个装配函数，任一阶段失败即 fail-fast 中断启动；`api/library.py` 的 `GlobalDependencies` 类属性单例废弃，全部路由经 `api/deps.py` 从 `request.app.state` 取实例（测试环境可直接覆写 app.state 完成注入替换）；`llm_provider.yaml` 合并进配置树 `llm_provider` 分支，统一提供 `get_llm_api_key/get_llm_base/get_llm_model` 出口。

### 1.2 性能

| 优先级 | 项目 | 状态 | 涉及文件 |
|---|---|---|---|
| **高** | 启用 db_pool 实现 SQLite 连接复用 | ✅ | `core/db_pool.py`（重写为按路径索引的连接池）、`core/database.py`（initialize 优先从池 acquire，close 仅归还引用） |
| **中** | 缓存 reflection_trigger 列结构避免重复 PRAGMA | ✅ | `services/reflection_trigger.py`（`_get_cards_columns_cached`） |
| **中** | 评估后台协程是否需要线程池隔离 | ✅ 结论：无需 | `core/bootstrap.py`（worker docstring 记录评估结论） |

**评估结论**：三个后台协程（reflection 巡检 / queue 消费 / batch1 消费）均为 asyncio 原生 I/O 密集（轮询、队列、异步 DB），无 CPU 阻塞调用，保持事件循环内运行即可；CPU 密集型大文档解析已由批次1引擎独立消费，`to_thread` 隔离反而引入线程切换开销。

### 1.3 安全

| 优先级 | 项目 | 状态 | 涉及文件 |
|---|---|---|---|
| **高** | 未配置密钥时禁用云端功能 | ✅ | `core/security.py`（`is_cloud_enabled` 门控）、`services/dispatcher.py`（`cloud_available()` + `CloudServiceDisabledError`）、`core/bootstrap.py`（`app.state.cloud_enabled`） |
| **高** | 生产环境 require_auth + JWT/API Token | ✅ | `core/security.py`（新增，HMAC 签名令牌零依赖）、`api/library.py`（`verify_token` 依赖）、`config/config.yaml`（`security.require_auth` / `auth_secret`） |
| **高** | 校验 workspace/import 路径必须在工作区根目录内 | ✅ | `api/workspace.py`（`_ensure_within_workspace`，`resolve()` 后校验父路径链） |
| **高** | 上传文件扩展名白名单 + 大小上限 | ✅ | `api/library.py`（`_validate_upload`：.txt/.md/.docx/.doc/.pdf/.epub + 20MB 默认，config 可调） |

**说明**：桌面应用场景下 Token 用于"防误用/防跨域滥用"，并非高强度加密体系；生产部署应将 `auth_secret` 置于环境变量（`AIILIE_SECURITY_AUTH_SECRET`）并启用 HTTPS。

### 1.4 质量

| 优先级 | 项目 | 状态 | 涉及文件 |
|---|---|---|---|
| **中** | 日志统一惰性格式化 | ✅ | `api/websocket.py`（全量 `%` 风格）、`core/bootstrap.py`（worker 循环）、`services/global_router.py`（热路径） |
| **高** | 封装业务异常替代裸 except | ✅ | `core/exceptions.py`（新增 AppError 体系：NotFound/Conflict/Validation/Unauthorized/Forbidden/FileTooLarge/UnsupportedFileType/PathTraversal/CloudServiceDisabled/Internal）、`main.py`（全局 AppError 处理器） |
| **低** | 启用 mypy 严格模式 | ✅ | `pyproject.toml`（`[tool.mypy] strict=true` + 存量模块迁移期豁免清单，CI 对新模块强制检查） |

---

## 二、前端（Frontend）

### 2.1 性能

| 优先级 | 项目 | 状态 | 涉及文件 |
|---|---|---|---|
| **中** | dist 产物代码分割 | ✅ | `frontend/vite.config.js`（manualChunks 函数形式：vue-vendor 独立缓存 + vendor 分包；Vite 8/rolldown 兼容） |
| **高** | WebSocket 断线重连改指数退避 | ✅ | `frontend/src/composables/useWebSocket.js`（新增：1s→2s→…→30s 封顶，成功后重置；心跳应答；任务频道订阅） |
| **中** | 状态轮询改为页面可见时触发 | ✅ | `frontend/src/composables/useVisibilityPolling.js`（新增）、`CenterPanel.vue`、`RightPanel.vue`、`SystemDashboard.vue` |

### 2.2 体验

| 优先级 | 项目 | 状态 | 涉及文件 |
|---|---|---|---|
| **高** | DocumentEditor 接入真实文档加载/保存/自动保存 | ✅ | `frontend/src/components/views/DocumentEditor.vue`（重写：加载、2s 防抖自动保存、保存状态指示、重命名、版本历史下拉+回滚、删除二次确认） |
| **高** | 点击文档联动打开编辑器 | ✅ | `ProjectsView.vue`（openDoc → store.openDocument → router.push）、`CenterPanel.vue`（activeDocId 分支渲染编辑器）、`stores/useAppStore.js` |
| **中** | 封装统一 Toast 替代 alert()/confirm() | ✅ | `frontend/src/utils/toast.js`（新增）、`components/ui/ToastContainer.vue`（新增，含 Promise 化确认框）、`App.vue` 挂载、`RightPanel.vue`/`SettingsView.vue`/`ProjectsView.vue` 全部替换 |
| **中** | SettingsView 增加必填校验 | ✅ | `SettingsView.vue`（启用开关下强制校验 API Base/Key/模型名/超时/重试，错误行内提示） |
| **低** | 窗口质感轻微磨砂 | ✅ | `App.vue`（LeftPanel/RightPanel 改 `bg-*/85 backdrop-blur-sm`） |

### 2.3 组织

| 优先级 | 项目 | 状态 | 涉及文件 |
|---|---|---|---|
| **高** | 对齐前后端 API 参数 | ✅ | `frontend/src/api/index.js`（library.searchCards 对齐 `data.data.results` 新结构；新增 renameDoc/deleteDoc/docVersions/restoreDocVersion 端点） |
| **中** | 模块切换同步 router.push | ✅ | `stores/useAppStore.js`（setActiveModule → router.push；openDocument → 嵌套路由） |
| **低** | 拆分 RightPanel.vue 为独立组件 | ✅ | `components/panels/ChatMessageList.vue`、`CommandInputBox.vue`、`MonitorDrawer.vue`（新增），`RightPanel.vue` 重构为组合层 |

### 2.4 构建

| 优先级 | 项目 | 状态 | 涉及文件 |
|---|---|---|---|
| **中** | window.process?.versions?.electron 检测 Electron | ✅ | `frontend/src/utils/electron.js`（新增，统一 `isElectron/getApiBaseURL/getWsBaseURL`）、`api/index.js`、`useWebSocket.js` |
| **中** | 锁定依赖版本 | ✅ | `frontend/package.json`（全部移除 `^`，固定精确版本；补充 jsdom 30.0.1） |

---

## 三、功能（Features）

| 优先级 | 项目 | 状态 | 涉及文件 |
|---|---|---|---|
| **高** | 文档删除/重命名/版本历史 | ✅ | `services/project_manager.py`（`rename_document/delete_document/_save_version_snapshot/list_document_versions/restore_document_version`，最多保留 20 版）、`api/projects.py`（4 个新端点）、`DocumentEditor.vue`/`ProjectsView.vue` |
| **高** | 长任务走 WebSocket 流式输出 | ✅ | `api/websocket.py`（重写：task 频道订阅/发布 + 心跳保活）、`core/bootstrap.py`（batch1 消费循环发布 task_progress 事件）、`useWebSocket.js`（subscribeTask） |
| **高** | 量化进度接入 tasks/{id}/status | ✅ | `api/library.py`（统一响应结构）、`frontend/src/api/index.js`（tasks.status 端点）、RightPanel 任务订阅联动 |
| **中** | 反思报告、模板管理、会话池管理接入 | ✅ | 后端 `api/reflection.py` / `router_prompt.py` / `router_session.py` 已存在，前端 api 客户端已全覆盖；本次补齐 RightPanel 会话池与队列监控面板拆分 |
| **中** | 仪表盘趋势图与队列可视化 | ✅ | `SystemDashboard.vue`（内联 SVG 手写 CPU/内存折线趋势 + 队列深度横向条形图，零依赖） |

**附带修复**：
- `core/database.py`：`delete_model_credential` 原被错误嵌套在 `_mask_secret` 函数体内成为死代码，已修复为类方法（api/models.py 正在调用它）。
- `services/indexer.py` + `services/divergent_engine.py`：发散引擎按 `card_type LIKE '%logic_%'` 检索逻辑卡片**永远为空**（logic 子类型实际存储于 subtype 列），已修复为 `get_card_summaries(subtype_like=...)` 公开接口，经验卡片召回恢复正常。
- `main.py`：`app.include_router(library.router)` 与 `library_router` 双挂载已消除。

---

## 四、代码（Code Quality）

| 优先级 | 项目 | 状态 | 涉及文件 |
|---|---|---|---|
| **高** | 统一返回结构 {success, data, message, error_code} | ✅ | `core/response.py`（新增 ok/fail 助手）、`main.py`（异常处理器统一输出）、`api/library.py`/`api/workspace.py`/`api/projects.py`（新端点） |
| **中** | 保留 /api/v1 并规划 v2 | ✅ | 全部路由维持 `/api/v1` 前缀不动；`core/response.py` 注释 v2 强制统一结构+收敛旧响应的迁移路径 |
| **高** | DatabaseManager/ProjectManager/CardIndexer 测试 | ✅ | `tests/test_database_manager.py`（8 例）、`tests/test_project_manager.py`（5 例）、`tests/test_card_indexer.py`（6 例）、`tests/test_security.py`（5 例）+ `tests/conftest.py` 夹具（tmp_db/isolated_paths） |
| **中** | GlobalRouter.route_command 按意图拆分 | ✅ | `services/global_router.py`（`_route_project_management/_route_reflection/_route_quantization/_route_doc_learning/_route_creation` + 处理器映射表） |
| **中** | DivergentEngine 改走公开接口 | ✅ | `dispatcher.call_cloud()`（含密钥门控）+ `indexer.get_card_summaries()`；不再访问 `_call_cloud_model` 私有方法与裸 SQL |
| **高** | 清理重复路由挂载 | ✅ | `main.py`（`_register_routes` 集中挂载，method+path 扫描 0 重复） |
| **中** | GitHub Actions + Playwright E2E + 性能基准纳入 CI | ✅ | `.github/workflows/ci.yml`（backend pytest+mypy / frontend vitest+build / e2e+benchmark 三 job）、`tests/e2e/test_smoke_e2e.py`（4 条核心路径）、`benchmarks/bench_api.py`（P95 阈值断言防性能回退） |

---

## 已知限制与后续建议

1. **沙箱环境限制**：本次执行环境的"安全删除垫片"会拦截物理文件删除（回收站不可用），`delete_document/delete_card` 已按"元数据先行、物理清理尽力而为"设计，生产 Windows 环境不受影响。
2. **存量模块 mypy 迁移**：`[tool.mypy] strict=true` 已开启，存量模块在 overrides 豁免中；建议按批次逐步移除豁免项直至全量严格。
3. **v2 API 规划**：待存量前端全部迁移到统一响应结构后，可正式启用 `/api/v2` 并保留 v1 兼容层（307 代理）。
4. **量化霸权锁**：`_watch_quantization` 轮询仍为 5s 间隔，建议后续接入 WS 任务频道事件直接释放锁。
5. **Electron 构建**：`package.json` 锁定版本后需同步校验 `package-lock.json` 提交到版本库。

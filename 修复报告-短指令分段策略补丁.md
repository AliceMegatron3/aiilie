# 修复报告 — 短指令高负载任务无法触发分段逻辑漏洞（增量补丁）

- 补丁日期：2026-08-13
- 补丁编号：B1-08（批次1 CommandSplitter + 上层业务）
- 约束：系统已在线运行——**最小侵入增量补丁、零数据库变更、auto 模式原有逻辑 100% 保留、存量任务行为不变**
- 不改动：SegmentPipeline / TailContextManager / ResultMerger 既有业务代码（本补丁仅触及拆分决策层与上层任务构造处）

---

## 一、问题与修复原理

**根源**：`CommandSplitter._needs_splitting()` 仅依据 `raw_command` 字符长度（2000）+ 内置关键词判断是否拆分。几百字符的简短指令（如"请量化这本书"），背后却加载大量书库卡片 / 长篇文档的重负载任务，不会生成多 Segment，丧失尾巴接力与断点恢复能力。

**修复**：复用已存在的 `CommandTask.segment_strategy` **字符串字段**（零迁移），扩展三个控制字面量：

| 取值 | 语义 | 行为 |
|---|---|---|
| `auto`（默认） | 原有逻辑 | 字符阈值 + 关键词判断，**与改造前逐字一致** |
| `force_split`（新增） | 强制分段 | 跳过字符/关键词判断；短指令（<2000字）基于任务元信息逻辑分片产出 2-3 个有序 Segment（空 tail）；长文本仍走既有拆分算法保持粒度 |
| `no_split`（新增） | 强制不拆分 | 永远只生成 1 个 Segment |

## 二、修改文件与关键点

### 1. models/task.py —— 模型层（新增 + 兜底）
- 新增 `SEGMENT_STRATEGY_ALLOWED = {auto, force_split, no_split, by_length, by_chapter}` 与 `normalize_segment_strategy()`；
- `CommandTask` / `TaskSubmitRequest` 增加 `field_validator(mode="before")`：**非法值自动回退 `auto`**（存量脏数据如历史 `"chapter"` 也不会崩溃）；大小写/空白容忍；
- 字段类型保持 `str`，无数据库迁移。

### 2. services/command_splitter.py —— 拆分决策层（核心）
- `split()` 入口按策略分流，每条路径打日志 `[SPLIT_STRATEGY:{strategy}]`：
  - **新增** `no_split` 分支 → `_build_single_segment()`；
  - **新增** `force_split` 分支 → 长文本（≥2000字）走 `_select_strategy` 既有算法；短指令走 `_logical_shard_segments()`：自然拆句（上限3段）→ 无句读符固定三等分 → 极短指令（<30字）生成3个同载荷逻辑分片（重负载钩子按序装载外部数据的处理节点/断点锚点），全部初始化空 tail；
  - **保留** auto 分支：`strategy_name in self._strategies` 注册策略 → `_needs_splitting` 字符阈值+关键词判断 → `_select_strategy`，原代码逐行未动（单段构造抽为 `_build_single_segment`，逻辑等价）。

### 3. services/task_manager.py —— 兼容保护
- `submit_task()` 入口防御：非法 segment_strategy 自动回退 `auto` 并打 WARN 日志；运行时注册的自定义策略优先保留；显式 `force_split`/`no_split` 原样生效。

### 4. services/quantifier.py（BookQuantifier，批次2）
- 任务构造处 `segment_strategy` 由非法值 `"chapter"` 改为 **`"force_split"`**（书籍量化=短指令+大量章节卡片）。

### 5. services/learning_engine.py（DocumentLearningEngine，批次3）
- 任务构造处新增 **`"segment_strategy": "force_split"`**（文档学习=短指令+大文档解析）。

### 6. services/global_router.py（批次5）
- `KeywordIntentDetector` 新增 `DOC_LEARNING` 意图（关键词：文档学习/学习文档/解析文档）；
- 新增 `DOC_LEARNING` 路由分支（批次1引擎 + force_split，回退旧队列占位闭包）；
- `QUANTIZATION` 分支自动标记 `force_split`（引擎路径与回退转发路径均生效）；
- `CREATION` 分支：DEEP_THINK（think/deep/reasoning/deepseek-r1）或强制分段规则 → 自动 `force_split`；普通轻量创作/简单问答维持 `auto`；
- 新增 `_resolve_segment_strategy()`：`options.segment_strategy` 透传（force_split/no_split/auto），非法值忽略保持默认。

## 三、集成注释（兼容性声明）

1. **存量任务全部默认 `auto`**：未显式设置 segment_strategy 的已有任务（包括 DB 中历史行）行为与补丁前完全一致；
2. 零数据库变更：复用字符串字段，无 ALTER TABLE，无新表；
3. 所有异常输入（None/非法串/未知策略名）三层兜底（模型校验 → TaskManager → 拆分器归一化），不可能导致任务崩溃；
4. 不影响 SegmentPipeline / TailContextManager / ResultMerger 及批次2/3/4既有执行逻辑。

## 四、验证结果

`tests/test_split_strategy_patch.py`（13 用例，pytest 与直接运行双通道）**全部通过**：

| 用例 | 验证点 |
|---|---|
| force_split + 300字符短指令 | 产出 2-3 个有序 Segment，空 tail 初始化，内容无丢失（**修复"字符数不足导致未触发分段"报错场景**） |
| force_split 尾巴接力 | 流水线中 processed_count 跨分段累计至分段总数 |
| no_split | 2700字符+章节标记文本也只产出 1 段 |
| auto×5 | 短文本单段 / 长文本多段 / 关键词+长文本 / 章节数=分段数 / 默认值 auto —— **与改造前完全一致** |
| 模型非法值兜底 | CommandTask/TaskSubmitRequest 非法值回退 auto |
| TaskManager 防御 | 脏参数回退 auto 不崩溃，合法值生效 |
| GlobalRouter 标记 | 量化/文档学习/深度思考自动 force_split；普通问答维持原路径；options 透传 no_split/auto 生效；非法透传忽略 |
| 上层业务 | BookQuantifier / DocumentLearningEngine 落库任务均为 force_split |

回归验证：上一轮 `tests/test_batch1_engine.py` 冒烟 7 项全过（GlobalRouter 改动无回归）；全部改动文件 py_compile 通过。

---

## 附：批次1 任务清单（合并 B1-08 后）

| 序号 | 风险描述 | 所属模块 | 优先级 | 原始需求来源 | 修改简述 | 验收标准 |
|---|---|---|---|---|---|---|
| 1 | ResultMerger 全量加载合并 OOM | 批次1 ResultMerger | P0 | 批次1原始蓝图 | 流式分块合并，产物落盘返回路径 | 百万字级合并 O(1) 内存 ✅ |
| 2 | SegmentPipeline 大结果写库致 DB 膨胀 | 批次1 SegmentPipeline | P0 | 批次1原始蓝图 | max_db_payload_bytes 阈值外置，超限只存 output_path | 大结果不入 SQLite ✅ |
| 3 | GlobalRouter 与批次1引擎集成断层 | 批次5 GlobalRouter | P0 | 批次1原始蓝图 | 长任务构造 CommandTask 提交 submit_task，全链路打通 | /command 长任务真实走分段引擎 ✅ |
| 4 | TailContext 无独立管理、无落盘 | 批次1 SegmentPipeline | P1 | 批次1原始蓝图 | 新建 TailContextManager 双模式+TTL | 大尾巴落盘可物化 ✅ |
| 5 | 任务接口散落 projects.py 手写 SQL | API 层 | P2 | 批次1原始蓝图 | 新建 task_api.py，旧路由 307 代理 | 接口零 RAW SQL ✅ |
| 6 | TempFileManager 缺配额/LRU/启动清理 | 批次1 TempFileManager | P2 | 补丁E增强 | 配额+LRU+孤儿扫描（保护运行中） | 超配额淘汰、启动清理 ✅ |
| 7 | 异常中断恢复粒度不足 | 批次1 引擎 | P2 | 批次1原始蓝图 | 失败现场快照落盘+重启注入 | 快照恢复续跑 ✅ |
| **B1-08** | **短指令、后台高负载任务，仅靠指令字符数判断不会触发多节点分段，丧失尾巴接力、断点恢复能力** | **批次1 CommandSplitter + 上层业务** | **P1** | **批次1原始蓝图（Segment 多节点执行）** | **复用 segment_strategy，新增 force_split/no_split；书库量化/文档学习任务自动 force_split；auto 模式保留旧逻辑** | **短指令设置 force_split，可拆分为 2-3 个 Segment；auto 模式原有逻辑不变；存量任务不受影响** ✅ |

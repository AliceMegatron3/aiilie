# 内核与插件平台架构讨论稿

> 定稿日期:2026-08-16|项目根目录:C:\Users\11482\Documents\aiilie
> 性质:九轮架构讨论的口径定稿,**不涉及任何代码修改**;作为 P2~P4 执行期的架构口径基准。
> 口径基准:三轮代码实勘(内核层/服务层/智能体与反思闭环)+ 九轮讨论决策(29 条台账见第十章)。
> 衔接:继承《P1核心架构落地全能管家调度中枢搭建方案_20260815》四模块定稿、《P4长期基建终版方案》插件生命周期规格、《后续系统性优化建议报告_20260815》§5.3 双 TaskManager 治理建议。

---

## 第一章 愿景终评(收官判定)

本项目的最主要需求表述为:**一个相对自主学习、可容纳大量书籍资料、智能体权限很高、可高强度依赖 PC 本地硬件运行的小说创作软件**。逐支柱评估如下。

### 1.1 四大支柱评估

**① 本地硬件重度依赖——合理,定位为"成本地板 + 云端质量天花板"的混合永续。**
小说创作是长周期、强隐私、重数据的活动:手稿不出本机是刚需;语料库以百本书计是本地磁盘的舒适区;反复迭代的调用模式使一次性硬件成本优于长期 API 计费。但必须校准一处:本地模型(Ollama)承接解析、初筛、廉价步骤绰绰有余,顶级创作质量仍需云端前沿模型。正确表述是**本地优先 + 混合永续**——现有 dispatcher 的云/本地双路互备(services/dispatcher.py)就是正确基线,坚持住。

**② 容纳大量书籍资料——合理,且架构已经想透。**
SQLite 热索引 + JSON 冷存储(services/indexer.py)在单机承载几十万张卡毫无压力。真正的难题从来不是容量,而是百书归并、矛盾裁决、检索不退化——由第六章的锁定场、归并机制、死卡退役、must 物化集解决。此支柱没有悬念。

**③ 权限很高的智能体——合理,前提是重新定义"权限高"。**
高权限 ≠ 无边权限。正确形态是第四章收敛的公式:**高自主度(编排层随便想)+ 确定执行(每个动作过门)+ 可中断(检查点+呈递点)+ 全审计**。作者要的不是听话的打字员,也不是失控的代笔,而是"能干大事但永远在栏杆里跑的执行团队"。

**④ 相对自主学习——合理,而"相对"二字恰恰是诚实的地方,必须守住。**
系统能学的是有可测目标的东西:预算达成度、检索命中、拆分参数、专家调度(真信号:作者编辑距离、确认率、上下文轨迹)。系统不能可靠学的是"什么让这部小说好"——审美判断权永远归作者(审而非发)。丢了"相对",系统会退化为用假指标自我强化的噪声机器——仓库根目录 diag_rules.py(AgentRuleExtractor 产不出 ACTIVE 规则的病历)就是前车之鉴。

### 1.2 方向判定

方向正确,基于两个基本事实:

- **痛点真实**:千章长篇的结构管理、一致性维护、知识沉淀,是当前所有写作工具(Scrivener 无 AI、云端工具无学习、均无结构层)都没有解决的真问题。本讨论稿的叙事结构层(第七章)+ 锁定场(第六章)+ 伏笔账本直击这个空白。
- **定位差异真实**:"本地优先、可学习、治理式智能体"的小说工作室,市场无同类,且该组合恰好匹配小说创作的隐私性、长周期、强个人风格三属性。

首轮讨论提出的"deepseek harness 式运行核心"愿景,经九轮讨论已从概念压实为每层可实施的规格——愿景没有漂移。

### 1.3 最大风险与下一步

**最大的风险不是方向,是设计与代码之间的距离。** 实勘反复证实:架构文档一直跑在代码前面——抽取器建了没人调用、子代理输出被丢弃、主链路是占位符(证据见第三章断点清单)。设计再对,不落地就是零。

**下一步只有一句:按既定顺序执行——先让创作主链路产出真实文本,其余一切(学习、结构层、锁定场)都在真实产出之上生长。**

---

## 第二章 内核与插件层(第一轮定稿)

### 2.1 现状差距盘点(逐条带证据)

四模块架构已于 2026-08-15 定稿(docs/plan.md:19:全能管家调度中枢/书库量化数据底座/执行插件集群/反思学习后置层),但代码尚未兑现:

| # | 差距 | 证据 |
|---|------|------|
| 1 | 插件系统空壳:lifecycle 仅 1 行占位注释 | core/plugin_lifecycle.py:1 |
| 2 | PluginManager.load_all() 定义后无任何调用方,plugins/ 目录为空 | core/plugin_manager.py:21 |
| 3 | 插件安装接口故意 501("不再伪造安装成功") | api/plugins.py:28 |
| 4 | bootstrap 上帝模块:796 行、30+ 函数级延迟 import、私有属性注入业务对象 | core/bootstrap.py:300-302、494 |
| 5 | 双同名 TaskManager(队列版 vs batch1 管线版) | core/task_manager.py:46 与 services/task_manager.py:68 |
| 6 | core→services 反向依赖(分层违规) | core/plugin_manager.py:7 |
| 7 | 数据库 schema 权威分裂:两处 DDL | core/database.py:35-113 与 core/bootstrap.py:60-97 |
| 8 | feature flags 在 import 时读死,config reload 无法改变已挂载路由 | api/api_router.py:46-73 |
| 9 | GlobalRouter 混书库卡片 CRUD 业务 | services/global_router.py:280 |
| 10 | dispatcher 混领域逻辑(WorldContextBuilder 注入) | services/dispatcher.py:108 |

### 2.2 内核五件事(判据:"换一个领域是否保留")

假如明天不写小说、改做剧本或漫画脚本,哪些代码原样保留?保留的是内核:

1. **进程生命周期编排**:bootstrap 固定启动顺序改为"配置→存储→模块注册→有序启动/逆序停止"的模块化协议,内核不再 import 任何具体业务服务;
2. **任务与队列**:双 TaskManager 统一为一个协议(持久化存储 + 任务编排器两层),与优化报告 §5.3 建议一致;
3. **模型访问层**:保留云/本地路由、熔断、failover;剥离 WorldContextBuilder(领域上下文组装归还书库量化);
4. **能力注册与调用门控**:plugin_manager 的 L0-L6 权限规划器(core/plugin_manager.py:69-205,默认拒绝、schema 校验、防权限提升)是全库最接近"系统调用层"的资产,升级为 capability registry + 决策点注册表;
5. **事件与可观测**:WebSocket 事件总线、health 探针、审计日志。

### 2.3 数据三层模型(数据安全的宪法)

| 层 | 内容 | 规则 |
|---|------|------|
| **原始资料层**(唯一事实来源) | 历史资料语料、项目文档、网络搜索缓存(带 URL+时间戳落地) | 只增不改不删;**删除权仅属用户**;任何插件、任何内核模块无权删源头 |
| **派生知识层**(卡片) | 全部由分析器从源头量化产出 | 每卡强制 SourceAnchor(schema 已有:models/cards.py:25)→硬约束;**卡片可从源头重建**——卡片库损毁≠资料丢失 |
| **消费层** | 生成器经内核接口读卡 | 必要时按锚点回源读原文 |

核心原则:**卡片是派生索引,不是数据本体。** 现有 LibraryManifestScanner 对历史资料库的处理("语料本体不动、manifest 派生")就是正确样板,上升为全局数据宪法。

### 2.4 插件四类治理域与权限矩阵

| 类型 | 成员 | 能做 | 不能做 |
|------|------|------|--------|
| **Provider 资料源** | 文件系统语料(内置)、项目文档(内置)、网络搜索(插件) | 读外部世界、写缓存区 | 不能直接写卡片——量化必须走分析器管道 |
| **Analyzer 分析器** | 书库量化、情感引擎、评分、深度思考 | 读源、写卡片 | 不能改/删源 |
| **Generator 生成器** | 时间线、分镜、TTS、发散、多 Agent、创作专家 | 读卡片与源、产内容 | 不能写卡片 |
| **Governor 治理** | 反思、技能治理、作者审核 | 后置触发、全审计、写规则/技能库 | 无自主执行权(只能被内核调度) |

- 权限上限映射 L0-L6;触发方式:生成器用户触发、分析器走队列、治理类只能由内核定时器/阈值触发;
- 对接 P4 七态生命周期(注册→加载→调度→执行→休眠→卸载→资源释放);
- 运行级别 S0-S3 详见第八章。

### 2.5 管制定稿

- **单一 PluginRegistry + 分类策略**,不采用多内核(理由:P1 已定稿"唯一核心中枢";本项目病根曾是多头调度与自主混乱执行;分类管制=同一注册中心按类型挂不同策略,不是多个调度器);
- **书库量化归属裁定**:卡片契约(BaseCard 类型、检索接口、锚点规范)与索引引擎=内核共享设施(被 dispatcher/生成器/多分析器共同消费,契约必须中立);量化管线(BookQuantifier、抽取器)=Analyzer 类内核插件;特权封在"写派生层",源头永远安全;
- **项目文档与网络搜索定位为 Provider**:网络搜索插件只做"抓取+缓存落地",产出层 1 新资料;量化、审核、入库由统一管道完成,网络数据与本地资料同一套溯源与治理规则,无法外之地;
- **演进路径**:阶段 0 拆 bootstrap、统一 TaskManager 协议 → 阶段 1 PluginManifest v1(在现有事实 schema 上补 type/生命周期/钩子)+ 填 plugin_lifecycle.py 七态状态机 → 阶段 2 情感引擎首试点(已 feature-gated、按请求组装、自带存储与 prompt 族)→ 阶段 3 书库量化、反思、GlobalRouter 业务归位。

---

## 第三章 智能体·反思·卡片运转逻辑(第二轮定稿)

### 3.1 飞轮七步(目标运转逻辑)

1. Provider 落一份新材料(或上一圈创作产出沉淀为项目文档);
2. Analyzer 把源头量化成卡(带 SourceAnchor 进卡片库);
3. Generator(管家调度的智能体)接单:按调度规则决定启用哪些专家、几轮、什么模式;每个子智能体带角色 prompt + 角色过滤的卡片上下文独立工作;产出汇合为正文;
4. 正文沉淀为项目文档(回源头),同时留下遥测:每专家审计(token/时长/错误/用户反馈)、上下文轨迹(实际注入了哪些卡)、评分;
5. Governor(反思)后置开箱,读遥测+卡片,挖掘两种东西;
6. 过治理门:作者审核(审而非发)、灰度、置信度阈值、可回滚;
7. 通过者分流两个平面回流,进入下一圈。

**角色定位**:智能体(supervisor)=无模型访问权的编排者(每次子代理调用走内核模型访问层);子智能体=角色 prompt + 检索特化(rag_filters)的执行器;反思=只写治理库的 Governor。

**两个平面**:知识面=卡片(技能也是卡:InfoCard 且 card_sub_type='novel_agent_skill',技能库天然获得卡片溯源/状态机/证据等级——这是反思系统与卡片系统的兼容点);控制面=规则(调度指令住 optimization_rules,不住卡片库)。**规则改调度行为,技能改子代理 prompt,权重改检索。**

### 3.2 断点清单(逐条带证据)

| 环节 | 断点 | 证据 |
|------|------|------|
| 创作主链路 | SegmentPipeline 生产环境从未设真实执行钩子,长任务产出占位符文本 | services/segment_pipeline.py:42-72(默认钩子返回"[PLACEHOLDER] 已处理内容");core/bootstrap.py:134(构建时未设钩子,set_execution_hook 仅测试调用) |
| 子智能体 | 四专家拿到的是原始用户指令(角色模板算了不用);产出被整体丢弃只留计数;技能 rag_filters 构建后从未消费 | services/novel_supervisor.py:348-360、197-199、320、432 |
| 路由 | 长任务在路由层绕过 supervisor | services/global_router.py:498-520 |
| 全局反思 | 只写不读:RuleExtractor/SkillExtractor 实例化无调用方;apply_discoveries 零调用;快照永无人读;universal_skills 永远空 | core/bootstrap.py:207-210;services/optimization_applier.py:52、159 |
| 治理 | SkillGovernance 的 promote/灰度/回滚无 API 入口;晋升后无人消费 | services/skill_governance.py:241-390;api/reflection_governance.py:121-165 |
| 评分 | 分数不持久化;经验沉淀 sink 是打日志的 stub;reflection_engine 依赖从未注入 | services/scoring_engine.py:82-87、90 |
| 终局反思 | ReflectionEngine.execute_final_reflection 无调用方 | services/reflection_engine.py:38 |
| 专家库 | ExpertAgents prompt 库生产无消费方(仅未注册的 LLMExtractionStrategy 使用) | strategies/llm_extraction.py:22、61;core/bootstrap.py:326 |
| 唯一闭环 | 智能体自学习环(审计→抽取→规则/技能→再消费→自动归档)结构闭合,但:双开关默认关、仅短任务路径可达、假指标驱动(命中率=len>50?0.9:0.5;越权=子串匹配) | services/novel_agent_learning_loop.py;services/novel_supervisor.py:367-372;config/config.yaml:96、150;diag_rules.py(病历) |
| 浪费 | novellearn_ 任务每次创作重复全量快照 | core/bootstrap.py:351-362 |

**沉睡的遥测金矿**:reflection_reports/*_raw.json 快照、novel_agent_audits(含用户反馈通道 api/novel_agent.py:41-68)、tasks 表重试率、dispatcher.last_world_context 上下文轨迹(dispatcher.py:55,从不持久化)、chat_history、卡片 weight(仅作快照过滤器)。

### 3.3 可行性判定

- **逻辑成立**:每个环节在代码里都有"通电先例"——闭环学习环证明电路能通电流、规则读取端已存在、技能卡机制已在跑、治理门已建好。这是接线工程,不是发明工程。
- **硬约束:小样本统计**。单机桌面应用的使用密度撑不起自动化统计决策;5 个样本的"效果分"置信度与抛硬币相当;现有"效果<0.25 自动归档"(novel_agent_learning_loop.py:238-249)在小样本下随机误杀好规则。**定位修正为"假设生成 + 人工裁决 + 灰度验证"**:任何规则/技能应用前设最低样本门槛;自动归档改为证据包打包送审;灰度(SkillGovernance 已有)是唯一 A/B 验证手段。作者审核门不是安全装饰,是小样本条件下唯一统计正确的决策主体。
- **四个设计优化**:①统一三套治理系统(batch4 全局反思/智能体学习环/SkillGovernance)为一条管道——一个抽取入口、一个审核队列(SkillGovernance 升任总门)、一个应用面;②免费真实信号够用(作者反馈/重试错误率/上下文轨迹/回流原稿 vs 定稿 diff);③规则作用面白名单化(内核决策点注册表,规则只能给已注册决策点赋值);④反思触发改阈值驱动(新增审计 ≥N 才触发,代替时钟)。

### 3.4 修复顺序与回流形态(定稿)

1. **先修创作主链路**:SegmentPipeline 接真实执行钩子;子代理接角色 prompt 与 rag_filters(都已存在只差消费);输出汇合而非丢弃。——学习环可行性以主链路真实产出为前提,这不是偏好是逻辑依赖:占位符上跑出来的规则全是噪声,还会污染治理库。
2. 再接学习环断点(反思 worker 调完快照后调用已实例化的抽取器/应用器;评分持久化;去掉重复快照);
3. 真实指标(用户反馈/编辑距离/上下文轨迹持久化);
4. 治理消费端接通(promote/灰度接 API;晋升技能注入 prompt 组装);
5. 产出回流。

**回流形态定稿:作者确认后回流**;确认环节留存"原稿 vs 定稿"diff——这一个动作同时喂饱积累螺旋(知识面)和指标体系(质量信号)。

---

## 第四章 主智能体自主编排(第三轮定稿)

### 4.1 harness 模式:自主在规划层,确定在执行层

用户拍板采用智能体自主编排,与 P1"唯一中枢、插件无自主执行权"的张力以此化解:

- **主智能体拥有"想"的自主权**:感知任务板→自主分解→组合跨组计划→发起调用→观察结果→迭代修正;
- **内核拥有"做"的全部权力**:每次调用都是发给内核的工具请求,经能力注册表→权限门(L0-L6)→风险分档→预算核算后才执行。

调度权、执行权、资源权仍只在内核;智能体拿到的是组合自由度。**内核永远保留四权:执行权(工具调用过门)、预算权(智能体无权自扩)、中断权(作者随时打断,任务板留检查点)、审计权(每次调用、每个决策入轨迹)。**

护栏现成资产:core/agent_runtime_patch.py(迭代上限+超时熔断中间件)、AgentRuntimeProtector(services/segment_pipeline.py:314-324 混沌拦截)、模型熔断器、plugin_manager 默认拒绝校验——只需从各管一段收拢为编排循环统一护栏。

### 4.2 工具面五类

| 工具类 | 例子 | 风险档 |
|--------|------|--------|
| 生成 | 派发专家子任务、调用发散引擎 | 低(产出草稿) |
| 检索 | 卡片查询、资料库调阅、回源读原文 | 低(只读) |
| 任务 | 提交子任务、查询状态、更新任务板 | 低-中 |
| 分析请求 | 请求量化某文档、请求入草稿卡 | 中(写派生层 draft) |
| 学习请求 | 提请反思、提请技能候选 | 高(只进治理队列,Governor 决定是否开箱) |

**Analyzer 与 Governor 永远不被智能体"调用",只能被"请求"**——请求进内核队列,是否执行由确定性管线和阈值决定。智能体可以推动一切,但不能命令学习系统、不能碰源头层。

### 4.3 风险三档(定稿)

- **低**:只读、草稿生成——自动执行;
- **中**:写派生层 draft、量化请求——自动执行留痕;
- **高**:触源头层、治理发布、学习应用——作者确认。

分档映射数据三层与治理门,与"审而非发"同一哲学:**计划也是审而非发**。

### 4.4 角色注册表(内置+共存,定稿)

四专家(lore/combat/emotion/event)为出厂预设条目;注册表条目统一六字段:角色 ID、prompt 模板 ID、默认 rag_filters、权限上限、预算档位、触发条件。新专家=新增一条数据(技能卡或配置),不写代码;技能卡 template_overrides 通道为过渡。

### 4.5 命令路由分工与编排遥测

- 简单命令→关键词快路径(KeywordIntentDetector 保留);
- 复合/模糊命令→编排循环;
- **编排轨迹遥测**:主智能体怎么分解、调了哪些工具、哪条路径被打断、哪条一次通过——比假指标真实百倍;Governor 的挖掘对象从"专家表现"扩展到"编排决策",可学习面从专家层升到编排层。

---

## 第五章 书库量化与双轨反思(第四轮定稿)

### 5.1 卡片分类:按消费端设计(四轴)

分类的第一问题不是"书里有什么",而是"**谁在什么时刻、用什么过滤器来查它**"。现有三轴(形态 Info/Data/Extend × 知识类型 FACT/RULE/ENTITY/STATE/TEMPLATE/RELATION × 作用域五级)新增第四轴**消费档位**:

| 卡片类型 | 主要消费方 | 生成时刻用途 |
|----------|-----------|--------------|
| 设定卡(worldview) | lore 专家、must 档注入 | 设定一致性 |
| 人物卡(character) | emotion 专家、一致性检查 | 人物弧线 |
| 情节卡(plot) | 连续性检查、时间线插件 | 前后呼应 |
| 风格卡(style) | prompt 适配器、风格锁 | 文风统一 |
| 数据卡(情绪曲线/节奏/冲突频率) | LoadEstimator、节奏控制 | 章节节奏决策 |
| 逻辑卡(因果链) | 行为合理性检查 | 动机自洽 |
| 规则卡(rule_strength=hard) | must 档注入 | 不可违反 |

逻辑卡因果链模板已存在(data/prompts/builtin/social_logic_card_extract:资源短缺→安全→情绪→欲望→关系→行为→后果),为现成第七类。

### 5.2 分型抽取管线七步

落料(Provider)→语义切分→**分型抽取**(CardTypeRegistry 升级为"类型→抽取模板"映射:设定卡走 quantization_asset_extract、逻辑卡走因果链模板、数据卡=单段 LLM 量化+计算聚合——情感引擎已是此模式)→证据锚定(SourceAnchor 强制)→入库 draft→作者审核→approved 进热索引。**分型抽取,不是一遍过;数据卡是"计算+聚合"不是纯 LLM 抽取。**

### 5.3 反思时机:任务后只落账,反思跟阈值

智能体完成任务后**不触发反思,只落账**(任务板终态/工具轨迹/专家遥测/检索命中与未命中/作者交互结果)。理由:单任务统计意义为零;反思是全库扫描级操作;Governor 不被创作路径同步调用是 P1 定稿。

| 触发器 | 条件 | 类型 |
|--------|------|------|
| 批量阈值 | 新增审计 ≥10 或新完成任务 ≥M | 流程反思 |
| 负信号加速 | 连续失败 ≥3 / 熔断 / 作者大幅修改+拒绝 | 流程反思提前开箱 |
| 里程碑 | 章节/卷完结 | 知识反思(全量) |
| 周期兜底 | 每周 | 知识反思 |

### 5.4 双轨反思对照(核心定稿)

| 维度 | 流程反思(系统怎么干的) | 知识反思(系统知道什么) |
|------|------------------------|--------------------------|
| 对象 | 调度/拆分/重试/成本/编排轨迹 | 卡片库对不对、全不全、有没有被用 |
| 方法 | 遥测统计挖掘 | 库审计:矛盾检测(同实体互斥)/覆盖分析(哪个域薄)/使用分析(死卡=从未被检索)/时效(源更新卡未更新) |
| 产物 | 控制面:MODEL_DISPATCH/TASK_SPLIT/NOVEL_AGENT_SCHEDULE 规则 | 知识面:卡片合并/拆分/退役提案、缺口报告、再量化任务、资料采购建议 |
| 消费方 | 内核调度、supervisor 编排 | 作者(审提案)、Analyzer(再量化)、Provider(补料优先级) |
| 风险 | 误用规则损性能→灰度+回滚 | 低(提案天然 draft,过作者审核) |
| 节奏 | 频繁小批 | 低频大批 |

知识反思是积累螺旋缺的那一环:没有它,卡片库只进不出、死卡堆积、缺口无人发现、库越大检索噪声越大。

### 5.5 知识反思产物与缺口闭环(定稿)

- **产物=可执行提案**:合并/退役/再量化以 draft 卡或任务单进队列,作者一键批准或驳回;
- **缺口采集闭环**:检索未命中日志→缺口报告→Provider(网络搜索)采集建议单→作者确认后执行→新料入源层→再量化。"作者不知道缺什么资料"变成"系统告诉你:某域缺 B 级证据以上卡片,本月未命中 17 次";资料积累从被动变主动;
- **上下文轨迹持久化**(dispatcher.last_world_context):回答"卡片到底帮没帮上生成",作使用分析与卡片权重依据。

### 5.6 复利全景

知识螺旋:创作→回流→量化→库厚→知识反思→定向补料+死卡退役→库更精;效率螺旋:遥测→流程反思→调度省→同预算写更多。两个螺旋都由 Governor 持有,产物分流知识面/控制面,共用治理门。

---

## 第六章 百书规模与世界观锁定场(第五轮定稿)

### 6.1 重复问题:归并,不删除

**同一事实的多源重复不是垃圾,是免费的置信度。** "190 年董卓迁都长安"出现在 80 本书里→1 张规范卡+80 条 SourceAnchor+证据计数,多源互证抬升 evidence_level。机制:

1. **实体归一先行**:同一人/地/事件不同称谓先归一(ENTITY 类型卡+别名表);
2. **一致检测与矛盾检测是一枚硬币两面**:同实体+同事实+时空一致→归并规范卡、锚点合并;不同说法→不自动取舍,生成分歧记录,作者裁决;
3. **增量归并**:每本书入库即跑,不等百书齐;
4. **作用域隔离**:库里一百本书的卡≠生成时面对一百本书的卡,检索只拉当前锁定场内集合;
5. **死卡退役**:从未被检索→冷存档。

### 6.2 卡片效果与权限:双维评分

- **证据维(说得对不对)**:evidence_level A-D + 多源计数 + SourceAnchor 完整性;
- **效果维(用得上没用)**:注入次数 × 注入后作者行为(原样确认/轻改/重写)——数据源为上下文轨迹+回流 diff;
- **权限=状态机×强度**:draft→reviewed→approved;**must 档资格=approved 且 rule_strength=hard**;
- 检索三档(must/should/optional,WorldContextBuilder 已分层:models/library.py:381-408)从按状态定档升级为双维联合定档,卡片 weight 接使用统计。

### 6.3 世界观锁定场(定义与字段)

**锁定场=一组命名、版本化的卡片筛选规则,决定生成时"现实是什么"**:

- 场名(如"乱世双璧·正史基底");
- 库绑定(根目录《历史资料》已入库为 historical_materials,52 条 manifest,首个锁定库);
- 类型规则:历史类规矩锁定=锁定库内 rule_strength=hard 且 evidence≥B 的卡→must 档,环境锁定(时间线/地理/制度/人物关系即史实基线);
- 优先级序:project 卡 > 锁定库卡 > global 卡;
- 冲突策略:锁定库之间冲突时的裁决序;
- 版本号与演化日志。

实现零发明:rag_filters+scope_level+rule_strength 的组合配置化,WorldContextBuilder 从"隐式全局"变"显式锁定场"。性能:锁定场加载时**物化 must 集合**,生成时不再全库过滤,百书规模检索延迟不涨。

### 6.4 矛盾裁决与偏离登记(定稿)

- **矛盾裁决=默认策略+例外**:作者预设裁决序(正史优先/证据级优先)批量裁决,个别分歧卡单独覆盖;
- **偏离登记=检测建议+作者确认**:作者写架空内容,系统检测与锁定场史实卡的冲突,生成登记建议,作者确认入册。新卡 scope=project、标记 diverged_from=<史实卡ID>、记下命令语境——**历史"改变"了,但系统记得从哪一点、因哪条命令改变**;
- **锁定场版本化**:每次调整(锁/解锁/晋升/降级/登记偏离)生成新版本,可回滚(VersionControlService 复用,prompt 模板 20 版本历史同机制)——演化有编年史;
- **建议的产生**:锁定场内缺口检测(接缺口采集闭环)+偏离敏感点提示(生成时命中偏离点附近史实卡→提示作者);
- **粒度=单场+版本演化**,为 branch_version_enable 分支写作预留多场接口;
- LibraryCommand 协议扩展:lock(锁库/锁类型)/diverge(登记偏离)/promote(晋升卡)/rollback(回滚版本)。

---

## 第七章 长篇叙事结构层(第六轮定稿)

千章规模的核心判断:**系统缺一个"叙事结构层"——卡片管知识、任务管执行,没人管"作品本身的骨架"。** 长而不乱、重复而有别的答案是三件套:结构资产、变异机制、学习闭环。

### 7.1 结构资产

- **卷/章/拍三级纲要进内核契约**(项目层一等结构资产):卷纲(弧线宣告:张力曲线目标/重大节点/主题)→章纲(状态+进出章活动线索)→拍纲(supervisor 分解单位)。拍纲是创作任务,卷章纲是它的上位契约;
- **节奏预算**:作者宣告弧线,系统对账——DataCard 字段(progression_pacing/conflict_frequency/emotion_curve,models/cards.py:93)按章聚合成时间序列与预算比对,偏差即提示("连续 5 章低冲突,蓄力期偏拖")。节奏从感觉变成可对账的数字,是一切学习的前提;
- **伏笔账本**:每个伏笔=情节卡带生命周期(已埋/推进中/已回收)+回收期限;知识反思巡检("12 个未回收伏笔,3 个超 20 章未提及");
- **千章记忆压缩**:全书→卷→章分级摘要卡;检索窗口=活动线程+近 N 章摘要+锁定场 must 集(情感帧滑动窗口 frame_manager.py:94 为先例推广)。

### 7.2 录入与执行(定稿)

- **纲要录入=自由文本+解析确认**:作者白话写卷纲章纲,系统解析成结构化字段(预算/节点/线索)后请作者确认入库——与"审而非发"同哲学,创作心流不打断;
- **预算执行=硬软分档**:伏笔回收等硬性契约=硬约束注入生成;节奏强度等软性目标=提示+事后对账告警。硬约束少而稳,软目标留创作弹性。

### 7.3 重复治理:模式库+变异算子

重复不是病,是长篇常态;病的是**模式留在模型隐层、变异靠模型心情**。

1. **模式显式化**:recurring 叙事模式(宴会/登场/战斗/告白)落 TEMPLATE 类型卡(knowledge_type 已有;SkillExtractor 挖的 TEMPLATE 技能即此物);
2. **不变量骨架+变异槽位**:每个模式=必须保真的功能骨架(宴会要完成的信息交换)+可变槽位(参与者/情绪基调/强度/结果走向/伏笔载荷);
3. **变异从状态计算,不从模型心情**:当前弧位(预算要求高强度→这次宴会收在掀桌)、参与者人物卡、**历史实例反查**("上次宴会以冲突收场,这次收和解;近 3 章都对话开场,这次动作用")——反重复约束在拍级规划时由 supervisor 注入;
4. **重复检测兜底**:生成后相似度巡检(与历史章节比对),产出改写建议,属知识反思库审计。

**模式库源=库挖+作品精炼(定稿)**:从已量化书库挖掘通用模式(库级卡)作起点,作者自己作品的使用记录持续精炼出项目级模式;作用域隔离、优先级项目>库。

### 7.4 节奏学习闭环

预算→生成→测量(章级聚合)→偏差→流程反思。实绩对预算的偏差(冲突密度达成率/情绪曲线符合度)成为流程反思新遥测维度;作者对节奏的修改(手工挪高潮)是最强反馈信号。**目标函数从"len>50 假命中率"换成"预算达成度"真指标。**

### 7.5 横切归属表

| 能力 | 归属 | 理由 |
|------|------|------|
| 纲要+预算数据模型 | 内核契约(项目层结构资产) | 作者/supervisor/Analyzer 三方共读写,须中立持有 |
| 节奏仪表盘/伏笔账本 UI | 前端 | 展示 |
| 模式库 | 知识面卡片 | Analyzer/Governor 产,Generator 消费 |
| 变异规划+反重复注入 | supervisor 拍级编排 | 编排逻辑 |
| 重复检测/伏笔巡检/预算对账 | Analyzer+知识反思 | 后置巡检,产可执行提案 |
| 节奏学习 | 流程反思 | 预算达成度新遥测 |

---

## 第八章 命令入口层与编排深度(第七、八轮定稿)

### 8.1 三段式流水线与响应深度三档

命令三独立轴:复杂度(单一/复合)、内容体量(短/长)、跨度(涉及功能组数)。处理矩阵:

| 命令 | 内容 | 路径 |
|------|------|------|
| 短 | 短 | 即答档,单专家直通,不进队列 |
| 短 | 长 | 单组档:意图简单体量大→分段管线(CommandSplitter+SegmentPipeline) |
| 长(详细但单一) | 短 | 解析为结构化创作约束注入拍纲,单组档 |
| 复合 | 任意 | 编排档:编排器拆跨组 DAG |
| 长 | 长 | 编排器+分段管线组合 |

**响应深度三档**:
- **即答档**:只读、单意图、无生成——内核直答,零插件零子代理;
- **单组档**:单一意图——快路径可判直入(KeywordIntentDetector,global_router.py:62),歧义时编排器组内规划;
- **编排档**:复合意图跨 ≥2 组——编排循环出 DAG,按风险三档过门控。

**"一道命令能达到什么程度":上限由预算与风险门控封顶,深度由意图跨度决定。** 编排档理论上可链起全部功能组,但每环过权限/风险/预算三道门——永远是"局部门控响应的组合",不存在"无门全响应"。字数从不决定响应规模("写完反思"五个字比五百字单一创作命令响应面更宽)。

### 8.2 调取顺序(固定结构优先级)

1. 结构层资产(章纲/活动线程/节奏预算);2. 锁定场 must 集;3. 角色注册表选角(由拍纲决定,非固定顺序;预算压力高时与本章预算最相关者优先);4. 技能卡与模式库;5. should/optional 档卡片。**先骨架后血肉,先硬约束后软建议。**

### 8.3 插件运行级 S0-S3(正交于 L0-L6)

| 运行级 | 成员 | 生命周期(对应 P4 七态) |
|--------|------|--------------------------|
| S0 内核常驻 | 内核五件事+卡片契约+结构层资产 | 永驻无开关 |
| S1 内置插件 | 量化管线、情感引擎、反思管线 | 随启动加载,feature flag 可停(休眠态) |
| S2 注册表件 | 内置四专家+自定义角色/技能 | 按项目会话加载,可休眠卸载 |
| S3 可安装件 | 第三方插件(未来) | 安装审核→灰度→启用全生命周期 |

### 8.4 三层拆分归属(原始功能放哪)

| 拆分层 | 内容 | 归属 |
|--------|------|------|
| 意图拆分 | 复合命令→跨组 DAG | 内核解析+编排器提议、确定性裁决 |
| 内容拆分 | 长内容→段落分段执行 | **机制留内核主逻辑**(batch1 分段管线是执行机器,不可让渡);**策略参数可学可锁**——TASK_SPLIT 规则 scope 现成(global_router.py:464-470 已在读取) |
| 创作拆分 | 章→拍→专家 | supervisor(Generator) |

**拆分的权力在内核,拆分的知识在控制面规则,拆分的创意在 supervisor。**

**参数学习权定稿(可学可锁)**:TASK_SPLIT 等拆分参数默认可由流程反思经灰度自动调整;作者可对任意参数下锁定指令(锁定参数进保护名单,学习只能建议不能改写),解锁随时可撤。

### 8.5 项目文档生成全链(以"重写第三章"为例)

理解(快路径判 CREATION+单组+长内容判定 _is_long_task:global_router.py:157)→取约束(章纲/活动线程/节奏预算/锁定场 must/近章摘要)→拆拍(supervisor 依章纲出拍纲,逐拍选专家)→分段执行(SegmentPipeline 逐段生成,每段过情感保护器与超时熔断,遥测落账)→汇合(ResultMerger 合稿+相似度巡检+节奏对账)→呈递回流(作者审改→确认→定稿入项目文档+留 diff→触发再量化请求,Analyzer 入队列,draft 卡待审)。

### 8.6 编排深度与检查点(用户方案+修正,定稿)

**不设跨组上限 + 版本化检查点库落盘续跑**(用户提议采纳)。落盘持久化把长链最大风险从"失败"降级为"暂停",batch1 断点续跑(recover_interrupted_tasks)为先例推广。三处修正:

1. **载体是版本化检查点库,不是临时文件**:临时文件是会被清理的资源(TempFileManager 孤儿清理+配额);编排链中途状态放检查点库(任务板快照/已完成组产物指针/预算消耗账),生命周期绑定编排链,链完成才归档,可续跑可审计。**临时区(易失缓冲)与持久区(链路状态)是两个治理域**;
2. **两道软闸**(磁盘兜"断了能续",不兜"白跑与跑偏"):预算包络(链路总 token/时长封顶,触顶暂停呈报)+阶段呈递点(每组完成或每档预算消耗,向作者推可廉价中断的进度点);
3. **磁盘全量/上下文摘要两级**:磁盘是链路数据的事实来源,上下文窗口只进结构层摘要+锁定场 must——磁盘随便大,窗口永远省。本地 Ollama 承接链上廉价步骤(解析/分段/初筛),与模型分级路由咬合。

---

## 第九章 预算与执行优化(第九轮收官,15 条)

三总原则:**省 token 的最大杠杆在输入侧不在输出侧;压力可控的最大杠杆在"确定性优先"和"降级阶梯";本项目最大隐性成本是"建了没接线"**(已付建设成本+永远收不到回报——三个从不被调用的抽取器是活例)。

### 9.1 API 预算五条(按杠杆排序)

1. **前缀缓存纪律**:DeepSeek API 有前缀缓存机制,命中部分大幅折扣。锁定场 must 集天然稳定:保证注入顺序确定性(锁定场卡片在前、固定排序、波动内容放后),同会话内几十次专家调用的公共前缀全部命中——零代码风险的架构纪律,收益可能大于其余优化之和;
2. **模型分级路由且可学习**:意图路由/解析→rapid 或本地;抽取量化→中档;核心创作→顶配。dispatcher 已有 rapid/think 双模式,缺"任务类别→模型档位"映射表;MODEL_DISPATCH 规则 scope 现成,流程反思可学降档;
3. **预算三账本+降级阶梯**:调用计划里的 resource_budget(plugin_manager.py)现在全是零、不生效——落地为项目账本/角色账本/日账本;超限不报错走降级阶梯(think→rapid→本地→延迟排队)。硬失败是最贵的失败;
4. **输入侧节流**:卡片半结构化要点文体代替自然语言段落(同事实注入成本约降 40%);分级摘要使千章历史上下文成本常数级;
5. **确定性优先**:能用算的不用 LLM——节奏指标文本统计可算、相似度本地跑、实体初筛本地做模糊样本才进 LLM。

### 9.2 执行层四条

6. **队列分道**:内核队列现 concurrency=1(bootstrap.py:406),创作/量化/反思互相阻塞——分三条道:创作道(低并发保质量)/量化道(可并行批处理)/反思道(空闲时段跑);
7. **增量快照**:反思快照现每次拉 2000 任务+1000 卡(reflection_trigger.py:64-109),改"上次会话以来增量",与阈值触发配套;
8. **冷热分离归档**:tasks 表按年龄归档、反思报告滚动压缩;emotion frames 30 天裁剪(frame_manager.py:142)先例推广;
9. **熔断按 provider 拆分**(现本地云端共用一个,运行时加固报告§5已指出)+ VRAM 预算进配置(现硬编码 6900XT 16GB,global_router.py:128)。

### 9.3 智能体压力三条

10. **编排物理上限内核强制**:最大轮次/单计划最大工具调用数/最大并行子代理数/单次编排 token 包络,写进内核护栏(agent_runtime_patch 模式推广),不依赖智能体自觉——压力可控的本质是物理上限不可协商;
11. **压力信号进编排循环**:LoadEstimator 升格为常驻观测(本地拥堵/熔断开启/预算余量低→作为观察注入循环,supervisor 自选降档/换道/延迟)。压力不是异常,是循环常规输入;
12. **反思自身限额**:每次反思会话设扫描卡数上限、LLM 调用数上限、抽样代替全量(矛盾检测抽高风险域优先)。治理系统自己也要被治理。

### 9.4 落地保障三条

13. **功能准入三件套**:kill-switch(现成 feature flags)+预算上限+遥测,三样不全不合并——防"功能堆叠"复发的制度闸门;
14. **回路闭合测试进 CI**:本项目最大教训是"实例化即遗忘"。每条闭环写闭合测试:断言产物有消费者(快照→抽取器→应用器每环),CI 里跑。防断线比修断线便宜一百倍;
15. **分层超时落地**:P3-3 三级超时规格(连接/单步推理/全局任务)已定稿未全面落地,是执行层压力兜底最后一块。

---

## 第十章 决策台账与衔接

### 10.1 二十九条决策台账

| # | 决策点 | 定稿 |
|---|--------|------|
| 1 | 内核边界 | 内核五件事;卡片契约与索引引擎留内核,量化管线降为 Analyzer 类内置插件 |
| 2 | 数据安全 | 三层模型:源头只增不改删(删除权仅属用户)→卡片派生可重建(SourceAnchor 强制)→消费可回源 |
| 3 | 插件管制 | 单一 PluginRegistry + 四类策略(Provider/Analyzer/Generator/Governor),不多内核 |
| 4 | 飞轮修复顺序 | 先修创作主链路(占位符钩子)→接学习环断点→真实指标→治理消费端→产出回流 |
| 5 | 产出回流 | 作者确认后回流,留原稿 vs 定稿 diff 作质量信号 |
| 6 | 学习定位 | 小样本硬约束下修正为"假设生成+人工裁决+灰度验证";统一三套治理系统为一条管道 |
| 7 | 编排模式 | harness:自主在规划层、确定在执行层;内核保留执行/预算/中断/审计四权 |
| 8 | 命令确认 | 风险三档自动分档(只读草稿自动/写派生层留痕/触源与治理发布需确认) |
| 9 | 子代理角色 | 内置四专家+注册表共存,新专家=数据不写代码 |
| 10 | 卡片分类 | 第四轴"消费档位":按谁检索/何时检索/默认 rag_filter 设计,七类卡对照消费方 |
| 11 | 量化行为 | 分型抽取管线:CardTypeRegistry 变类型→模板映射;数据卡=单段量化+计算聚合 |
| 12 | 反思时机 | 任务后只落账不反思;批量阈值/负信号加速/里程碑/周期兜底四触发 |
| 13 | 反思双轨 | 流程反思(遥测→控制面规则)+知识反思(库审计→知识面提案),分轨不混 |
| 14 | 知识反思产物 | 可执行提案:draft 卡/任务单进队列,作者一键批驳 |
| 15 | 缺口闭环 | 检索未命中日志→缺口报告→Provider 采集建议单→作者确认→入源层→再量化 |
| 16 | 百书重复 | 归并不删除:实体归一→规范卡+多源锚点+证据计数;一致归并/矛盾分歧;增量归并;死卡退役 |
| 17 | 卡片效果 | 双维评分:证据维×效果维;must 档资格=approved+hard |
| 18 | 世界观锁定场 | 命名+版本化的卡片筛选规则集合;must 集合物化预算,百书检索延迟不涨 |
| 19 | 矛盾裁决 | 默认策略+例外:预设裁决序批量裁决,个别分歧单独覆盖 |
| 20 | 偏离登记 | 检测建议+作者确认:project 卡 diverged_from 标记,编年可回滚 |
| 21 | 锁定场粒度 | 单场+版本演化,为分支写作预留多场接口 |
| 22 | 叙事结构层 | 卷/章/拍三级纲要进内核契约;节奏预算对账+伏笔账本+分级摘要(千章压缩) |
| 23 | 纲要录入 | 自由文本+系统解析成结构化字段+作者确认入库 |
| 24 | 预算执行 | 硬软分档:伏笔等硬约束注入;节奏强度软提示+事后对账 |
| 25 | 模式库 | 库挖通用模式(库级卡)+作品持续精炼(项目级卡),优先级项目>库 |
| 26 | 命令入口 | 三段式(理解→规划→执行);三档响应深度(即答/单组/编排);深度随意图跨度不随字数 |
| 27 | 插件运行级 | S0 内核常驻/S1 内置/S2 注册表件/S3 可安装,正交于 L0-L6 |
| 28 | 拆分归属 | 意图拆分=内核+编排器;内容拆分机制=内核主逻辑、参数可学可锁;创作拆分=supervisor |
| 29 | 编排深度 | 不设跨组上限+版本化检查点库落盘续跑+两道软闸(预算包络、阶段呈递点);磁盘全量/上下文摘要两级 |

### 10.2 与既有文档的衔接

**继承**:
- P1 四模块定稿(全能管家/书库量化/执行插件/反思学习)——本稿是其实施规格化;
- P4 插件全生命周期规格(七态/三级权限/灰度/回滚)——本稿第八章 S0-S3 运行级与其对接;
- 优化报告 §5.3 双 TaskManager 治理——本稿第二章内核五件事之"任务与队列"为其落地口径。

**张力及化解**:本稿第四章"主智能体自主编排"与 P1"插件无自主执行权"存在表述张力,化解方式已定稿:**自主在规划层、确定在执行层**——智能体拥有分解/组合/迭代自由度,但执行权/预算权/中断权/审计权仍在内核,每次工具调用过权限门与风险分档。"唯一中枢"未被破坏,被破坏的只是"智能体只能被动接单"的旧设想。

**执行期排期参考**(与第三章修复顺序一致):
1. 主链路真实化(执行钩子/子代理 prompt+rag_filters/输出汇合)——一切的前提;
2. 学习环接线(抽取器/应用器调用、评分持久化、阈值触发);
3. 结构层落地(卷章拍纲要+预算对账+伏笔账本);
4. 锁定场与归并(物化 must 集/实体归一/偏离登记);
5. 预算执行优化随批嵌入(前缀缓存与三账本优先,零风险高收益)。

> 本稿为讨论定稿,不修改任何业务代码;实施以各批次方案文档为准,冲突时以最新批次方案为准。

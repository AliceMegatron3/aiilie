# No.0 AI V4.0 后续系统性优化建议报告

> 编制日期：2026-08-15  
> 编制范围：后端、前端、功能、代码优化四个部分  
> 依据：当前代码库、既有优化报告、运行时与凭证加固审查结果及现有测试/构建结果  
> 说明：本文是后续优化建议，不修改业务代码；优先级表示实施紧迫度，不等同于开发工作量。

---

## 一、评估结论与范围边界

### 1.1 总体结论

当前系统已经完成一轮较大范围的运行时和工程整改，已经具备以下基础能力：

- 后端采用 FastAPI，已接入 SQLite WAL、数据库连接池、后台队列和长任务分段执行能力；
- 任务队列已经处理 coroutine 所有权、worker 异常、取消和 `task_done()` 等运行时问题；
- 前端已经具备 Vue 3、Pinia、Axios、WebSocket 指数退避重连、可见性感知轮询和文档自动保存；
- 云端凭证门控、Ollama 配置校验、路径越界防护、上传限制和基础 API 错误处理已经落地；
- Python 全量测试、前端类型检查、前端构建和基础 Vitest smoke test 已经通过。

因此，下一轮不再把“增加基础队列”“增加健康轮询”“移除硬编码密钥”“补充 Ollama 基础校验”等已经完成的工作列为未解决问题。后续风险主要集中在：

1. 任务的业务级幂等、重启恢复、状态一致性和可追溯性还需要形成完整闭环；
2. SQLite 作为单机数据层的并发边界、迁移、备份和扩展策略还需要明确；
3. WebSocket 虽然可以重连，但重连后的订阅恢复和断线期间事件补偿仍不充分；
4. 前端 API 类型、错误结构、请求缓存和编辑器保存流程仍存在分散实现；
5. CI 对前端类型检查、构建、单元测试、依赖警告和发布链路的约束还不够完整；
6. 任务、模型、历史记录和失败恢复能力需要从“接口可用”提升为“用户流程完整”。

### 1.2 已完成事项，不作为本报告待办项

以下事项已在既有报告中确认完成，本报告仅在相关建议中说明其后续深化方向：

- 队列 worker 异常、取消、`task_done()` 和异步任务工厂治理；
- 前端健康探针单例、显式启停、可见性轮询和项目列表短窗口去重；
- 云端密钥门控、启动脚本明文凭证移除、敏感配置脱敏；
- Ollama 基础配置检查、HTTP 错误分类、超时和畸形响应处理；
- 基础 API 鉴权依赖、路径安全、上传格式和大小限制；
- 文档创建、保存、重命名、删除、版本历史和回滚基础功能；
- Tailwind 配置、死组件清理、中心视图补全等上一轮收尾事项。

### 1.3 三阶段实施总览

| 阶段 | 目标 | 重点内容 |
|---|---|---|
| 第一阶段：高优先级稳定性 | 避免数据丢失、任务失控和部署不可用 | 生产鉴权与 readiness；任务幂等、恢复和取消；WebSocket 订阅恢复；编辑器保存可靠性；CI 纳入前端与类型检查 |
| 第二阶段：中优先级体验与工程化 | 提升跨模块一致性、可维护性和故障诊断能力 | API 类型契约；任务中心；provider 级熔断；缓存/Docker 配置统一；测试扩展；mypy 豁免收缩 |
| 第三阶段：低优先级维护性 | 降低长期维护成本并建立演进基础 | 死代码和分包优化；架构文档；配置 Schema；性能基准趋势监控 |

---

## 二、后端优化建议

### 2.1 生产鉴权、启动配置与 readiness

**优先级：高｜实施阶段：第一阶段**

#### 现状

- `C:\Users\11482\Documents\aiilie\config\config.yaml` 中 `security.require_auth` 默认值为 `false`，开发态方便，但生产部署如果没有显式覆盖，可能以未鉴权模式启动。
- 当前已有 `/health` 基础健康接口和鉴权依赖，但“进程已启动”“应用已完成初始化”“数据库/队列/模型可接收任务”尚未明确区分。
- Docker、桌面启动和直接运行三种方式的配置注入路径不完全一致，容易出现开发配置被带入生产环境的问题。

#### 优化目标

- 让生产环境默认拒绝未鉴权请求，并对不安全配置启动失败或明确告警。
- 区分 liveness、readiness 和业务依赖健康状态。
- 在应用尚未完成数据库、队列和必要依赖初始化时，不对外宣称“可接收任务”。

#### 具体措施

1. 增加 `/health/live` 和 `/health/ready`，分别表示进程存活和服务已就绪；保留现有基础 `/health` 作为兼容入口。
2. readiness 至少检查数据库连接、迁移版本、后台 worker 状态、关键目录可写性和必要模型配置状态。
3. 为生产模式增加显式配置校验：`require_auth=true`、非空 `auth_secret`、受限 CORS、合法的外部服务 URL；校验失败时阻止启动或进入 not-ready 状态。
4. 为 Docker、Electron 和本地开发分别提供示例配置，明确区分开发配置与生产配置，避免直接修改受版本控制的默认配置。
5. 将 readiness 状态接入 Docker healthcheck 和部署编排；未 ready 时不接收长任务提交，但仍允许健康检查和诊断接口访问。
6. 在响应和日志中返回脱敏的状态原因、配置版本和启动阶段，不输出密钥、完整 URL 或内部堆栈。

#### 预期收益

- 避免服务“端口已监听但实际不可用”的误判。
- 降低生产环境误关闭鉴权、数据库未完成迁移即接收请求等风险。
- 为 Docker、Electron 后端进程管理和未来多实例部署提供标准健康语义。

#### 验收关注点

- `require_auth=true` 时，未携带令牌访问业务接口返回 401；合法令牌访问返回成功。
- 数据库初始化失败、迁移失败或 worker 未启动时，readiness 返回非 2xx。
- liveness 不依赖外部模型可用性，模型暂时故障不会导致进程被错误重启。

---

### 2.2 任务幂等、状态持久化与重启恢复

**优先级：高｜实施阶段：第一阶段**

#### 现状

- 项目同时存在 `C:\Users\11482\Documents\aiilie\core\task_manager.py` 和 `C:\Users\11482\Documents\aiilie\services\task_manager.py` 两套任务管理语义，已有职责说明，但跨模块任务状态和生命周期仍有统一空间。
- 任务已写入 SQLite，并支持分段执行和状态查询；但是“请求已落库、尚未入队”“进程在执行中崩溃”“客户端重试提交”这些边界需要明确的状态转换和恢复策略。
- 前端 Axios 已配置网络重试，但并非所有 POST 操作都天然幂等；网络超时后重复提交可能创建两个任务。

#### 优化目标

- 每个用户意图或客户端请求只创建一个可识别的任务。
- 服务重启后能够识别并恢复、重试或终止异常中断任务，而不是留下永久 `RUNNING` 状态。
- 任务状态以数据库为事实来源，内存队列只是执行载体。

#### 具体措施

1. 为任务提交接口引入 `Idempotency-Key`，按用户/项目/操作类型保存请求指纹、任务 ID、参数摘要和过期时间。
2. 为任务状态建立明确状态机，例如 `PENDING → QUEUED → RUNNING → SUCCEEDED/FAILED/CANCELLED`，禁止任意状态回退，并记录状态变更时间和原因。
3. 将“创建任务”和“进入可消费队列”设计为可恢复流程：数据库写入任务后，由启动扫描或 outbox/claim 机制补偿未入队任务。
4. worker 启动时扫描超时的 `RUNNING` 任务，根据任务类型判断为可重试、需人工恢复或直接失败；所有恢复动作记录 attempt 次数和恢复原因。
5. 取消操作使用幂等语义：重复取消返回当前终态，不重复执行清理，不覆盖已成功任务；执行中的任务通过取消标记、协作式检查和资源释放完成取消。
6. 将重试策略按任务类型配置：最大次数、退避时间、是否允许部分分段重试、是否允许切换模型；不可重试错误不得反复进入队列。
7. 为量化、文档学习、创作和深度思考统一输出 `task_id`、`operation_type`、`attempt`、`status`、`progress`、`error_code` 和 `created_at/updated_at`。

#### 预期收益

- 防止网络重试或用户重复点击造成重复任务和重复扣费/重复写入。
- 服务重启、worker 崩溃或机器异常后，任务不会长期悬挂。
- 后续任务中心、WebSocket 补偿和审计日志可以依赖统一状态模型。

#### 验收关注点

- 同一个 `Idempotency-Key` 重复提交只返回同一个任务。
- 模拟“落库后进程退出”“执行中进程退出”“取消与完成竞争”场景，最终状态符合状态机约束。
- 重复调用取消、重试、查询接口不会产生额外副作用。

---

### 2.3 SQLite 并发、迁移、备份与扩展策略

**优先级：高｜实施阶段：第一阶段至第二阶段**

#### 现状

- 系统已使用 SQLite WAL 和连接池，适合当前单机桌面/轻量部署场景。
- 任务、卡片、项目、版本、反思和配置等数据持续增长；多个后台 worker、API 请求和定时任务可能同时写入。
- 当前架构仍以 SQLite 为主要事实源，尚未形成明确的 schema 版本发布、备份恢复、归档和迁移到外部数据库的决策边界。

#### 优化目标

- 在 SQLite 规模内保证写入可预测、事务边界清晰和数据可恢复。
- 及时发现锁等待、长事务、数据库膨胀和迁移失败。
- 为未来 PostgreSQL 或其他服务型数据库迁移保留抽象边界，而不提前引入不必要的复杂度。

#### 具体措施

1. 统一数据库初始化和迁移入口，为每次 schema 变更提供递增版本、前向迁移、迁移日志和失败回滚/人工恢复说明。
2. 复核 `C:\Users\11482\Documents\aiilie\core\database.py`、`C:\Users\11482\Documents\aiilie\core\db_pool.py` 中的事务边界，禁止跨请求长期持有连接和事务；写事务尽量短小。
3. 统一设置并记录 WAL、busy timeout、foreign key、同步等级等连接参数，避免不同连接路径配置不一致。
4. 为高频查询补充索引并用实际查询计划验证，重点关注任务状态/项目 ID/创建时间/文档版本/卡片检索字段。
5. 增加备份策略：安全快照、备份文件校验、保留周期、恢复演练和应用关闭时的最终同步；不要只依赖 Docker volume 或工作区目录。
6. 对大结果、历史版本、临时文件和行为日志设置保留/归档策略，避免主数据库无界增长。
7. 增加数据库锁等待、事务耗时、连接池占用、WAL 文件大小和迁移版本指标；达到阈值时给出诊断告警。
8. 当并发量、写入吞吐或多实例需求超过 SQLite 能力边界时，再依据明确指标评估迁移 PostgreSQL；迁移前先抽象 repository/事务接口和数据导出格式。

#### 预期收益

- 减少 `database is locked`、迁移不一致和异常退出后的数据损坏风险。
- 让 SQLite 在当前单机架构下继续稳定工作，同时避免过早引入外部数据库运维成本。
- 为未来多用户、多实例或云部署提供可执行的扩展路线。

#### 验收关注点

- 并发写入压力测试下无未处理锁错误，事务耗时和等待时间有可观测记录。
- 从备份恢复后，任务、项目和文档版本数据可读取，schema 版本正确。
- 新版本从空库、旧库和中断迁移库启动均有明确结果。

---

### 2.4 API 错误模型、超时、鉴权审计与可观测性

**优先级：高｜实施阶段：第一阶段至第二阶段**

#### 现状

- 已有 `AppError` 体系和统一响应方向，但存量路由的响应结构、异常捕获和错误字段仍可能存在差异。
- 前端当前会根据 `detail`、`message` 和 Axios `error.message` 做兼容处理，说明接口错误契约还没有完全收敛。
- 请求超时、模型调用超时、队列等待超时和任务总时长超时属于不同问题，当前需要进一步区分。

#### 优化目标

- 为调用方提供稳定的 HTTP 状态码、错误码、消息、trace ID 和可重试标识。
- 让超时、鉴权失败、业务校验失败、模型不可用和内部错误可以被准确区分。
- 能够从一条请求追踪到任务、队列、模型调用和最终结果。

#### 具体措施

1. 固化错误响应格式：`success`、`data`、`message`、`error_code`、`retryable`、`trace_id`、`details`；敏感细节只写服务端日志。
2. 统一 400/401/403/404/409/422/429/500/502/503/504 的使用规则，并为重复提交、资源冲突和配额超限定义专用错误码。
3. 为请求、任务和模型调用生成或传递 trace ID；日志采用结构化字段记录 route、task_id、provider、model、duration、status 和 error_code。
4. 分离连接超时、读取超时、队列等待超时、单段执行超时和任务总超时；超时后明确是否可重试以及重试位置。
5. 对鉴权失败、异常来源、敏感设置读取、配置变更和任务取消记录审计事件，但不记录 token、API key、完整 prompt 或隐私正文。
6. 为 `/api/v1/system/status` 之外增加机器可读的指标输出或 Prometheus 兼容适配层，至少覆盖请求量、错误率、P95、队列深度、任务终态比例和模型调用成功率。

#### 预期收益

- 前端可以稳定地展示错误和决定是否重试。
- 运维可以快速定位是网络、排队、模型、数据库还是业务校验问题。
- 发生生产问题时具备最小必要的审计和追踪能力。

---

### 2.5 WebSocket 断线重连与任务状态同步

**优先级：高｜实施阶段：第一阶段**

#### 现状

- `C:\Users\11482\Documents\aiilie\frontend\src\composables\useWebSocket.ts` 已实现指数退避、心跳应答和任务订阅入口。
- 连接重建后，前端保存了 handler，但服务端订阅关系通常绑定在旧连接上；如果没有重新发送订阅动作，重连后的任务可能收不到后续事件。
- WebSocket 事件不是持久化消息，断线期间的进度事件可能丢失，当前需要依赖查询接口补足状态。

#### 优化目标

- 断线重连后自动恢复订阅并重新同步所有订阅任务的最新状态。
- 事件丢失不影响最终任务状态和用户判断。
- WebSocket 只承担实时加速，不能成为任务状态的唯一事实来源。

#### 具体措施

1. 前端保存订阅集合，不仅保存 handler；每次 `onopen` 后重新发送全部 `subscribe_task` 消息。
2. 服务端为事件增加 `event_id` 或单调递增 `sequence`，并在订阅确认时返回任务快照、当前版本和最后事件序号。
3. 增加重连同步接口，例如批量获取订阅任务状态；前端重连后先拉快照，再恢复实时事件，避免旧事件覆盖新状态。
4. 对任务终态事件进行短期持久化或通过数据库查询补偿；前端发现序号跳跃时主动请求状态快照。
5. 明确 WebSocket 鉴权方式，避免只依赖 URL 查询参数传递长期 token；Electron 场景使用短期会话令牌或安全握手。
6. 为连接关闭原因、重连次数、订阅恢复成功率和事件补偿次数增加日志与指标。

#### 预期收益

- 用户切换页面、网络抖动或电脑休眠后，任务进度不会停留在旧状态。
- 降低“任务实际完成但前端显示运行中”的投诉。
- 为统一任务中心和多页面任务订阅提供可靠基础。

---

### 2.6 Provider 级熔断、限流与预算隔离

**优先级：中｜实施阶段：第二阶段**

#### 现状

- 当前云端模型和本地 Ollama 已具备基础超时、错误分类和降级路径。
- 既有审查已指出本地与云端共享模型熔断器；一个 provider 的故障可能影响另一个 provider 的恢复和调度判断。
- 不同任务类型对 token、延迟和可靠性的要求不同，尚未完全形成 provider/模型/任务三级预算。

#### 优化目标

- 云端、Ollama 及未来其他 provider 独立统计、独立熔断和独立恢复。
- 在预算、并发和超时范围内做可解释的模型选择。
- 防止单类任务耗尽所有模型资源。

#### 具体措施

1. 为每个 provider 建立独立熔断器、半开探测、恢复窗口和错误分类。
2. 按 provider/model/task_type 设置并发上限、每分钟请求数、token 预算、请求超时和最大重试次数。
3. 区分配置错误、认证失败、限流、服务不可用、模型不存在和内容拒答，不把所有错误都计入同一种熔断原因。
4. 在调度日志中记录选择原因、备选 provider、切换次数和预算消耗，前端只显示脱敏后的可理解诊断。
5. 为本地模型增加资源感知阈值，避免 CPU/内存不足时继续堆积任务；云端切换也要遵守用户配置和隐私设置。

#### 预期收益

- Ollama 故障不会阻断云端恢复，云端限流也不会误判为本地模型故障。
- 模型调用成本、延迟和稳定性更加可控。
- 调度行为可以被解释、测试和持续优化。

---

### 2.7 缓存与 Docker 配置统一

**优先级：中｜实施阶段：第二阶段**

#### 现状

- `C:\Users\11482\Documents\aiilie\docker-compose.yml` 默认启动 Redis，但 `CACHE_BACKEND` 默认值为 `memory`，单体部署仍会产生不必要的 Redis 依赖。
- memory 和 Redis 两种后端的容量、过期、故障降级和一致性语义需要进一步对齐。

#### 优化目标

- 明确单机 memory 模式、Docker Redis 模式和未来多实例模式的适用边界。
- 配置、服务依赖、健康检查和故障降级行为保持一致。

#### 具体措施

1. 将 Docker Compose 拆分为基础单体配置和 Redis profile，例如 `--profile redis` 才启动 Redis；或在文档中明确使用方式并移除无条件依赖。
2. 为缓存配置增加 schema：backend、URL、前缀、默认 TTL、最大条目、连接超时和失败策略。
3. 统一两个后端的 key 命名、TTL、序列化、删除和 miss 语义；memory 模式必须有容量上限和过期驱逐。
4. 明确缓存不可用时是降级为 miss、阻止请求还是返回 503，避免不同调用方各自处理。
5. 增加缓存命中率、驱逐次数、Redis 连接失败和降级次数指标。

#### 预期收益

- 开发者可以无 Redis 运行单体模式，部署更轻量。
- 多实例部署时可以明确切换 Redis，而不是依赖隐式降级。
- 缓存故障不再以不可预测的方式影响业务任务。

---

## 三、前端优化建议

### 3.1 API 类型契约与统一错误处理

**优先级：高｜实施阶段：第二阶段**

#### 现状

- `C:\Users\11482\Documents\aiilie\frontend\src\api\index.ts` 已集中管理大量端点，但仍存在 `any`、`Record<string, unknown>`、多个响应层级兼容访问和调用方自行解析错误的情况。
- `DocumentEditor.vue`、项目 store 和其他面板分别读取 `error.response?.data?.detail` 等字段，错误处理逻辑分散。
- 后端接口数量较多，部分新旧接口返回结构并存，前端类型无法完整表达状态、分页、任务和错误模型。

#### 优化目标

- 前后端共享稳定的请求、响应、分页、任务和错误类型。
- 组件只处理业务语义，不直接依赖 Axios 内部错误结构。
- API 变更可以在类型检查和契约测试阶段尽早发现。

#### 具体措施

1. 从 FastAPI OpenAPI 生成或维护前端 TypeScript 类型，至少覆盖任务、项目、文档、版本、模型设置、错误和分页结构。
2. 将 `api/index.ts` 按领域拆分为 `api/tasks`、`api/projects`、`api/models` 等模块，但保留统一客户端和导出入口。
3. 定义 `ApiError`、`TaskStatus`、`TaskProgressEvent`、`PaginatedResponse<T>` 等基础类型，逐步消除 `any`。
4. 在 Axios 拦截器中把网络错误、超时、HTTP 错误和业务错误归一化为前端错误对象，组件使用统一 `getErrorMessage()` 或 composable。
5. 对 GET、可安全重试的查询和不可自动重试的 POST 明确策略，避免全局重试误伤非幂等请求。
6. 在 CI 中加入 OpenAPI 快照或契约差异检查，接口变更必须同步前端类型和迁移说明。

#### 预期收益

- 减少字段拼写错误、错误响应解析不一致和后端升级导致的隐性回归。
- 提升 IDE 补全、重构安全性和新功能开发速度。
- 让 API 失败提示和重试行为在所有页面保持一致。

---

### 3.2 全局状态、请求缓存与数据失效治理

**优先级：中｜实施阶段：第二阶段**

#### 现状

- 已有 Pinia 项目 store 和 `loadedAt` 短窗口去重，但任务、文档、系统状态、反思、模板和模型设置仍由多个组件分别加载。
- 各页面的 loading、error、刷新和清空逻辑存在重复，组件卸载或切换项目时容易留下旧数据。
- WebSocket 实时数据、轮询数据和手动刷新之间缺少统一的更新优先级。

#### 优化目标

- 建立按领域划分的全局状态和缓存策略。
- 明确数据的缓存时间、失效条件、强制刷新和乐观更新回滚行为。
- 切换项目或文档时不展示旧项目数据，不产生重复请求。

#### 具体措施

1. 以项目、文档、任务、系统状态、设置为边界整理 Pinia store，统一 `isLoading/error/loadedAt/requestId` 状态。
2. 为查询数据定义 TTL 和失效事件：项目更新、文档保存、任务终态、模型设置修改后主动失效关联缓存。
3. 使用请求去重和 request ID 防止旧请求晚到后覆盖新数据；切换项目时取消或忽略旧请求。
4. 统一 loading、空状态、错误状态和重试按钮，避免每个面板自行复制交互逻辑。
5. 规定数据来源优先级：WebSocket 事件更新局部状态，定时查询做兜底，手动刷新做强制同步。
6. 对大列表采用分页或增量加载，对历史记录和任务日志避免一次性加载全部内容。

#### 预期收益

- 减少重复请求和页面切换时的数据闪烁。
- 避免旧请求覆盖新项目状态、实时事件被轮询覆盖等一致性问题。
- 降低页面组件的状态管理复杂度。

---

### 3.3 长任务进度、断线恢复与统一任务中心

**优先级：高｜实施阶段：第一阶段至第二阶段**

#### 现状

- 前端已经能够通过 `useWebSocket` 订阅 `task_progress`，同时保留任务状态查询接口。
- 不同面板可能各自展示量化、反思或创作任务，用户无法在一个入口查看所有长任务，也难以理解失败原因和下一步操作。
- WebSocket 重连后的状态补偿、任务终态持久化展示和浏览器刷新后的任务恢复还需要统一。

#### 优化目标

- 所有长任务均有统一的列表、详情、进度、日志、取消、重试和恢复入口。
- 前端刷新、切换页面、网络断开后仍能恢复任务状态。
- 进度信息真实表达阶段和可预期等待，而不是只显示百分比。

#### 具体措施

1. 建立任务中心 store 和页面，按项目、任务类型、状态、创建时间筛选。
2. 任务卡片显示阶段、当前分段、已处理/总量、耗时、预计剩余时间、模型、错误码和最近更新时间。
3. 统一接入 WebSocket 实时事件、重连快照和 REST 状态查询；事件缺失或序号跳跃时自动补偿。
4. 对 `FAILED` 任务展示可操作原因：重试、修改配置后重试、导出日志、取消后清理或联系管理员。
5. 任务终态在前端保留一段时间，页面刷新后从后端恢复，不依赖组件内存。
6. 对不可精确计算的任务使用阶段进度和文案，避免向用户展示误导性的精确百分比。

#### 预期收益

- 长任务从“发起后等待”变成可追踪、可恢复的产品能力。
- 降低用户重复点击和误以为任务失败而重复提交的概率。
- 为后端统一任务状态、幂等和审计提供直接使用场景。

---

### 3.4 文档编辑器保存可靠性

**优先级：高｜实施阶段：第一阶段**

#### 现状

- `C:\Users\11482\Documents\aiilie\frontend\src\components\views\DocumentEditor.vue` 已支持输入防抖自动保存、手动保存、版本历史和回滚。
- 组件卸载时当前采用 fire-and-forget 的异步保存方式，页面关闭、Electron 进程退出或网络异常时不能保证请求完成。
- 保存期间再次编辑、多个窗口同时编辑、回滚与自动保存竞争等场景的冲突处理不够明确。

#### 优化目标

- 明确区分已保存、保存中、待保存、保存失败和冲突状态。
- 尽量避免窗口关闭或组件切换造成未保存内容丢失。
- 发生并发修改时不静默覆盖其他版本。

#### 具体措施

1. 引入客户端草稿状态和本地恢复机制，记录文档 ID、版本号、更新时间和内容校验摘要；敏感内容按桌面应用安全策略存储。
2. 保存接口携带 `base_version` 或 `updated_at`，服务端检测版本冲突并返回 409，而不是直接覆盖。
3. 对保存请求增加序列号或 AbortController：新保存请求不能被旧请求晚到结果覆盖；保存失败保留草稿并提供重试。
4. Electron 关闭时使用可等待的保存流程和关闭确认；浏览器场景使用 `beforeunload` 仅做提示，不依赖异步请求保证保存。
5. 回滚前保存当前草稿或明确提示未保存内容，回滚完成后刷新本地版本号和内容基线。
6. 增加版本差异预览、草稿恢复和“另存为新版本”选项，减少用户对回滚的顾虑。

#### 预期收益

- 降低长篇创作中的数据丢失和静默覆盖风险。
- 用户可以清楚知道当前内容是否安全落库。
- 文档版本系统从基础 CRUD 提升为可靠编辑闭环。

---

### 3.5 Electron 生命周期、请求策略与发布体验

**优先级：中｜实施阶段：第二阶段**

#### 现状

- 项目使用 Electron 启动本地后端，并在 `C:\Users\11482\Documents\aiilie\frontend\index.js` 处理后端进程生命周期。
- 前端 API 已根据 Electron 环境切换地址，健康探针和 WebSocket 也已有生命周期治理。
- 后端启动失败、端口冲突、升级迁移失败、退出时任务处理和发布包内配置校验仍需要统一用户反馈。

#### 优化目标

- Electron 用户能够知道后端是否启动、为什么失败以及如何恢复。
- 应用退出和升级时不产生残留进程、未完成任务和配置丢失。
- 发布包使用明确的生产配置和可回滚版本。

#### 具体措施

1. 将后端进程状态建模为 starting/ready/degraded/stopping/failed，并在 UI 中展示状态和诊断入口。
2. 启动时执行端口、目录、数据库迁移和配置检查；失败时提供可读错误和日志位置。
3. 退出时先停止接收新任务，再等待可配置的优雅关闭窗口；超时后记录未完成任务并由后端恢复机制接管。
4. 校验 Electron 打包产物、`package-lock.json`、后端资源、配置模板和数据库目录，避免开发路径被打入发布包。
5. 为 Windows 安装包/便携包提供版本号、升级策略、日志导出和回滚说明。

#### 预期收益

- 减少“窗口打开但后端不可用”“退出后残留后台进程”等桌面端问题。
- 提升发布可重复性和故障自诊断能力。

---

## 四、功能优化建议

### 4.1 统一任务管理与失败恢复

**优先级：高｜实施阶段：第一阶段至第二阶段**

#### 现状

- 量化、文档学习、创作、反思和深度思考均具有不同的任务入口和执行链路。
- 后端已有任务查询、取消或进度相关接口，但用户操作路径和结果反馈未完全统一。
- 失败时用户通常只能看到“失败”或错误文案，缺少可执行的恢复建议。

#### 优化目标

- 用户可以在一个任务中心完成查看、暂停/取消、重试、恢复和清理。
- 失败原因分为可重试、需修改配置、资源不足、输入错误和系统错误，并给出对应操作。

#### 具体措施

1. 统一任务生命周期和任务详情页，关联项目、文档、模型、输入摘要和输出位置。
2. 支持按任务类型定义恢复点：分段任务从失败分段继续，模型调用任务从可重试节点继续，非幂等任务明确禁止盲目重试。
3. 增加取消确认、重试确认和清理临时文件操作，显示操作后果。
4. 将失败日志、trace ID、错误码和建议操作集中展示，支持导出脱敏诊断包。
5. 对长任务完成、失败、取消和需要用户操作的状态提供统一通知，不依赖用户一直停留在原页面。

#### 预期收益

- 提升长任务的可用性和完成率。
- 减少重复提交、无效等待和无法解释的失败。

---

### 4.2 进度反馈与等待体验

**优先级：高｜实施阶段：第一阶段至第二阶段**

#### 现状

- 分段执行引擎和 WebSocket 已能提供部分进度信息，但不同任务的进度粒度和文案不一致。
- 对模型排队、数据库处理、外部服务调用和结果落盘等阶段的等待原因展示不足。

#### 优化目标

- 让用户知道任务处于排队、处理中、外部调用、合并结果还是清理阶段。
- 对不可预测耗时提供可信的阶段提示、最近更新时间和取消入口。

#### 具体措施

1. 为任务定义统一阶段枚举和阶段说明，后端事件与前端展示共享类型。
2. 进度同时提供阶段进度和总体进度；没有可靠总量时不伪造百分比。
3. 展示队列位置、已等待时间、最近心跳、预计剩余时间区间和资源占用提示。
4. 长时间无事件时提示“仍在运行/正在等待 provider/连接已恢复”等明确状态，而不是显示假死。
5. 对浏览器隐藏、Electron 最小化和断线场景保留任务通知及恢复入口。

#### 预期收益

- 降低用户误判、重复点击和强制关闭应用的概率。
- 让系统性能问题更容易通过用户反馈定位到具体阶段。

---

### 4.3 模型配置、连接诊断与隐私提示

**优先级：中｜实施阶段：第二阶段**

#### 现状

- 系统支持云端模型和本地 Ollama，并有配置读取、凭证门控和基础错误处理。
- 用户对当前 provider、模型是否可用、失败是配置问题还是服务问题，缺少统一可视化诊断。
- 云端调用与本地调用涉及不同的隐私边界和成本，设置页应提供更明确的说明。

#### 优化目标

- 用户能在设置页完成配置检查、连接测试和模型选择，并理解数据会发送到哪里。
- provider 不可用时有明确的替代方案和恢复条件。

#### 具体措施

1. 展示 provider、模型、连接状态、最近检查时间、熔断状态和脱敏错误原因。
2. 提供“测试连接”和“检查模型”操作，区分配置缺失、鉴权失败、模型不存在、服务未启动和网络超时。
3. 云端模型显示隐私、费用和数据传输提示；本地 Ollama 显示模型下载、磁盘和内存要求。
4. 允许用户设置任务级 provider 偏好、是否允许自动降级以及最大等待/重试策略。
5. 配置变更后立即校验并刷新相关缓存、熔断状态和前端显示。

#### 预期收益

- 减少模型配置相关的黑盒失败。
- 提升用户对模型选择、隐私和成本的控制感。

---

### 4.4 历史记录、版本和结果可追溯性

**优先级：中｜实施阶段：第二阶段**

#### 现状

- 文档已有版本历史，任务和反思等功能也会产生结果，但跨任务、项目、文档和模型的关联检索能力有限。
- 大结果可能落盘，用户需要明确知道结果存放位置、生成时间和来源任务。

#### 优化目标

- 任一创作结果都能追溯到输入、配置、任务、模型和版本。
- 用户可以搜索、筛选、比较和恢复历史结果。

#### 具体措施

1. 为结果增加来源元数据：project_id、doc_id、task_id、provider、model、prompt/config 摘要、版本和时间。
2. 增加统一历史页，支持按项目、文档、任务类型、状态和时间筛选。
3. 提供版本差异、结果预览、导出和恢复为新版本，避免直接覆盖当前内容。
4. 对已清理或过期的临时结果显示状态，不展示失效下载链接。
5. 定义历史记录保留策略、用户主动删除策略和必要的隐私清理机制。

#### 预期收益

- 提高创作过程的可复盘性和可恢复性。
- 方便定位模型差异、任务失败和文档版本问题。

---

### 4.5 功能开关与用户提示一致性

**优先级：中｜实施阶段：第二阶段**

#### 现状

- `C:\Users\11482\Documents\aiilie\config\config.yaml` 中存在多个功能开关，例如情感量化、分支版本、时间轴、TTS、分镜和深度思考。
- 后端关闭功能时可以拒绝请求，但前端未必总能在入口处展示关闭原因，容易出现按钮可点击、提交后才失败的体验。

#### 优化目标

- 后端能力、前端菜单、按钮状态和错误提示由同一份能力信息驱动。
- 功能关闭、依赖缺失、模型不可用和权限不足在产品上有不同提示。

#### 具体措施

1. 增加 capability/config 接口，返回功能是否启用、依赖是否满足、是否允许当前用户操作及脱敏原因。
2. 前端启动或切换项目时缓存能力信息，统一控制菜单、按钮、快捷入口和路由访问。
3. 禁用状态提供说明和配置入口，不使用笼统的“服务器错误”。
4. 为每个功能建立启用、禁用、依赖缺失、执行成功、执行失败和恢复后的验收矩阵。

#### 预期收益

- 降低前后端配置不一致造成的误操作。
- 让实验性功能逐步开放时具备可控的灰度基础。

---

## 五、代码与工程质量优化建议

### 5.1 CI 纳入前端类型检查、测试和构建

**优先级：高｜实施阶段：第一阶段**

#### 现状

- 当前 `C:\Users\11482\Documents\aiilie\.github\workflows\ci.yml` 的主要步骤是使用 uv 安装依赖并运行后端 pytest。
- 前端 `npm run type-check`、`npm run build` 和 Vitest smoke test 已在本地验证通过，但没有形成与后端同等强度的持续门禁。
- 既有报告曾提出前端、E2E 和性能基准纳入 CI，但应以当前工作流实际内容为准重新核实并落地。

#### 优化目标

- Pull Request 必须同时通过后端、前端和关键质量检查。
- CI 失败能明确区分依赖安装、类型、测试、构建、契约和性能回归。

#### 具体措施

1. 后端 job 增加 Python 编译、pytest、mypy 目标范围和必要的路由/主应用导入检查。
2. 前端 job 使用锁文件安装依赖，执行 `npm run type-check`、`npm run test`/Vitest 和 `npm run build`。
3. 增加 OpenAPI 契约检查、`git diff --check`、敏感信息扫描和依赖审计。
4. 为 Docker 构建、Electron 打包和核心 E2E 测试设置按需 workflow，避免每次 PR 都执行过重流程但不能完全缺失发布前验证。
5. 对第三方弃用警告建立允许清单和过期时间；重点处理 `httpx`/Starlette TestClient 兼容警告，避免 `--disable-warnings` 长期掩盖问题。
6. 上传测试报告、覆盖率、构建产物摘要和失败日志，方便定位，不上传密钥和用户数据。

#### 预期收益

- 前端回归、类型漂移和构建失败可以在合并前被拦截。
- 质量标准从“本地验证通过”变为“每次变更自动验证”。

#### 验收关注点

- 故意引入前端类型错误、Vitest 失败、后端测试失败和构建失败时，CI 均能正确阻断。
- 正常提交完成后，后端与前端报告均可追溯。

---

### 5.2 收紧 mypy 豁免并提升类型安全

**优先级：高｜实施阶段：第二阶段**

#### 现状

- `C:\Users\11482\Documents\aiilie\pyproject.toml` 已开启 strict 方向，但仍有存量模块迁移期配置和部分未类型化代码。
- 双 TaskManager、路由依赖、模型响应和服务适配层是类型边界较多的区域。

#### 优化目标

- 新代码默认无豁免，旧代码按模块逐步收紧。
- 任务状态、API payload、配置和 provider 适配器具有明确类型。

#### 具体措施

1. 统计当前豁免模块、错误数量和负责人，按 API/任务/模型/数据层分批移除。
2. 先处理高风险公共接口和新增模块，禁止新增 `# type: ignore`，例外必须带原因和跟踪编号。
3. 为配置对象、任务状态机、模型 provider、API 响应和数据库记录定义 TypedDict、dataclass 或 Pydantic 模型。
4. 将 `Any` 限制在外部库边界，进入业务层立即转换为明确类型。
5. CI 对新增文件执行严格检查，对存量豁免设置错误数不可增加的基线门禁。

#### 预期收益

- 在运行前发现字段漂移、可空值错误和异步接口误用。
- 降低双任务管理器和多 provider 适配的维护成本。

---

### 5.3 双 TaskManager、模块拆分与重复逻辑治理

**优先级：中｜实施阶段：第二阶段至第三阶段**

#### 现状

- `core` 与 `services` 下存在两套 `TaskManager`，虽然注释已经说明区别，但名称相同会增加阅读、依赖和测试成本。
- API、任务状态查询、错误解析、前端 loading/通知等位置仍有相似逻辑分散实现。

#### 优化目标

- 每个模块只有清晰、稳定、可发现的职责边界。
- 共享行为集中实现，差异通过接口或适配器表达。

#### 具体措施

1. 先绘制两套 TaskManager 的职责、调用方、状态来源和生命周期关系；不要在未完成依赖分析前直接删除其中一套。
2. 为底层持久化任务管理器和业务执行编排器使用具有语义的名称，例如 `PersistentTaskStore`、`TaskOrchestrator` 或等价命名。
3. 定义统一任务协议、状态枚举、取消、重试和查询接口，增加兼容适配层后再迁移调用方。
4. 将重复的错误提取、时间格式化、分页、任务状态转换和 toast 逻辑提取为共享工具或 composable。
5. 按领域拆分过大的 API 客户端、路由模块和前端面板，拆分过程中保持公开导出兼容。

#### 预期收益

- 新成员更容易理解任务架构，减少导错类和调用错状态 API 的风险。
- 修复一次即可覆盖更多调用方，长期维护成本下降。

---

### 5.4 异常、日志与敏感信息治理

**优先级：中｜实施阶段：第二阶段**

#### 现状

- 已有业务异常体系和部分参数化日志，但存量代码仍可能存在宽泛异常捕获、直接展示底层错误、日志格式不一致和用户输入过度记录的问题。
- 模型调用、任务输入和设置数据可能包含隐私或凭证关联信息。

#### 优化目标

- 日志可检索、可关联、不过度暴露敏感信息。
- 异常在边界处转换为稳定业务错误，内部保留完整诊断上下文。

#### 具体措施

1. 统一日志字段和级别：trace_id、task_id、provider、operation、duration、result、error_code。
2. 禁止记录 API key、Authorization、完整 prompt、完整生成内容和用户原文；必要时使用长度、摘要或脱敏片段。
3. 将 `except Exception` 限制在任务边界、后台 worker 和应用边界，并在捕获后保留 `logger.exception` 上下文、转换错误或重新抛出。
4. 统一使用参数化日志，不使用高频路径 f-string 拼接；对异常重试增加采样和限频，避免日志风暴。
5. 为日志保留、轮转、导出和删除建立策略，Electron 用户可一键导出脱敏诊断包。

#### 预期收益

- 提升问题定位效率，同时降低凭证和用户内容泄露风险。
- 避免大量重复重试日志影响实际故障判断。

---

### 5.5 依赖升级、弃用警告与安全供应链

**优先级：中｜实施阶段：第二阶段**

#### 现状

- 当前测试已通过，但存在 `httpx` 与 Starlette TestClient 相关弃用警告。
- 前后端依赖锁定和安装方式并存，发布链路还需要持续验证锁文件、构建工具和 Electron 产物的一致性。

#### 优化目标

- 依赖升级可控、可回滚，弃用警告不会无限期累积。
- CI 和发布包使用可复现依赖。

#### 具体措施

1. 建立依赖升级月度/季度窗口，先在兼容性分支运行全量测试，再合并锁文件变化。
2. 处理 `httpx`/Starlette TestClient 警告，明确升级 Starlette、固定兼容版本或改用推荐的异步测试客户端。
3. 使用 Dependabot/Renovate 或等价工具提交升级 PR，并结合安全公告和许可检查。
4. 校验 `C:\Users\11482\Documents\aiilie\uv.lock`、`C:\Users\11482\Documents\aiilie\frontend\package-lock.json` 与声明文件的一致性。
5. 对 Electron、PyInstaller、Vite 和 Python 运行时进行发布前兼容矩阵验证。

#### 预期收益

- 降低突然升级导致的运行时回归和构建不可重复风险。
- 及时清理弃用 API，避免未来升级时一次性爆发大量问题。

---

### 5.6 测试分层与覆盖范围扩展

**优先级：中｜实施阶段：第二阶段**

#### 现状

- 当前后端已有较完整的单元/集成回归测试，最近全量结果为 `70 passed, 1 skipped`；前端基础 smoke test 已通过。
- WebSocket 重连补偿、API 错误归一化、Pinia 缓存失效、编辑器保存冲突、Electron 生命周期和 Docker readiness 等关键场景仍需要专门测试。

#### 优化目标

- 用分层测试覆盖纯逻辑、数据库、路由、外部 provider、前端状态和桌面生命周期。
- 关键故障场景有自动化回归，不依赖人工重复验证。

#### 具体措施

1. 后端分为单元测试、数据库集成测试、API 契约测试、队列/恢复测试和 provider mock 测试。
2. 增加任务幂等、状态机非法迁移、重启恢复、取消竞态、SQLite 锁、迁移、备份恢复和 readiness 测试。
3. 前端增加 Axios 拦截器、健康状态、WebSocket 重连/订阅恢复、任务 store、项目缓存和编辑器保存测试。
4. 增加关键用户路径 E2E：提交长任务并断线恢复、文档编辑保存/冲突/回滚、模型配置测试、任务失败重试。
5. 对外部云端和 Ollama 使用 deterministic mock；真实 provider 只在手动或 nightly workflow 执行。
6. 维护测试夹具隔离、临时目录清理和警告基线，避免测试依赖本机真实配置或残留数据。

#### 预期收益

- 让高风险边界在重构和依赖升级时自动暴露。
- 提高测试结果的稳定性和对生产问题的解释能力。

---

### 5.7 死代码清理、组件分包与性能基线

**优先级：低｜实施阶段：第三阶段**

#### 现状

- 经过多轮整改，仍需要持续确认未引用组件、兼容路由、旧 API、诊断脚本和临时构建产物的归属。
- 前端已有基础代码分割，但功能模块、Electron 首屏和大页面组件的体积趋势还缺少长期基线。

#### 优化目标

- 删除确认无引用、无发布价值的死代码和旧入口。
- 在不牺牲用户体验的前提下控制首屏、内存和构建产物增长。

#### 具体措施

1. 使用静态分析、构建产物分析和运行时路由检查确认死代码，先删除无引用组件，再处理兼容层和诊断脚本。
2. 对大型功能按路由或面板懒加载，避免首页加载量化、分镜、深度思考等非首屏模块。
3. 提取重复主题色和布局样式为设计变量，减少组件内硬编码和样式重复。
4. 记录前端首屏 JS/CSS、gzip/brotli 体积、构建时间和主要 chunk 的基线。
5. 记录后端关键接口 P50/P95、任务排队时间、SQLite 锁等待、provider 延迟和错误率，按版本比较趋势。
6. 性能基准只对稳定环境执行，区分本地开发指标、CI 指标和发布前基准，避免单次机器噪声误报。

#### 预期收益

- 降低长期代码噪音和打包体积。
- 在性能退化真正影响用户前发现趋势，而不是依赖主观感受。

---

### 5.8 架构文档、配置 Schema 与决策记录

**优先级：低｜实施阶段：第三阶段**

#### 现状

- 仓库包含多份批次实施报告和优化报告，但“当前有效架构”和“历史已废弃方案”分散在不同文档中。
- 配置项较多，包含安全、任务、模型、功能开关、TTS、时间轴和深度思考等多个领域；目前主要依赖 YAML 注释说明。

#### 优化目标

- 新成员可以快速理解系统边界、任务生命周期、数据流和部署方式。
- 配置错误在启动时发现，而不是运行到具体功能才失败。
- 重要架构取舍有可追踪记录。

#### 具体措施

1. 增加当前架构总览，至少描述 API、任务状态机、SQLite、缓存、provider、WebSocket、Electron 和文件存储关系。
2. 为关键取舍建立 ADR：继续使用 SQLite 的边界、双 TaskManager 合并路线、WebSocket 快照补偿、provider 熔断隔离和 Electron 关闭策略。
3. 为 `config.yaml` 建立 Pydantic 配置模型或等价 Schema，包含类型、默认值、范围、敏感字段、环境变量覆盖名和生产要求。
4. 启动时进行配置校验并输出脱敏后的有效配置摘要；禁止把敏感字段写入日志或报告。
5. 每次 schema、API、配置和任务状态变更同步更新迁移说明、验收用例和回滚方案。

#### 预期收益

- 降低知识只存在于历史对话和个人经验中的风险。
- 配置、架构和代码演进更加可控，便于后续团队协作。

---

## 六、建议验收指标

| 领域 | 第一阶段目标 | 第二阶段目标 | 第三阶段目标 |
|---|---|---|---|
| 鉴权与可用性 | 生产未鉴权请求全部拦截；readiness 准确反映初始化状态 | 能区分依赖降级与服务不可用 | 形成发布前配置合规检查 |
| 任务可靠性 | 重复提交不重复创建；重启后无永久 RUNNING；取消幂等 | 任务中心统一管理并支持按类型恢复 | 任务生命周期和架构文档稳定 |
| 实时进度 | WebSocket 重连自动恢复订阅；断线可查询快照 | 事件序号/补偿和统一任务通知 | 性能和事件延迟有趋势基线 |
| 编辑体验 | 保存失败可重试；关闭/切换有未保存提示 | 版本冲突、草稿恢复和差异预览 | 编辑器状态和恢复策略文档化 |
| API/类型 | 错误结构和重试边界明确 | OpenAPI 与 TypeScript 契约同步 | 配置 Schema 与 API 版本策略稳定 |
| CI/测试 | 后端、前端类型、Vitest、构建均为门禁 | 关键恢复、WebSocket、Electron E2E 覆盖 | 性能、依赖和发布矩阵长期运行 |
| 数据层 | 事务边界、迁移和备份可验证 | 锁等待、连接池、缓存指标可观测 | 依据指标决定是否迁移 PostgreSQL |

## 七、实施注意事项

1. **先统一事实来源，再扩展界面。** 任务中心、进度条和通知应建立在统一任务状态与数据库快照之上，不应继续为每类任务维护独立状态语义。
2. **不要把所有问题都通过引入新基础设施解决。** 当前 SQLite 适合单机和轻量场景，应先完成事务、迁移、备份和指标治理，再根据真实并发数据决定是否迁移数据库。
3. **自动重试必须有幂等前提。** GET/查询可以安全重试，但任务提交、文档保存和模型调用必须根据操作语义配置重试，而不能只依赖 Axios 全局策略。
4. **WebSocket 不是持久化队列。** 所有关键任务状态都必须可以通过 REST/数据库快照恢复，实时事件只用于降低延迟。
5. **生产配置和开发配置分离。** 特别是鉴权、CORS、外部服务地址、日志级别、数据目录和模型凭证，不能依赖开发默认值。
6. **继续处理已知依赖警告。** 现有测试中的 `httpx`/Starlette 兼容弃用警告不影响当前通过结果，但应纳入依赖治理，不应长期使用 `--disable-warnings` 掩盖。
7. **外部凭证必须在供应商控制台轮换。** 历史上已暴露的凭证不能仅通过删除代码恢复安全，仍需实际吊销并重新生成。

## 八、结论

下一轮优化的核心不是继续堆叠单点功能，而是建立“可鉴权启动、可恢复任务、可补偿进度、可冲突安全保存、可持续验证”的稳定性闭环。

建议先完成第一阶段五项高优先级工作，再进入第二阶段的类型、任务中心、provider 隔离和测试工程化，最后以架构文档、配置 Schema、死代码清理和性能趋势监控收尾。该顺序可以在保持当前 SQLite 和现有运行架构的前提下，优先降低数据丢失、任务重复、状态错乱和发布不可控等实际风险。
# 统一指令网关 500 错误修复报告
> **报告日期**: 2026-08-13
> **修复范围**: `POST /api/v1/system/command` 接口 500 错误
> **修复状态**: ✅ 全部完成
---
## 一、问题描述
系统日志显示以下错误：
```
2026-08-13 01:41:15,816 [ERROR] api.system: [API] 统一指令网关异常崩溃: OptimizationApplier.fetch_active_rules() missing 2 required positional arguments: 'scope' and 'context_features'
INFO:     127.0.0.1:64131 - "POST /api/v1/system/command HTTP/1.1" 500 Internal Server Error
```
**根因**: `OptimizationApplier.fetch_active_rules()` 方法被调用时缺少 `scope` 和 `context_features` 两个必需的位置参数。
---
## 二、根因分析
### 2.1 方法定义
`services/optimization_applier.py` 中的 `fetch_active_rules` 方法定义如下：
```python
async def fetch_active_rules(
    self,
    scope: str | None = None,
    context_features: dict[str, Any] | None = None,
    **kwargs: Any
) -> list[OptimizationRule]:
```
- `scope`: 规则作用域过滤条件（如 `"MODEL_DISPATCH"`、`"TASK_SPLIT"` 等），为 `None` 时查询所有激活规则。
- `context_features`: 上下文特征字典，用于规则条件匹配。包含请求的关键特征（如 `command_type`、`estimated_length` 等）。
### 2.2 调用链
`POST /api/v1/system/command` → `api/system.py::execute_command()` → `services/global_router.py::route_command()` → `self.optimization_applier.fetch_active_rules()`
### 2.3 问题原因
在**旧版本代码**中，`fetch_active_rules` 的签名可能要求 `scope` 和 `context_features` 为**必需位置参数**（没有默认值），而 `global_router.py` 调用时未传入这两个参数，导致 500 错误。
---
## 三、修复方案
### 修复 1: 加固 `fetch_active_rules` 方法签名
**文件**: `C:\Users\11482\Documents\aiilie\services\optimization_applier.py`
**修改内容**:
- 为 `scope` 和 `context_features` 添加默认值 `None`，使其成为**可选参数**。
- 添加 `**kwargs` 以兼容任何调用方式（包括不传参数、传位置参数、传关键字参数）。
- 完善方法文档字符串，说明参数含义和来源。
### 修复 2: 加固调用点传参
**文件**: `C:\Users\11482\Documents\aiilie\services\global_router.py`
**修改内容**:
- 在 `route_command()` 的 `CREATION` 分支中，构造 `context_features` 字典（包含 `command_type`、`command_text`、`estimated_length`、`project_id`）。
- 显式传入 `scope=None` 和 `context_features=context_features`，确保规则匹配引擎能根据当前请求上下文准确匹配优化规则。
---
## 四、验证结果
### 4.1 接口验证
| 测试用例 | 请求体 | 状态码 | 结果 |
|----------|--------|--------|------|
| 创作指令 | `{"command": "帮我写第一章，注重环境描写"}` | 200 | ✅ PASSED |
| 反思指令 | `{"command": "进行系统反思"}` | 200 | ✅ PASSED |
| 量化指令 | `{"command": "量化入库"}` | 200 | ✅ PASSED |
| 项目指令 | `{"command": "新建项目"}` | 200 | ✅ PASSED |
| 数字指令 | `{"command": "1"}` | 200 | ✅ PASSED |
| 空指令 | `{"command": ""}` | 200 | ✅ PASSED |
### 4.2 其他接口验证
| 接口 | 状态码 | 结果 |
|------|--------|------|
| `GET /health` | 200 | ✅ PASSED |
| `GET /api/v1/sessions/stats` | 200 | ✅ PASSED |
| `GET /api/v1/sessions/list` | 200 | ✅ PASSED |
| `GET /` | 200 | ✅ PASSED |
### 4.3 日志确认
服务日志中**未再出现** `OptimizationApplier.fetch_active_rules()` 相关错误，`POST /api/v1/system/command` 正常返回 200。
```
[INFO] services.global_router: [GlobalRouter] 收到原始指令: 帮我写第一章，注重环境描写
[INFO] services.priority_queue: [PriorityTaskQueue] 任务入列: create_b0bf6de9, 优先级等级: LV4
[INFO] services.global_router: [GlobalRouter] 收到原始指令: 进行系统反思
[INFO] core.task_manager: 任务已提交: 509f227d-9d9f-42c2-9e6f-4f57f4f525df (优先级: 50)
[INFO] services.reflection_trigger: [Reflection] 成功注册反思任务: 509f227d-9d9f-42c2-9e6f-4f57f4f525df (Type: MANUAL)
```
---
## 五、修改文件清单
| 文件 | 修改内容 |
|------|----------|
| `C:\Users\11482\Documents\aiilie\services\optimization_applier.py` | 为 `fetch_active_rules` 添加 `scope` 和 `context_features` 默认值，添加 `**kwargs` 兼容任意调用方式 |
| `C:\Users\11482\Documents\aiilie\services\global_router.py` | 构造 `context_features` 并显式传入 `scope` 和 `context_features` 参数 |
---
## 六、总结
本次修复解决了 `POST /api/v1/system/command` 接口的 500 错误：
1. **`fetch_active_rules` 方法签名加固**：为 `scope` 和 `context_features` 添加默认值，兼容任何调用方式。
2. **调用点传参加固**：构造 `context_features` 字典并显式传入，确保规则匹配准确。
所有接口验证通过，服务正常运行，无回归问题。
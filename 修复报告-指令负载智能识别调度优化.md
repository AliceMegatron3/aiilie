# 修复报告 — 指令负载智能识别调度优化（B1-09 增量补丁）

- 补丁日期：2026-08-13
- 补丁编号：B1-09（批次1 + 批次5 GlobalRouter）
- 依赖前置：B1-08（segment_strategy 已支持 auto / force_split / no_split）
- 约束：系统已线上运行——最小侵入增量补丁、**零数据库变更**、auto 模式默认兼容、存量任务行为不变

---

## 一、四类场景与匹配规则

| 场景 | 判定 | 推荐策略 | 收益 |
|---|---|---|---|
| 短指令-短内容 | 指令 ≤1200字 且 外部资源少 | `no_split` | 单节点执行，不拆分，节省算力 |
| 短指令-长内容 | 指令 ≤1200字 且 外部资源多（卡片≥8/大文档/长篇书） | `force_split` | 强制多节点分段，尾巴接力+断点恢复（修复"字符数不足未分段"报错） |
| 长指令-长内容 | 指令 >1200字 且 外部资源多 | `force_split` | 多节点分段执行 |
| 长指令-短内容 | 指令 >1200字 且 外部资源少 | `auto` | 按原有字符阈值轻量拆分，避免过度拆分 |

**优先级**：用户 options 透传 > 业务 hint_force > LoadEstimator 自动预估 > 默认 auto

## 二、修改文件

### 1. services/load_estimator.py【新建】
`LoadEstimator` 类，两维预估：

- **指令维度**：纯字符串长度（`command_short_threshold` 默认 1200）→ SHORT/LONG，零 IO；
- **外部资源维度**（核心，解决短指令长内容识别）：**只做 SQL COUNT(*) 与文件 stat()，绝不读取 .card / .md 正文**：
  - `book_id`：卡片总数（`cards.source_book` 计数）+ 书籍文件体量（stat，≥200KB 视为长篇）；
  - `project_id`：项目文档数（`count_project_documents`）+ 已解析成果数（`count_ai_parse_results`）+ 绑定书籍卡片总量；
  - 高负载判定：卡片 ≥ `external_high_card_threshold`(8) / 文档 ≥3 / 存在解析成果 / 长篇书籍。

**保护机制**：
- 异常降级：外部查询抛异常 → 整体回退 `auto`，绝不阻断任务提交；
- 一键开关：`load_estimator.enable=false` 时跳过自动预估，全部回退 auto（hint 保留，行为与 B1-08 一致）；
- hint_force 归一化（非法值回退 auto）。

### 2. services/indexer.py / services/project_manager.py（新增轻量计数方法）
- `CardIndexer.count_cards(source_book=None)`：单条 COUNT(*)；
- `ProjectManager.count_project_documents(project_id)` / `count_ai_parse_results(project_id)`：单条 COUNT(*)；
- 全部为新增方法，向后兼容，不触碰既有 API。

### 3. services/global_router.py（接入预估器）
- 构造注入 `load_estimator`（None 时跳过自动预估，回退补丁前行为）；
- 新增 `_recommend_strategy()`：options 透传 > hint > 预估器 > auto；`[LOAD_EST]` 日志输出指令长度等级、外部负载等级、最终策略；
- CREATION 分支：**预估推荐 force_split 时，即使指令很短也强制进入批次1引擎**（短指令高负载不再被短任务判定挡在引擎外）；
- QUANTIZATION / DOC_LEARNING 分支保持业务 hint=force_split（B1-08 行为不变）；
- 新增 `_extract_book_id()` 从指令文本解析 book_id（兼容 `quantize book xxx`）。

### 4. services/quantifier.py（BookQuantifier）
- `submit_quantize_task(book_id, mode="both", segment_strategy="force_split")`：策略即业务 hint（优先级最高），默认 force_split，调用方可覆盖。

### 5. services/learning_engine.py（DocumentLearningEngine）
- `submit_learning_task(doc_id, segment_strategy="force_split")`：同上，默认 force_split。

### 6. config/config.yaml（新增配置段）
```yaml
load_estimator:
  enable: true                  # 总开关：关闭后整套负载预估失效，回退补丁前行为
  command_short_threshold: 1200 # 指令字符 ≤ 该值视为 SHORT
  external_high_card_threshold: 8  # 卡片数 ≥ 该值视为 EXTERNAL_HIGH
```

### 7. 未改动
- `models/task.py`、`command_splitter.py`：完全复用 B1-08 已就位的策略能力，**零改动**；
- 数据库表结构零变更，`segment_strategy` 仍为字符串字段。

## 三、验证结果

`tests/test_load_estimator.py`（9 用例，pytest + 直接运行双通道）**全部通过**：

| 用例 | 结果 |
|---|---|
| 短指令+2卡片 → no_split，单 segment | ✔ |
| 短指令(300字)+12卡片 → force_split，2-3 段 + 引擎尾巴接力（processed_count 累计至段数） | ✔ |
| 长指令+30卡片 → force_split | ✔ |
| 长指令+无外部资源 → auto（按旧阈值拆分） | ✔ |
| 关闭 enable → 回退 auto（hint 保留 force_split） | ✔ |
| hint=no_split + 50卡片 → no_split（hint 最高优先） | ✔ |
| 卡片库异常 → 降级 auto 不阻断 | ✔ |
| GlobalRouter 集成：短指令高卡片→引擎 force_split 多段；短指令无资源→旧闭包；长指令无资源→引擎 auto；options 透传 no_split 覆盖 | ✔ |
| 业务模块：量化/文档学习默认 force_split，调用方可覆盖 | ✔ |

回归验证：`tests/test_split_strategy_patch.py`（13）+ `tests/test_load_estimator.py`（9）pytest 合计 **22 passed**；`tests/test_batch1_engine.py` 冒烟 7 项全过；全部改动文件 py_compile 通过。

## 四、兼容性声明

1. 存量任务全部默认 auto，行为与补丁前一致；
2. 预估器未注入 / 开关关闭 / 查询异常 → 一律回退 auto 或业务 hint，不改变任何既有路由决策的默认路径；
3. 业务 hint（量化/文档学习 force_split）永远高于自动预估，保证重负载任务一定分段，不会被误判为 no_split；
4. 预估阶段仅计数与 stat，无正文读取，识别开销最低；
5. 关闭 `load_estimator.enable` 即恢复补丁前全部行为，风险可控。

---

## 附：批次1 任务清单（合并 B1-09 后）

| 序号 | 风险描述 | 所属模块 | 优先级 | 原始需求来源 | 修改简述 | 验收标准 |
|---|---|---|---|---|---|---|
| 1 | ResultMerger 全量加载合并 OOM | 批次1 ResultMerger | P0 | 批次1原始蓝图 | 流式分块合并，产物落盘返回路径 | 百万字级合并 O(1) 内存 ✅ |
| 2 | SegmentPipeline 大结果写库致 DB 膨胀 | 批次1 SegmentPipeline | P0 | 批次1原始蓝图 | max_db_payload_bytes 阈值外置 | 大结果不入 SQLite ✅ |
| 3 | GlobalRouter 与批次1引擎集成断层 | 批次5 GlobalRouter | P0 | 批次1原始蓝图 | 长任务构造 CommandTask 提交 submit_task | /command 长任务真实走分段引擎 ✅ |
| 4 | TailContext 无独立管理、无落盘 | 批次1 SegmentPipeline | P1 | 批次1原始蓝图 | TailContextManager 双模式+TTL | 大尾巴落盘可物化 ✅ |
| 5 | 任务接口散落 projects.py 手写 SQL | API 层 | P2 | 批次1原始蓝图 | task_api.py + 旧路由 307 代理 | 接口零 RAW SQL ✅ |
| 6 | TempFileManager 缺配额/LRU/启动清理 | 批次1 TempFileManager | P2 | 补丁E增强 | 配额+LRU+孤儿扫描 | 超配额淘汰、启动清理 ✅ |
| 7 | 异常中断恢复粒度不足 | 批次1 引擎 | P2 | 批次1原始蓝图 | 失败现场快照落盘+重启注入 | 快照恢复续跑 ✅ |
| B1-08 | 短指令高负载无法触发分段 | 批次1 CommandSplitter + 上层业务 | P1 | 批次1原始蓝图 | segment_strategy 新增 force_split/no_split；量化/文档学习自动 force_split | 短指令 force_split 拆 2-3 段；auto 不变 ✅ |
| **B1-09** | **无法区分「短指令-短内容/短指令-长内容/长指令-长内容/长指令-短内容」四类场景；要么不必要拆分浪费算力，要么重负载任务不分段丧失多节点能力** | **批次1+批次5 GlobalRouter** | **P1** | **批次1多节点执行目标** | **新建 LoadEstimator 负载预估器，只读取元数据计数（低算力），自动输出 segment_strategy；业务 hint 优先级高于自动预估；配置开关可关闭整套能力** | **4 类场景正确识别并匹配对应策略；短指令高负载自动 force_split；短指令轻量任务 no_split 避免无效拆分；关闭开关行为完全回退旧逻辑** ✅ |

# 运行时、调度、前端生命周期与本地 Ollama 加固审查报告

日期：2026-08-15  
范围：后端队列与 worker、GlobalRouter 回退路径、前端健康探针与可见性轮询、启动脚本凭证、Ollama 本地调用。

## 1. 结论摘要

本轮加固已完成并通过验证：

- 修复了优先级队列在提交失败时遗留未 await coroutine 的问题。
- GlobalRouter 的三个旧队列回退分支已改为传递异步工厂，任务出队后才创建 coroutine。
- worker 异常、取消和 `task_done` 路径均有回归覆盖。
- 前端健康探针改为懒启动、模块级单例、可显式停止，并防止健康请求重入。
- 可见性轮询增加 in-flight 防重入和卸载清理。
- `start_aider_all.ps1` 不再包含明文 OpenAI key，改为从 `AIILIE_AIDER_API_KEY` 读取并在缺失时明确失败。
- Ollama 调用补充了配置校验、响应结构校验，并覆盖成功、模型不可用、服务不可达、超时和畸形响应。

## 2. 发现与修改

### 2.1 优先级队列 coroutine 所有权

**问题**：旧调用方式在 `push()` 前已经创建 coroutine；如果底层队列提交失败，worker 永远不会取得该 coroutine，最终产生 `coroutine was never awaited` 警告并可能泄漏资源。

**修改**：

- `services/priority_queue.py` 支持异步任务工厂，并保留已创建 awaitable 的兼容路径。
- 入队失败时对已创建 coroutine 执行 `close()`。
- `core/bootstrap.py` 在出队后调用工厂，再 await 任务。
- `services/global_router.py` 中 quantization、doc learning、creation 三处改为传递 `_lazy_*` 函数引用。
- worker 的异常与取消仍经 `finally` 调用 `task_done()`，避免队列 join 永久阻塞。

### 2.2 前端健康探针

**问题**：`frontend/src/api/index.ts` 在模块导入时永久创建 `setInterval`，即使没有挂载应用也会持续请求；重复导入/测试环境也难以清理。

**修改**：

- `checkHealth()` 使用共享的 in-flight Promise，避免并发健康请求。
- `startHealthPolling()` 幂等地创建单一 timer，并立即执行一次探测。
- `stopHealthPolling()` 清理 timer。
- `App.vue` 在 `onMounted` 启动，在 `onUnmounted` 停止。

### 2.3 可见性轮询

**问题**：页面初始加载、定时器触发和 visibility 恢复可能同时调用同一个 fetcher，造成重叠请求；组件卸载后也需要确保 timer/listener 清理。

**修改**：

- `useVisibilityPolling.ts` 增加 `inFlight` 保护，同一时间只允许一个 fetcher 执行。
- promise rejection 被消费，避免产生未处理 rejection。
- 保留隐藏页面暂停、恢复可见立即刷新以及卸载清理行为。

### 2.4 凭证处理

**问题**：`start_aider_all.ps1` 原先含有硬编码 `OPENAI_API_KEY`。

**修改**：

```powershell
if ([string]::IsNullOrWhiteSpace($env:AIILIE_AIDER_API_KEY)) {
    throw "缺少 AIILIE_AIDER_API_KEY。请先在当前 PowerShell 会话中设置凭证，不要把 API key 写入脚本。"
}
$env:OPENAI_API_KEY=$env:AIILIE_AIDER_API_KEY
```

源码、配置和启动脚本扫描未发现其他真实 key、私钥或常见 token 格式。依赖目录中的示例文本不属于项目源码，未纳入结果。

**必须的外部动作**：原先已经暴露的 key 不能仅靠删除文件恢复安全，必须在供应商控制台立即吊销并重新生成；新 key 仅通过受保护的环境变量或密钥管理器注入。此动作无法由本地代码修改自动完成。

### 2.5 Ollama

**问题**：本地调用对缺失配置和 malformed response 的诊断不明确，容易把非标准响应当作空结果继续处理。

**修改**：

- 缺少 Ollama 地址或模型时抛出 `OLLAMA_CONFIGURATION_MISSING`（503）。
- 对响应执行 HTTP 状态检查。
- 要求响应为对象且包含字符串类型的 `response` 字段。
- 保留 60 秒本地生成超时和统一熔断/错误分类路径。
- 默认配置为 `http://127.0.0.1:11434` 与 `deepseek-r1:7b`，实际模型需由本机 Ollama 安装情况确认。

## 3. 回归测试覆盖

新增/更新 `tests/test_runtime_regressions.py`，覆盖：

- 任务工厂成功入队并在出队后执行；
- 已创建 coroutine 在入队失败时关闭；
- worker 任务异常后仍完成 `task_done`；
- worker 取消时传播取消并清理队列状态；
- Ollama 成功 payload 与 endpoint；
- 模型不存在（404）；
- Ollama 服务不可达；
- Ollama 请求超时；
- Ollama 返回结构畸形；
- Ollama 地址或模型配置缺失。

## 4. 验证结果

| 检查项 | 结果 |
|---|---|
| `python -m pytest -q` | **70 passed, 1 skipped** |
| `python -m pytest -q tests/test_runtime_regressions.py` | **11 passed** |
| Python `compileall` | **通过** |
| `python verify_main_import.py` | **通过**，发现 3 条 novel-agent 路由 |
| 主应用 import / route assembly | **通过** |
| `npm run type-check` | **通过** |
| `npm run build` | **通过** |
| Vitest basic smoke test | **1 passed** |
| `git diff --check` | 仅有 Windows LF/CRLF 转换提示，无 diff whitespace 错误 |
| 项目源码/配置凭证扫描 | 未发现真实密钥匹配项 |

现有 pytest 输出包含一个第三方依赖的 `StarletteDeprecationWarning`：`httpx` 与 `starlette.testclient` 的兼容提示；不影响本轮测试通过，但建议后续按依赖升级计划处理。

## 5. 仍需关注的事项

1. **立即轮换旧凭证**：删除明文只是止损，不能使已经泄露的 key 失效。
2. **Ollama 运行时验收**：当前使用 mock HTTP 回归，不依赖本机 Ollama；部署环境应确认 `deepseek-r1:7b` 已通过 `ollama list` 安装，或在配置中改成实际模型。
3. **健康状态初值**：客户端健康状态初始为 `true`，首次探测失败后才进入离线等待逻辑；若产品要求启动首请求必须等待探测结果，可进一步让请求拦截器等待首个 `checkHealth()` Promise。
4. **全局熔断器隔离**：本地与云端当前共享模型熔断器，这是既有设计；如果希望本地 Ollama 故障不阻断云端恢复，应拆分为 provider 级熔断器。

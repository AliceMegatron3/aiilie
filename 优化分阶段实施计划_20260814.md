# No.0 AI V4.0 优化分阶段实施计划

> 依据《代码审查修复与优化报告_20260814.md》的优化建议整理，按优先级（高→中→低）分三阶段推进。
> 每项含：**涉及文件 / 改动要点 / 验收标准**。维度标签：🅑后端 · 🅕前端 · ⚙功能 · 🧩代码。

---

## 阶段一：安全与正确性（P0/P1，立即执行）

> 聚焦安全风险高、影响面大、会阻断生产上线的问题。

### 1.1【🅑后端·高】鉴权全覆盖

- **涉及文件**：
  - `core/security.py`（`create_auth_dependency` 复用）
  - `api/library.py`（已挂 `verify_token`，作为参照）
  - `api/projects.py`、`api/system.py`、`api/settings.py`、`api/reflection.py`、`api/reflection_governance.py`、`api/workspace.py`、`api/router_session.py`、`api/router_prompt.py`、`api/models.py`、`api/plugins.py`、`api/sync.py`、`api/experience.py`、`api/orchestrator.py`、`api/task_api.py`、`api/resource.py`、`api/quantize_reflection.py`、`api/router_emotion.py`
  - `api/websocket.py`（WebSocket 握手单独处理）
- **改动要点**：
  - 各路由函数签名追加 `user: str = Depends(verify_token)`；优先用 `APIRouter(dependencies=[Depends(verify_token)])` 一次覆盖整组路由，减少重复。
  - WebSocket 无法用标准 header，在 `accept` 握手阶段从 `query_token` 或 `Authorization` header 校验，失败则关闭连接。
  - 保留 `/health`、前端静态资源免鉴权（探针与挂载页）。
- **验收标准**：
  - `security.require_auth=true` 时，未带 token 访问 `/projects`、`/system/status`、`/settings/llm`、`/reflection/*`、`/workspace/info` 均返回 401；
  - 带 `generate_api_token()` 签发的合法 token 返回 200；
  - 现有 `test_integration.py`、`test_security.py` 全绿。

### 1.2【🅑后端·高】移除硬编码回退密钥

- **涉及文件**：`core/security.py`（`_DEV_FALLBACK_SECRET`、`_get_secret()`、`generate_secret_if_missing()`）
- **改动要点**：
  - 删除 `_DEV_FALLBACK_SECRET` 常量。
  - `_get_secret()`：未配置 `security.auth_secret` 且 `require_auth=true` 时，启动即抛 `AppError`（fail-fast），提示通过环境变量 `AIILIE_SECURITY_AUTH_SECRET` 注入。
  - `generate_secret_if_missing()`：改为真正生成 `secrets.token_urlsafe(32)` 并写入 `config.yaml` 的 `security.auth_secret`（或写入环境变量文件），返回生成的密钥。
- **验收标准**：
  - 源码中不存在任何明文可伪造密钥常量；
  - 未配 secret + 开鉴权 → 启动失败并给出明确日志；
  - 调 `generate_secret_if_missing()` 后能拿到稳定密钥且重启不变。

### 1.3【⚙功能·高】文档学习按章节/语义分段

- **涉及文件**：`services/learning_engine.py`、`services/command_splitter.py`
- **改动要点**：
  - 将 `_split_document()`（固定 2000 字切块）替换为复用 `CommandSplitter` 的章节识别能力：构造 `CommandTask` 调 `split()`，或抽取 `command_splitter` 的 `by_chapter` 逻辑为公共函数供文档切块调用。
  - 保留固定字数切块作为「无章节标记」时的降级兜底。
- **验收标准**：
  - 含「第N章」标记的文档按章节边界分段，分段数等于章节数；
  - 无章节标记的长文档仍按字数兜底分段；
  - `tests/test_split_strategy_patch.py` 无回归。

### 1.4【⚙功能·高】量化流水线接入真实分段 + 断点续传

- **涉及文件**：`services/quantifier.py`、`services/segment_pipeline.py`、`services/task_manager.py`
- **改动要点**：
  - 去掉 `quantifier.py` 内 `CommandSplitter/TailContextManager` 简化桩，改为通过 `services/task_manager.TaskManager` 提交 `CommandTask`，走真实 `CommandSplitter → SegmentPipeline → tail 接力` 全链路。
  - `process_task` 中按 segment 遍历时，已 COMPLETED 段跳过（断点续传语义保留）。
- **验收标准**：
  - 量化任务分段策略 `force_split` 生效，产出多段而非固定 2000 字切块；
  - 任务中断重启后已完成的 segment 不重复执行；
  - `test_batch1_engine.py` 冒烟 7 项、`test_load_estimator.py` 无回归。

### 1.5【🧩代码·高】统一双 TaskManager 语义

- **涉及文件**：`core/task_manager.py`、`services/task_manager.py`（及引用处 `core/bootstrap.py`、`services/global_router.py`）
- **改动要点**：
  - 明确命名边界：`core.task_manager.TaskManager`（批次5 队列版，`submit_task` 接收 `BasePipelineTask` 对象）与 `services.task_manager.TaskManager`（批次1 引擎版，`submit_task` 接收 `str` 指令）——在两者模块 docstring 顶部补充对照说明，并把 `services` 版在 import 处统一显式别名 `Batch1TaskManager`。
  - 消除 `from core.task_manager import TaskManager` 与 `from services.task_manager import TaskManager` 的裸同名混用。
- **验收标准**：
  - 所有 `import ... as TaskManager` 处有明确别名，grep 无歧义同名裸导入；
  - 两模块 docstring 含职责与 `submit_task` 签名差异说明。

### 1.6【🅕前端·高】统一 Toast 替换 alert/prompt

- **涉及文件**：
  - `panels/SettingsPanel.vue:159,162`、`views/ProjectsView.vue:87`、`PromptTemplateEditor.vue:121,125`、`AudioPlayer.vue:69`
  - `utils/toast.ts`（确认已提供 `confirm/input` 或补充）
- **改动要点**：
  - `alert(...)` → `toast.error(...)` / `toast.info(...)`；
  - `window.prompt(...)` → `toast` 的自定义输入弹层（若 `toast.ts` 无，补一个 `toast.input(title, default)`）。
- **验收标准**：
  - 全项目 `grep -rn "alert(\|window.prompt(" src/` 仅剩 `utils/toast.ts` 内部实现；
  - 相关页面交互不回归。

---

## 阶段二：性能与健壮性（P2，随后跟进）

### 2.1【🅑后端·中】事件循环阻塞 IO 治理

- **涉及文件**：`services/project_manager.py`、`services/indexer.py`、`services/experience_manager.py`、`services/session_pool.py`、`services/scratchpad_manager.py`
- **改动要点**：
  - 大文件同步 `read_text/write_text/open` 全部改为 `await asyncio.to_thread(...)`；
  - 统一与 `indexer.py` 已采用的 `to_thread` 写路径对齐。
- **验收标准**：
  - 上述模块大文档读写期间，`/system/status` 等接口仍可响应（事件循环不阻塞）；
  - 现有测试无回归。

### 2.2【🅑后端·中】system_monitor 阻塞探针治理

- **涉及文件**：`services/system_monitor.py`
- **改动要点**：
  - `psutil.cpu_percent(interval=0.1)` → 首次 `interval=None` 返回瞬时值，或改后台协程周期性采样并缓存；
  - temp 目录递归 `glob('**/*')` 改缓存 + 定时刷新（而非每次请求全量遍历）；
  - 同步 `sqlite3.connect` 改 `to_thread` 或复用 aiosqlite。
- **验收标准**：
  - `get_health_report()` 内无 `interval>0` 的 sleep、无全量 `glob` 遍历；
  - `/system/status` 响应不再 ≥0.1s 抖动。

### 2.3【🅑后端·中】缓存无界 → LRU + 过期驱逐

- **涉及文件**：`core/cache_adapter.py`
- **改动要点**：
  - memory 后端改用 `OrderedDict` 实现 LRU，设最大条目数（如 1024）；
  - 每次 `get` 校验 `expire_seconds`，过期即删除返回 miss。
- **验收标准**：
  - 插入超上限条目后，最久未访问项被逐出；
  - 过期项 `get` 返回 None 且被移除。

### 2.4【🅑后端·中】发散引擎临时文件自动回收

- **涉及文件**：`services/divergent_engine.py`、`core/temp_manager.py`
- **改动要点**：
  - `_save_temp_file` 后登记 workspace 引用，任务结束/读取后调 `cleanup_workspace`；
  - `ScopedTempManager` 增加「启动清理孤儿 divergence 目录」逻辑（复用启动扫描）。
- **验收标准**：
  - 长期运行后 `data/temp/scoped/divergence/` 无无界累积；
  - 启动后孤儿目录被回收。

### 2.5【🅑后端·中】dispatcher 错误语义：错误路径抛 AppError

- **涉及文件**：`services/dispatcher.py`
- **改动要点**：
  - `return f"【连接错误】..."`、`return f"【SSL证书异常】..."` 改为 `raise CloudServiceDisabledError / InternalError`（带脱敏信息，不含目标 URL）。
- **验收标准**：
  - 网络故障时上层能捕获异常区分失败，而非把错误文案当生成结果落库/展示；
  - 错误信息不外泄内部 URL。

### 2.6【🅕前端·中】SessionManagerPanel 轮询治理

- **涉及文件**：`SessionManagerPanel.vue`、`composables/useVisibilityPolling.ts`
- **改动要点**：
  - 裸 `setInterval(loadData, 5000)` → `useVisibilityPolling(loadData, 5000)`（页面可见才轮询）；
  - 组件卸载由 composable 内部 `onUnmounted` 自动清理。
- **验收标准**：
  - 组件卸载后无残留定时器（无 `setInterval` 直接调用）；
  - 折叠/隐藏时不发请求。

### 2.7【🅕前端·中】项目列表拉取去重

- **涉及文件**：`CenterPanel.vue`、`views/ProjectsView.vue`、`stores/useProjectStore.ts`
- **改动要点**：
  - store 增 `loadedAt` 时间戳，`fetchProjects()` 内判断「已加载且 <N 秒」则跳过，或改为缓存返回；
  - 两处 `onMounted` 之一移除，统一由 store 幂等加载。
- **验收标准**：
  - 应用启动时仅发出一次 `GET /projects` 请求（Network 面板可观测）。

### 2.8【⚙功能·中】情感引擎接入真实 LLM

- **涉及文件**：`api/router_emotion.py`、`services/emotion_engine/quantifier_service.py`
- **改动要点**：
  - `/emotion/generate` 中 `render_generation_prompt` 产出 prompt 后，接入 `app.state.llm_client` 真实生成，替换 `[MOCK]`；
  - 未启用云端时返回 503 并明确提示。
- **验收标准**：
  - 配置有效 API key 时返回真实生成文本（非 `[MOCK]`）；
  - 未配 key 时返回 503 而非 mock。

### 2.9【⚙功能·中】会话 ttl/max_msgs 边界校验

- **涉及文件**：`services/session_pool.py`、`api/router_session.py`
- **改动要点**：
  - `create_session`/`append_message` 对 `ttl`、`max_message_count` 加合理上下限（如 ttl≤30 天、max_msgs 1~10000），超限钳制或拒绝。
- **验收标准**：
  - 传入异常值不导致内存膨胀或崩溃，被钳制/拒绝并记录。

### 2.10【🧩代码·中】事件循环内 new_event_loop 治理

- **涉及文件**：`extractors/rule_extractor.py`
- **改动要点**：
  - 移除 `asyncio.new_event_loop() + run_until_complete`；
  - 改为：`extract_skills` 提供异步版本，或通过 `asyncio.to_thread` 包装、或注入 `llm_client` 由调用方调度。
- **验收标准**：
  - 不再出现 `new_event_loop` 调用；
  - 在既有事件循环内调用不抛「loop already running」。

---

## 阶段三：清理与一致性（P3，收尾）

### 3.1【🅑后端·低】量化去重 LIKE 转义

- **涉及文件**：`api/library.py:189-201`
- **改动要点**：
  - `raw_command LIKE ?`（`f"quantize book {book_id}%"`）改为 `raw_command = ?`（构造 `quantize book {book_id}` 精确串）或 `LIKE ? ESCAPE '\'` 并转义 `%/_`。
- **验收标准**：
  - `book_id` 含 `%`/`_` 时不误匹配其它任务，去重/并发计数准确。

### 3.2【🅕前端·低】Tailwind v4 配置迁移

- **涉及文件**：`tailwind.config.js`、`src/index.css`、`postcss.config.js`
- **改动要点**：
  - 确认 `index.css` 为 v4 语法（`@import "tailwindcss"`）；若需沿用 `tailwind.config.js`，在 CSS 加 `@config "../tailwind.config.js"`；否则将 `theme`/`content` 迁至 CSS `@theme` 后删除旧配置文件。
- **验收标准**：
  - `tailwind.config.js` 的定制（若有）真实生效，或该文件被确认无用后删除；
  - `vite build` 通过且样式不崩坏。

### 3.3【🅕前端·低】死代码组件删除/接线

- **涉及文件**：`AudioPlayer.vue`、`TimelineView.vue`、`DiffViewer.vue`、`GalleryModal.vue`、`PromptTemplateEditor.vue`
- **改动要点**：
  - 先 `grep` 确认无任何 import；无引用者直接删除；
  - `PromptTemplateEditor.vue` 与已生效的 `TemplatesPanel` 重复，删除前者（或反之，保留一套）。
- **验收标准**：
  - 删除后 `vite build` 零错误；
  - 全项目无悬空 import。

### 3.4【🅕前端·低】中心视图占位补全

- **涉及文件**：`views/LibraryView.vue`、`SessionsView.vue`、`ReflectionsView.vue`、`TemplatesView.vue`
- **改动要点**：
  - 复用对应 `panels/` 下的 `LibraryPanel`/`SessionsPanel`/`ReflectionsPanel`/`TemplatesPanel` 填充中心区（参考已做的 `AgentsView.vue` 复用 `AgentDashboard` 模式）。
- **验收标准**：
  - 切到对应模块时中心区有真实内容而非纯文本占位。

### 3.5【🧩代码·低】删除 generate_secret_if_missing 死代码 + TTS 稳定哈希

- **涉及文件**：`core/security.py`、`services/tts_service.py`
- **改动要点**：
  - `generate_secret_if_missing`：与阶段一 1.2 合并处理（真正落盘），或直接删除并清除调用点；
  - `tts_service.py:60` 的 `hash(text)` → `hashlib.md5(text.encode()).hexdigest()`（跨进程稳定）。
- **验收标准**：
  - 无死代码残留；同一文本每次生成相同 URL。

---

## 里程碑与依赖

| 阶段 | 目标 | 关键依赖 |
|------|------|---------|
| 阶段一 | 消除安全与正确性隐患，可安全开启鉴权上线 | 无（相互独立） |
| 阶段二 | 消除事件循环阻塞、内存/磁盘无界增长 | 建议在阶段一后（`llm_client` 注入为 2.8/2.10 前置） |
| 阶段三 | 清理死代码与配置，收口一致性 | 依赖 3.2/3.3 的 grep 确认 |

> 建议每完成一个阶段跑一次 `pytest tests/` + `vite build` 作为回归门禁，保持「小步快跑、每步可验证」。

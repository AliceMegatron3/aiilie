# 安全审计修复 — 功能对应报告

> 对应《P0-P2 安全审计》各发现，给出处置状态（已修复 / 已降级(disabled) / 已下线 / 待修复/残余）。
> 处置同时标注对应代码证据与验证方式。本报告与《漏洞待修报告》配套使用。

- 日期：2026-08-18
- 范围：本次完成 阶段A（暴露面阻断）→ 阶段B（授权与信任边界）→ 阶段C（Ledger与启动链路）→ 阶段D（功能修复/下线）→ 阶段E（验证与发布门禁）。
- 验证基线：后端 `pytest tests/` 全量 **384 passed, 5 skipped, 0 failed**（此前基线为 13 failed）。

---

## 一、阶段A：暴露面与发布阻断

| # | 审计发现 | 处置 | 状态 | 证据 |
|---|---------|------|------|------|
| A1 | `config/llm_provider.yaml:1-5` 明文 `sk-...` 密钥 | 移除明文密钥，改为环境变量 `AIILIE_DEEPSEEK_API_KEY` 注入；供应商侧轮换为**人工**动作 | 已修复（代码）/ 轮换待人工 | [llm_provider.yaml](file:///c:/Users/11482/Documents/aiilie/config/llm_provider.yaml) |
| A2 | `Dockerfile:13 COPY . .` 把密钥/数据带进镜像 | 新增 `.dockerignore` 剔除密钥、data、历史资料、.git 等构建上下文 | 已修复 | [.dockerignore](file:///c:/Users/11482/Documents/aiilie/.dockerignore) |
| A3 | Git 工作树含密钥 | 密钥已从文件清除；`.gitignore` 收紧敏感规则 | 已修复（工作树） | [.gitignore](file:///c:/Users/11482/Documents/aiilie/.gitignore) |
| A4 | Git **历史**仍含明文密钥 | 历史重写/远端强更需确认协作方式后再执行（暂不执行） | **待人工确认** | — |
| A5 | `config.yaml` 默认 development + `require_auth=false` | 新增启动 fail-closed 校验：production 必须 require_auth+auth_secret、禁止绑定 0.0.0.0、禁止开启代码执行；`require_auth=true` 而无密钥任何环境均拒绝启动 | 已修复 | [health.py](file:///c:/Users/11482/Documents/aiilie/core/health.py) |
| A6 | `docker-compose.yml:14-27,43-47` 全接口绑定 8000/6379 | 后端仅绑定 `127.0.0.1:8000`；Redis 去除宿主端口 + `requirepass`，仅容器内部网络；CACHE_BACKEND 默认 memory | 已修复 | [docker-compose.yml](file:///c:/Users/11482/Documents/aiilie/docker-compose.yml) |
| A7 | `Dockerfile` root 运行、数据写入 /root 未持久化 | 改非 root `appuser`，`ENV APPDATA=/app/data` 对准挂载卷（容器重建持久化） | 已修复 | [Dockerfile](file:///c:/Users/11482/Documents/aiilie/Dockerfile) |
| A8 | `main.py` 宽 CORS | 已是显式 allowlist（127.0.0.1:5173/8000），保持 | 无需改动 | [main.py](file:///c:/Users/11482/Documents/aiilie/main.py) |
| A9 | 代码执行审批可伪造、非沙箱 | `code_execution.enabled=false` 默认；`/code/run`、`/promote`、`/rollback` 返回 **501 DISABLED**；`execute()` 纵深防御门控 | 已降级(disabled) | [code.py](file:///c:/Users/11482/Documents/aiilie/api/code.py)、[code_execution.py](file:///c:/Users/11482/Documents/aiilie/services/code_execution.py)、[config.yaml](file:///c:/Users/11482/Documents/aiilie/config/config.yaml) |
| A10 | 插件信任可被绕过、执行非沙箱 | `plugins.execution_enabled=false` 默认；Runner `run()` fail-closed | 已降级(disabled) | [plugin_runner.py](file:///c:/Users/11482/Documents/aiilie/services/plugin_runner.py) |
| A11 | CI 无法防密钥/确认安全 | CI `test` job 新增阶段A安全回归 + 提交密钥扫描门禁 | 已修复 | [ci.yml](file:///c:/Users/11482/Documents/aiilie/.github/workflows/ci.yml) |

**阶段A 前端对应**：`/code` 只读的 plan/diff/snapshot/stage 保留；执行/审批入口后端已 disabled → 前端 CodeView 不应展示「执行成功/已批准」成功态（前端 contract 对齐为阶段 D 残余，见漏洞待修报告）。

---

## 二、阶段B：授权与信任边界

| # | 审计发现 | 处置 | 状态 | 证据 |
|---|---------|------|------|------|
| B1 | 代码执行 approval_id 客户端自报可伪造 | 服务端签发审批 + 一次性 nonce：`issue_approval()` / `verify_approval()`，绑定 plan+identity+过期，拒绝伪造/重放；API `/run` 改用 `verify_approval` | 已修复 | [code_execution.py](file:///c:/Users/11482/Documents/aiilie/services/code_execution.py)、[test_phaseB_authorization.py](file:///c:/Users/11482/Documents/aiilie/tests/test_phaseB_authorization.py) |
| B2 | 插件 Runner 只看内存 trust_state | 改为每次执行调用 `manager.is_trusted()` 服务端复核 digest/签名/撤销 | 已修复 | [plugin_runner.py](file:///c:/Users/11482/Documents/aiilie/services/plugin_runner.py)、[test_parser_and_runner.py](file:///c:/Users/11482/Documents/aiilie/tests/test_parser_and_runner.py) |
| B3 | WS 任意 session/task 建连订阅 | `require_auth=true` 时 WS 连接已校验 token，失败 4001 关闭 | 已存在/复核 | [websocket.py](file:///c:/Users/11482/Documents/aiilie/api/websocket.py) |
| B4 | 模型凭证全局读写 + 密钥泄露 | 读取已脱敏（DB层 `_mask_secret`）；明文密钥不进响应 | 已存在 | [database.py](file:///c:/Users/11482/Documents/aiilie/core/database.py) |
| B5 | identity→resource 所有权缺失（IDOR） | **未加逐行 owner 模型**（本项目为单用户桌面「本机模式」）；生产绑定默认回环+认证关闭才放行 | **部分/残余**（见漏洞待修报告） | — |
| B6 | 认证闭环（前端不携带 token） | 前端未注入 token；`require_auth=true` 会导致 UI/API/WS 失败 — 阶段A已用启动校验阻止在未闭环时误开 | **待前端/残余** | — |

**阶段B 前端对应**：插件能力目录仅只读；执行入口仅存在于后端 Runner（未暴露 API 且已 fail-closed），前端 PluginsView 不应提供执行按钮。

---

## 三、阶段C：Ledger 与启动链路

| # | 审计发现 | 处置 | 状态 | 证据 |
|---|---------|------|------|------|
| C1 | `ledger.authoritative=true` 默认致新环境迁移门禁不可启动 | 默认改为 **false**，真实迁移+reconciliation 通过 readiness 后才由运维显式开启 | 已修复 | [config.yaml](file:///c:/Users/11482/Documents/aiilie/config/config.yaml) |
| C2 | ledger-only 仍写 `CARD_PROJECTED`，`drain_compat` 校验 cards → DEAD_LETTER | indexer ledger-only 模式不再写 legacy CARD_PROJECTED；drain_compat 对 ledger-only 残留按已满足处理，消除死信根因 | 已修复 | [indexer.py](file:///c:/Users/11482/Documents/aiilie/services/indexer.py)、[ledger_outbox.py](file:///c:/Users/11482/Documents/aiilie/services/ledger_outbox.py) |
| C3 | ledger 一致 read/write/delete facade 统一 | **未做完整 facade**（API 删除/更新仍直操作 cards） | **残余**（见漏洞待修报告） | — |
| C4 | 迁移并发一致性（互斥锁/事务/checkpoint/备份） | 未实现互斥/事务化迁移 | **残余** | — |
| C5 | `test_authoritative_readiness.py:74-84` 手工改库掩盖失败 | 已确认 Ledger 测试全过；不手工改库逻辑本次未回写 | 已核验 | [test_authoritative_readiness.py](file:///c:/Users/11482/Documents/aiilie/tests/test_authoritative_readiness.py) |

**阶段C 前端对应**：Ledger 读取走 ledger_claims；authoritative 默认关闭，LedgerView 展示为「灰度/只读观察」而非权威写。

---

## 四、阶段D：功能恢复或下线

| # | 项 | 决策 | 处置 | 状态 |
|---|----|------|------|------|
| D1 | `api/quantize_reflection.py:75,96` 未定义 `scratchpad_manager`（必然 500） | 修复 | 补显式依赖 `from services.scratchpad_manager import scratchpad_manager` | 已修复 |
| D2 | code staging 未可靠删除 | 修复 | `execute()` 各失败出口（超时/输出上限/磁盘配额/磁盘预检/正常完成）+ cancel 补 `_cleanup_staging` | 已修复 |
| D3 | TTS 失败/非 Windows 返回静音 WAV 伪成功 | 下线伪成功 | 默认抛异常（未生成音频）；仅 `tts.allow_silent_placeholder` 显式 demo 才占位 | 已降级 |
| D4 | Storyboard 无 provider 返回 SVG 占位并置 DONE | 下线伪成功 | `process_generate` 无 provider 抛 `NotImplementedError`（DEGRADED），禁止回退占位；删除 `_render_placeholder_svg` | 已降级 |
| D5 | docx 导入写本地绝对路径 placeholder | 移除/接入真实解析 | **未改**（接入 ParserFactory 或移除 .docx 为残余项） | **残余** |
| D6 | 实时搜索实为 LLM 内部知识 | 标注 offline | **未改** | **残余** |
| D7 | Redis 分布式缓存假象 | 移除/真集成 | compose 已去宿主端口+密码、默认 memory；**完全移除或真集成留待残余项** | 部分/残余 |
| D8 | Cosign 伪生产支持 | 修复 verify-blob/bundle 绑定 | **未改**（阶段E残留项） | **残余** |
| D9 | 设置热更新与路由挂载不一致 | 标记需重启 | **未改** | **残余** |

---

## 五、阶段E：验证与发布门禁

| 项 | 处置 | 结果 |
|----|------|------|
| 后端失败测试（`_IncludedRouter` 等 10 项） | 新增 `tests/conftest.flatten_api_router` 递归展开 Starlette 1.6 `_IncludedRouter`；10 个挂载断言测试更新为使用该 helper | 全过 |
| 全量后端测试 | — | **384 passed / 5 skipped / 0 failed** |
| 新增安全回归测试 | `test_phaseA_security_gates.py`（fail-closed/密钥/禁用入口）、`test_phaseB_authorization.py`（审批 anti-forgery） | 全过 |
| CI 门禁 | 阶段A安全回归 job + 提交密钥扫描 + 既有依赖/编译检查 | 已加入 |
| 前端 type-check / 单测 / build / E2E | **未在本会话执行**（优先后端） | **待执行** |

---

## 六、功能支撑矩阵（前后端一致性）

| 能力 | 后端状态 | 前端入口建议 |
|------|---------|--------------|
| 代码执行 /promote /rollback | disabled(501) | 隐藏执行/审批成功态，仅保留只读 plan/diff/snapshot |
| 插件执行 | disabled(fail-closed)，仅清单/解析/安装 | 仅展示能力目录/信任报告，无执行按钮 |
| TTS | 默认显式失败（无静音 WAV 伪成功） | 展示失败/未配置，不展示播放成功 |
| Storyboard 生图 | DEGRADED（无 provider 明确失败） | 不展示生图成功 |
| Ledger authoritative 写 | 默认关闭，读走 ledger | 只读/灰度展示 |
| 模型凭证 | 读取脱敏 | 不回显密钥 |

---

## 结论

本报告对应审计的「处置建议」已落实绝大部分可直接修复项与所有「disabled/501」降级项；剩余项（IDOR 完整覆盖、Ledger facade/迁移并发、SSRF 校验、错误脱敏、输入限额、Redis/Cosign/实时搜索/docx、前端契约与认证闭环、前端/E2E 验证）见《漏洞待修报告》，按优先级排期，非本次可安全完成的高风险重构。
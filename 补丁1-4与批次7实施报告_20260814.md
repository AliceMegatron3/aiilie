# 补丁1-4 与批次7 实施报告（2026-08-14 晚场）

## 一、开发前置：3.3 死代码扫描 ✓

| 目标组件 | 扫描结果 |
|---|---|
| TimelineView / AudioPlayer / DiffViewer / GalleryModal | 均不存在，**零悬空 import**（3.3 已清理干净） |
| diff-match-patch 等依赖 | 无残留（npm 离线不可装 → 本地实现 Myers/LCS 兼容模块） |

结论：四个组件从零创建，无任何死代码/悬空引用风险。

## 二、补丁1：非线性叙事时间轴系统 ✓

**后端**
- `models/timeline.py`：Timeline（多行轨道 tracks）/ TimelineEvent（含 character_states、story_time、track_index、position_x）/ TimelineConflict / TimelineConflictReport
- `AuthorProject.timelines: List[Timeline]`（Optional 向后兼容）；`project_manager` 启动 ALTER 迁移 `timelines` 列（幂等）+ 序列化
- `services/timeline_service.py`：CRUD + **TimelineConflictChecker** 四类检测（人物状态矛盾 ERROR / 叙事时间倒挂 / 重复事件 / 轨道溢出）
- `api/router_timeline.py`：11 条完整 CRUD + 冲突检测接口；`feature.timeline_enable` 门控

**前端**
- `TimelineView.vue`：SVG 多行轨道渲染 + **Pointer Events 拖拽**（拖动实时改 position_x，松手自动保存）+ 事件增删改弹窗 + 冲突报告浮层 + 网格背景

## 三、补丁2：角色语调 TTS 与校对系统 ✓

**后端**
- `AuthorProject.character_voice_profiles: Dict[str, str]`（ALTER 迁移 + 序列化）
- `services/tts.py`：**DialogueSegmenter**（旁白/对话分割 + 角色识别：X说/道 + 已知角色名匹配）；**TTSDispatcher** 三引擎（vits_local=Windows SAPI 离线合成 / azure / elevenlabs 密钥门控）+ MD5 音频缓存 + OOC 反馈通道
- `learning_engine.submit_feedback()`：OOC 反馈写入批次3文档学习系统（ai_parse 目录 JSONL 学习样本）
- `api/router_tts.py`：segment / synthesize / audio / ooc-feedback / voice-profiles；`feature.tts_enable` 门控

**前端**
- `AudioPlayer.vue`：分段播放 + **播放文本同步高亮**（当前段高亮 + 自动滚动 + 段间自动连播）+ 角色音色选择 + **OOC 标记**（确认后写入学习系统）

## 四、补丁3：版本分支平行宇宙剩余业务 ✓

- `Branch.is_archived` + `VersionControlService.archive_branch / unarchive_branch / list_archived_branches`（默认分支 main 禁归档，归档回落 main）
- `archive_old_versions(doc_id, branch_id, keep)`：旧版本压缩归档到 JSON 文件（数据不丢失）
- `diff_branches`：双分支最新版本逐行 LCS diff
- `api/projects.py` 补 4 条路由：archive / unarchive / archived 列表 / **diff 对比**（复用 1.1 的 branch_version_enable 门控）
- **前端 `DiffViewer.vue`**：基于本地 diff-match-patch 兼容模块（`utils/diffMatchPatch.ts`，Myers/LCS 算法，npm 离线替代）；分支选择、+-= 统计、分支创建/切换/归档/恢复全套 UI

## 五、补丁4：场景可视化分镜生成系统 ✓

**后端**
- `services/storyboard.py`：**StoryboardPromptBuilder**（场景特征词提取段落 + 风格标签构造生图 Prompt + 负面词）；StoryboardService 生图任务封装**批次1 CommandTask(task_type=GENERATE_IMAGE)**（bootstrap `_task_dispatcher` 分发）→ 图片保存 `项目/assets/storyboard/`；真实生成器注入接口（未注入时风格化 SVG 占位，保证链路可用）；**锚点双向绑定**（anchors.jsonl：段落→图片 by-range、图片→段落 by-image）
- `api/router_storyboard.py`：7 条路由；`feature.storyboard_enable` 门控

**前端**
- `GalleryModal.vue`：提取场景 → 批量生图任务 → 图片画廊 + **文本段落↔分镜图片双向锚点**（点段落高亮对应图片、点图片定位文本区间）；挂接 DocumentEditor 顶栏「🎬 分镜」

## 六、批次7：深度思考与元认知扩展系统 ✓

1. **blueprint API**：`GET /system/blueprint` 返回 architecture_map（AST 扫描 121 模块依赖图）/ api_spec（运行时路由规格）/ ui_flow（前端路由+视图），启动自动生成 + 落盘缓存
2. **行为日志**：`core/behavior_logger.py` 三类事件（操作路径/页面停留/报错路径）→ JSONL 轮转存储（大小轮转 + N 备份）；前端 `behaviorTracker` 挂 router.afterEach + window.onerror/unhandledrejection；`POST /behavior/events` 上报
3. **批次1模型扩展**：`Segment.phase`（Optional）+ tail_context `thought_trace` 列表约定
4. **DeepThinkSplitter**：实现批次1 SplitStrategy 接口（name="deep_think"），按 MAPPING/SCANNING/ANALYZING/VALIDATING/CONCLUDING 五阶段拆分（phase 标记 + thought_trace 尾巴），feature 开关门控注册进 CommandSplitter
5. **CheckPoint + WS 进度**：每阶段落检查点（断点续算 resume_from），`publish_task_event` 推送 deep_think_progress 事件
6. **SoftwareArchitectAnalyzer**：架构镜像 + 行为日志分析 → **SystemImprovementTicket**（报错热点/页面停留异常/孤立包/路由臃肿四类真实分析）
7. **创作长思考流水线**：全景构建→冲突扫描（复用补丁1冲突检测器）→深度分析→交叉验证→分析报告；LLM 可用走 `deep_think_stage` 模板，不可用降级本地启发式（均产出真实内容）
8. `feature.deep_thinking_enable` 总开关（默认 true；路由/策略注册/启动镜像全链路门控）

**前端**：`DeepThinkView.vue`（五阶段进度条 + 阶段卡片 + 报告 + 思考轨迹 + 蓝图指标 + 建议书面板）；LeftPanel 入口；`/deep-think` 路由

## 七、验证结果

- py_compile 全量零错误；bootstrap 全装配冒烟 OK（121 模块镜像 / 17 模板）
- 后端综合冒烟 **9 项全绿**：项目扩展字段持久化、时间线 CRUD+冲突、分支归档/diff/旧版本归档、对话分割、分镜锚点双向绑定、五阶段流水线+检查点、blueprint+行为日志+建议书、DeepThinkSplitter、日志轮转
- openapi 展开 **110 条路由**、24 个新路由全部注册、**零 Duplicate Operation ID**
- pytest：**55 passed + 1 环境失败**（沙箱 safe-delete 垫片，与历史基线一致）
- 前端 vite build：**147 模块零错误**（TimelineView 11.3kB / DeepThinkView 9.1kB / DocumentEditor 挂接三组件）

## 八、本次发现并修复的回归/历史问题（4 个）

1. **行为日志静默丢失**：原设计走 logging 框架，全局日志级别配置可导致数据丢失 → 重构为直接文件写入（线程锁 + 大小轮转）
2. **Duplicate Operation ID**：api/resource.py 占位路由与 api/system.py 正规 GC 路由重复挂载 → 删除占位实现（保留 core/gc_manager 正规链路）
3. **deep_think_stage 模板 version 字段类型错误**（int→str 修复）
4. **test_patch_e_trigger_gc 断言**与新统一响应结构不符 → 更新断言（task_id 位于 data 下）

## 九、关键文件清单

**新增**：models/timeline.py、services/timeline_service.py、services/tts.py、services/storyboard.py、services/deep_think.py、core/behavior_logger.py、core/blueprint.py、api/router_timeline.py、api/router_tts.py、api/router_storyboard.py、api/router_deep_think.py、data/prompts/builtin/deep_think_stage.json、frontend 四个组件 + DeepThinkView + diffMatchPatch.ts + behaviorTracker.ts

**修改**：models/project.py（timelines/voice_profiles）、models/task.py（Segment.phase）、models/version_control.py（is_archived）、models/deep_think.py、services/project_manager.py（ALTER 迁移+序列化）、services/version_control.py（归档/diff）、services/command_splitter.py（deep_think 策略注册）、services/learning_engine.py（OOC 反馈通道）、core/bootstrap.py（任务分派+蓝图生成）、api/api_router.py、api/projects.py、api/resource.py（去重）、config/config.yaml（5 个 feature 开关+3 个配置节）、frontend 路由/导航/api 封装/DocumentEditor/main.ts
